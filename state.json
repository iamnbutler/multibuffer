{"r": "30360728546", "READ_FIRST": "Full state lives in ISSUE #623 (month tracker) — read it each run. This file holds only what #623 does NOT record.", "rules": "main FROZEN ce545ec since 2026-03-23. VERIFY HEAD==ls-remote origin main FIRST; NO speculative PRs while frozen (134 PRs/113 RA => bottleneck is REVIEW). Writes via mcp__safeoutputs__* ONLY. PROTECTED(PR refused): CLAUDE.md, package.json, safe-outputs cfg. CAPS/run: update_issue 1, push_to_pr_branch 4, add_comment 10.", "statefile": "ONE json line, keep TINY (<=2.5KB). Put detail in #623, not here.", "ENV": "bun is NOT preinstalled on every runner — 07-28c had none. Fix: export BUN_INSTALL=$HOME/.bun; curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH (~15s, gets 1.3.14). Symlink repo node_modules into any worktree. Bash caps 60s => poll with sleep 50 or run_in_background.", "git": "CI checkout is SHALLOW (grafted at ce545ec). FIX: git fetch --unshallow origin (~35s), then git fetch origin 'refs/pull/*/head:refs/remotes/pr/*' = all 489 PR refs in ONE call. NEVER use `git diff ce545ec pr/N` to list a PR's files — bot branches sit on a PRE-ce545ec base so it reports files the PR never touched (this faked a wrong conflict-attribution table). Use `git diff $(git merge-base ce545ec pr/N) pr/N`.", "VERIFY": "Run lint AND typecheck AND test. Apply PR diff to ce545ec, NOT to the PR branch. lint 'Found N warning'+exit0 = pass. SPEED: full suite 2.6s, typecheck 3.0s => a 66-step cumulative sweep w/ both is only ~7 min. Sweeps are CHEAP; use them.", "FLAKE": "'line access is O(1)' buffer.test.ts:444 is HOST-DEPENDENT, not a fixed rate: 10 fails/30 runs on earlier runners but 0 fails/78 runs on 07-28c. #611 still the right fix. Don't re-quote '1 in 3' as universal.", "verified": "COMPLETE: all 80 non-dup RA PRs green individually on ce545ec. DON'T RE-VERIFY unless main moves.", "COMPOSE": "07-28c: (a) ALL 66 PREFIXES of the batch verified green one-by-one (merge+typecheck+test each step, 2275->2431 pass, 0 fail) => partial merges provably safe. (b) Exact conflict map built (in #623 table): 13 conflicters, 10 hit exactly ONE file; #355+#507<-#337 only, #491<-#481 only, #533<-#501/#511, #508<-#365/#438, #421<-cursor.ts, #406/#449/#342<-multibuffer.ts, #452/#479/#484/#513<-editor.ts. (c) #537 is the SOLE merges-clean-but-breaks-tsc PR (TS2554 editor.ts:509; bun test stays green). COMPOSITION WORK IS DONE — redo only when main moves or a batch lands.", "push": "push_to_pull_request_branch: local branch name MUST equal remote PR head ref. curl /pulls/N|.head.ref; git fetch origin $R; git checkout -B $R FETCH_HEAD; commit; tool pushes current branch.", "dead": "T1 dead (0 unlabelled). T3 dead (all 9 bug issues have a fix PR). T2 NOW DEAD: every open issue has an RA comment except the 3 monthly trackers (#623/#624/#626) and noise #86. 0-comment ones were #92/#315/#356 — #92 done 07-28c; #315/#356 add nothing (their work is still only in PRs #439/#355). T4 parked while frozen. Repo DORMANT: maintainer last spoke 2026-07-06.", "last": "07-28c sel T2,T6. Verified all 66 prefixes green; built exact conflict/rebase map; reconfirmed #537 tsc clash is unique; corrected flake framing to host-dependent; commented #92 (fully superseded by MERGED PR #91 — the only free close, needs nothing merged); updated #623. NO PR pushed (none red; the 13 rebases are NOT actionable until the batch lands — rebasing now would target a tree that doesn't exist). NEXT: if main MOVED => redo COMPOSE+prefix sweep, unlock speculative, submit parked CI PR; else genuinely little left — consider only re-checking for new PRs/issues.", "i623_format": "Updating #623: curl raw body -> python edit w/ assertions -> submit inline as a FULL replace. NOW 61.5KB/65.5KB (94%) — only ~4KB headroom, so NEXT RUN MUST condense TWO detailed run entries into 'Earlier July runs' before adding one. Strip the trailing '> Generated by' line AND the '<!-- gh-aw-agentic-workflow' marker or they accumulate. Never tick a box; delete actioned lines.", "reds": "10 of 80 RA PRs (12.5%) had real defects, ALL invisible to CI (bot PRs get 0 check runs). Fixed+pushed: #513,#511,#414,#362,#327 (lint: type-import sorts FIRST),#412 (TS2769 branded row vs literal — use num(x?.row ?? mbRow(-1)), NOT Biome's ?. autofix),#326. Blocked on maintainer: #449,#448,#342 (one shared semantics call; do NOT edit the pre-existing test).", "MEMORY_BROKEN": "PROVEN 07-28c: the size check has a ~32KB FIXED BASELINE unrelated to content — a 19-byte state.json still reported '32 KB' vs the 12KB limit; my 4.6KB file reported 37KB. So even an EMPTY memory dir fails validation and shrinking is futile (do not waste a run trimming). Pushes have been rejected since 07-28b. Assume this file may be STALE or missing; treat issue #623 as the source of truth."}