{"READ_FIRST":"Full state = the OPEN Monthly Activity issue (Aug=#666); read EVERY run. This file = lessons #666 lacks.","MEMLIMIT":"HARD: dir INCL .git <=12288 B; .git ~6500 => state.json MUST be <=5750 B. stat -c%s AFTER the write, du -sb, push_repo_memory. Shorten by DELETING clauses. rm -f .git/hooks/*.sample; never git gc.","rules":"main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST). Hold SPECULATIVE PRs (bottleneck=REVIEW); CORRECTNESS+SECURITY in scope, but anything that FLIPS AN ASSERTED behaviour = ISSUE not PR. safeoutputs writes CAN SILENTLY VANISH (2x) => FIRST ACTION each run = verify last run's output. BEFORE ANY FIX: grep #666 for an existing PR on that symbol (I duplicated #492=#693). PROTECTED: CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. update_issue CAP=1/run => spend on #666.","AUTOINDENT":"SETTLED 08-07e, do NOT re-litigate (evidence in #666): Enter DOES auto-indent. Dispatch editor.ts:487-489 is a bare _insertText(snap,'\\n'); the auto-indent is ONE LEVEL DOWN at :604-612. /^( +)/ = SPACES ONLY. 2 doc PRs wrong by reading the dispatch only.","TESTHARNESS":"mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; multi-cursor via dispatch({type:'addCursor',at}); getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts. noUncheckedIndexedAccess => compare ARRAYS with toEqual.","ENV":"GITHUB_TOKEN empty => mcp__github__*. Big list_*/search_* overflow to a file => python3 json.load. Unauth `curl api.github.com/repos/.../pulls/N` WORKS, cheaper. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile from REPO ROOT, ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup to a FILE. lint '1 info'+exit 0 = PASS.","git":"NEVER 'git diff ce545ec pr/N' (bot branches predate it); use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. --3way FAILS on the archive tree, use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq. RED/GREEN: `git stash push -q src/<file>` keeps the test, reverts the fix.","FLAKE":"buffer.test.ts:444 fires ~29% of full-suite runs - EXPECT it, never blame a patch (confirm by running that file alone). Also multibuffer.test.ts:1038 (#611) and :1041 (#690).","NEGATIVE":"DONE, don't redo: REDGREEN sweeps, MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, 342-vs-449, #333,#345,#678,#679,#683,#684,#685,#687,#689,#691,#692,#686,#694, ADDEXCERPT_COW(unopened), SECAUDIT(08-06c; redo only if deps change; dependabot 403 => list_global_security_advisories), SEARCH_LINEOFFSETS(=#492,dup=#693), RESOLVEINVIEWPORT(no prod callers), BUFFERROPE_ORACLE(44.7k checks: only 2 defect classes, both PR'd=#687+#546. DO NOT RE-SWEEP src/buffer), HITTEST_PATHS(08-08a: 3 consumers identical; out-of-range rows clipped by setCursor->clipPoint; webgpu's raw gutterWidth is self-consistent=missing feature), DIFFVIEW_PAIR(08-08a, incl teardown). FALSE ALARMS: dup-test-name scan; mergeability; 'Closes #N' regex; dom.ts:511 precedence; clipPoint-vs-clipOffset Math.min.","dead":"T1 dead (0 unlabelled). T2 via ISSUES dead: all 37 open issues have an RA comment (#86=dumping ground, never comment); 0 new issues since 08-05 => live T2 supply = NEW sibling-bot PRs (curl pulls?sort=created&direction=desc; newest=#694). T6 DRY x16 - BEST CHECK = ci.yml runs: every pull_request run conclusion=action_required => NO bot PR EVER had a check run. Never rescan 126.","last":"08-08a sel T6,T10. T6 dry(16th). Closed 2 veins CLEAN (hitTest paths, diff-view pair). FOUND+FILED (issue, not PR): adjustOffset src/buffer/offset.ts NON-MONOTONIC for Bias.Right on REPLACEMENTS (del>0 AND ins>0) => anchors swap order => range inverts. 360/16200 exhaustive; Left+pure-ins+pure-del=0. Cause :32-37. 1-line fix (drop `offset===edit.offset &&`) => 0 inversions, 2263 pass/1 fail = offset.test.ts:148 ONLY, which ASSERTS the old behaviour (:125 vs :148 contradict) => maintainer's call. Issue number not returned by safeoutputs - search 'non-monotonic'. NEXT: verify it exists; WrapMap-vs-oracle; undo/redo (editor.ts:1676 replays by POINT not anchor). Full detail in #666 run history.","EXPIRY":"gh-aw-expires=48h AUTO-CLOSES UNMERGED, Doc Unbloat PRs ONLY; #662,#663,#669,#686 died so. LIVE: #694 expires 2026-08-09T14:44Z. CHECK: curl pulls/N | grep gh-aw-expires.","HARNESS_TRAPS":"MUTANTS MUST DEFEAT THE HARNESS: happy-dom rects ALWAYS 0, so 'fallback 8' vs 'always 8' are indistinguishable until the 'MMMMMMMMMM' probe reports real layout (defineProperty on container.appendChild + its getBoundingClientRect). NEVER Element.prototype via `this as Element` (TS2352). Copy #689/#692 test files. Fixture must never be a file you're editing; pin via `git show ce545ec:path`. PERF: stub the fn to a constant FIRST to bound the win; ONE ARM PER PROCESS.","AUDIT_METHOD":"TWO impls of one interface => DIFF THEM (4 hits). ONE impl => BRUTE-FORCE ORACLE, or better an INVARIANT (see INVARIANTS). EXHAUSTED: renderer trio, buffer/rope, hitTest paths, diff-view pair. NEXT: WrapMap vs oracle; undo/redo stack. A comment EXPLAINING a loose assertion marks a bug worked around (dom.test.ts:606); so does a FIXTURE dodging the branch (editor.test.ts:410). Reading a dispatch case WITHOUT following into the helper = how 2 PRs got Auto-Indent wrong. Small+pure+well-tested != correct.","INVARIANTS":"BEST TECHNIQUE (08-08a): on a PURE fn test an INVARIANT, not examples - needs NO assumption about intended semantics, so it survives the 'is this by design?' objection that kills most findings here. Monotonicity caught adjustOffset, invisible to 30 hand tests. Also: idempotence, order-independence, round-trip, bounds. An ORACLE encodes MY guess at semantics; an invariant doesn't."}