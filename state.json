{
"r":"30676674690",
"READ_FIRST":"Full state = the OPEN '[Repo Assist] Monthly Activity' issue (Aug tracker; #623=July CLOSED, kept for derivations). Read it each run. This file = ONLY what it lacks. #657: cap is 12288B on the whole DIR and .git alone is ~6.4KB packed (irreducible) => keep this <=5.8KB; confirm via push_repo_memory.",
"rules":"main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST; done 08-01a). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS fixes ARE in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs (NOT '[docs]'). CAPS/run: update_issue 1, push_to_pr 4, add_comment 10, create_issue 4.",
"AUTOINDENT_SETTLED":"CLOSED — do NOT flip again. Enter DOES auto-indent. 3x by EXECUTED test; 08-01a also read the test BODY: describe('Newline auto-indent') dispatches insertNewline (NOT insertLineBelow) and asserts '    hello'+Enter -> '    hello\\n    ', 12pass/0fail. editor.ts:487 -> _insertText special-cases '\\n', inherits /^( +)/ on BOTH paths (:604-614, :654-662). #656(closed) AND #662(open) claim the opposite, citing editor.ts:487 = the dispatch site, one call short. RULE: never conclude from the dispatch site; follow the call, prefer an EXECUTED test, read the test BODY not its name.",
"CI_RUNS":"08-01a from API over ALL 90 ci.yml runs since freeze: EXACTLY 11 at action_required, on #326 #327 #362 #408 #412 #414 #435 #511 #513 #662 #663. July tracker said 12, named #655/#656 (CLOSED PRs, no run), omitted #663. Reconciles two old notes: a RUN is created but parks at action_required and never becomes a CHECK RUN on the PR — so '0 check runs' AND 'pushes create runs' are both true.",
"BINDINGS_DONE":"08-01a docs/bindings.md 'Not Yet Implemented > Indentation' claimed 5 bindings missing; ALL 5 work on ce545ec (Tab-w/-sel -> _indentLines editor.ts:490-497; Shift+Tab input-handler.ts:358; Mod+] :389; Mod+[ :393; Enter). PR branch repo-assist/fix-bindings-doc-indentation, docs-only. Other NYI entries CHECKED and ACCURATE — do NOT re-audit.",
"TESTHARNESS":"Scratch-test gotchas (3 iterations lost 08-01a): (a) mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => on Linux use ctrlKey; (b) selections via editor.extendSelectionTo(pt), NOT dispatch selectTo (leaves COLLAPSED, silently tests the wrong branch); (c) getText = snap.lines(0, snap.lineCount).join('\\n'). Copy helpers from tests/editor/indent.test.ts; scratch files must sit under tests/; rm before commit.",
"DONE_DONT_REDO":"REDGREEN, MERGE_TRUTH (all 135 merge clean; '11 conflicts' are CROSS-PR only), NOSRC_BLOCK, SWEEP, DUP_AUDIT (#468/#469 the ONLY both-twins trap), #468-vs-#479-both-needed: all done, numbers in the tracker. Redo only if main moves.",
"NEGATIVE":"Don't re-investigate: (a) dup-test-name scan = 1 hit, FALSE ALARM (#311 fine). (b) union-merge by sed-stripping markers BREAKS syntax. (c) mergeability-vs-main ANSWERED. (d) 'which bug issues lack a fix PR' = none; 'Closes #N' regex MISSES most — grep for the SYMBOL. (e) auto-indent CLOSED. (f) 'reorder to dodge conflicts' WRONG — only TRADES which PR rebases. (g) #655/#656 are CLOSED PRs.",
"OUTAGES":"#659 (PR/Implement dead since 2026-03-18), #661 (Docs/Update protected-files), #657 (memory size check): root causes in the tracker. Both workflow fixes need a human 'gh aw compile' => blocked while main frozen. docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md (HEALTHY). Others healthy 07-30b — do NOT refile.",
"ENV":"GITHUB_TOKEN empty => mcp__github__*. Large list_* overflow to payloadPath/file => parse w/ python3, never read raw. list_issues key='issues'; list_pull_requests payload = content[0].text JSON string (perPage=100, 2 pages = all 135). list_workflow_runs caps 30/page => pages 1-3 cover the post-freeze window. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile. suite+typecheck ~3s. lint 'N info'+exit0=PASS.",
"git":"NEVER 'git diff ce545ec pr/N' — bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. Clone is SHALLOW: git fetch --unshallow origin. SWEEP (if main moves): apply diffs ASCENDING w/ 'git apply --3way --index', commit each; to undo use 'git reset --hard HEAD; git clean -fd' ('checkout -- .' does NOT undo --3way). RED/GREEN: split diff '-- tests/' vs '-- src/'; tests first must FAIL, then src must PASS. tsc exit direct, NOT piped. Bash cwd PERSISTS across calls.",
"FLAKE":"'line access is O(1)' buffer.test.ts:444 host-dependent: ~2 fails per 6 runs on CLEAN ce545ec. NEVER blame a PR without re-running BOTH sides >=5x. #611 is the fix. #373/#376 lint-RED, #374/375/377/378 green (human PRs).",
"dead":"T1 dead (0 unlabelled). T3: every open BUG ISSUE has a fix PR — but 08-01a shows T3 still has fuel via defects found by AUDIT, not the issue list. T2 EXHAUSTED (all open issues have an RA comment except trackers + noise #86). T6: only NEW bot PRs are real work (11 PRs have a run, all action_required needing HUMAN approval; all 135 merge clean). T4/T5/T8/T9 parked. DORMANT: maintainer last spoke 2026-07-06; 0 tracker items ticked. 135 open PRs (113 RA), non-dup 80, dup 32.",
"last":"08-01a sel T6,T3 — both empty in classic form, so redirected into auditing my own reading-based claims; PAID OFF twice (CI-approval 12->11; stale bindings.md line -> real docs PR). Also AUGUST ROLLOVER: closed #623, opened the lean Aug tracker. NEXT: (1) keep auditing claims resting on READING — 2 of 2 were wrong; (2) new bot PRs = the only real T6 fuel; (3) if main moves, redo the sweeps."
}
