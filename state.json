{"READ_FIRST": "Full state = OPEN Monthly Activity issue (Aug=#666). Read it EVERY run. This file = ONLY operational lessons #666 lacks. NEVER duplicate numbers/verdicts/tables here.",
"MEMLIMIT": "TWO checks, both default 10KB (12288 effective), BOTH constrain me. (1) in-agent push_repo_memory MCP tool walks live dir INCL .git (~33KB boilerplate) => stopgap every run: rm -f /tmp/gh-aw/repo-memory/default/.git/hooks/*.sample. (2) push JOB measures the git PATCH = old bytes + new bytes of changed lines (.git NOT in artifact). Rewriting this file wholesale => patch ~= 2x filesize. My 08-03d push was 10217/12288 = 83%. KEEP THIS FILE <=4.5KB and prefer editing FEW lines. Diagnosed 08-04a on #677 (Perf Improver hit check 2), corrected on #657. Real fix = exclude .git from walk AND raise max-patch-size on all 4 workflows (bare 'repo-memory: true' at perf:61 test:61 implementor:62 repo-assist:72) - both needed, neither alone.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST; done 08-04a). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS + SECURITY in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs (NOT '[docs]').",
"T2_ALIVE": "Every HUMAN issue has an RA comment, but sibling BOTS (Perf/Test Improver) + [aw] failure issues appear between runs. RULE: list issues by CREATED desc, not cursor order; these are the live T2 supply. [aw] failure issues are HIGH VALUE - read the failing JOB LOG, don't trust the issue body's 'permission/config/network' boilerplate.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt) NOT dispatch selectTo; getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts; createBuffer(id,text) NOT (text). Multi-cursor: dispatch({type:'addCursor',at:pt}).",
"ENV": "GITHUB_TOKEN empty => mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. list_pull_requests caps 100/page => MUST page. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile. Bash cap 60s => nohup+log, ABSOLUTE paths (cwd resets after heredocs). `bun test` piped to tail in nohup can flush EMPTY - redirect to a file, append EXIT=$?, re-read after sleep. lint '1 info'(biome migrate)+exit0=PASS. Repo SHALLOW: fetch --unshallow before merge-base. Trial alt toolchains in a SCRATCH dir so the repo tree stays clean.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. In an ARCHIVE tree (not a repo) --3way FAILS: use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq. A failed --3way leaves an UNMERGED index => reset --hard, re-apply only what succeeded.",
"HIGHLIGHTER_OPEN": "#672 reports linear child-scan at highlighter.ts:193 (2-4x/frame). VERIFIED + EXTENDED: SAME pattern at injection-highlighter.ts:362 and :460. InjectionHighlighter is PUBLIC (renderer/index.ts:10). Fix must be a SHARED helper or it covers 1 of 3. No open PR touches it (#377 near-miss: edits :460, only drops a dead param).",
"FLAKE": "'line access is O(1)' buffer.test.ts:444 host-dependent. MY runs 7/28 (~25%); Perf Improver INDEPENDENTLY 5/17 (~29%) => real, not one host. NEVER blame a PR without re-running BOTH sides >=5x. #611 fixes it; #673 is the write-up. #373/#376 lint-RED; #374/375/377/378 green.",
"NEGATIVE": "DONE, don't redo: fix( REDGREEN sweep, perf( sweep (22/22), MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, bindings.md NYI, 342-vs-449, #345, #333, dep audit, TS7(->#678). Don't re-investigate: dup-test-name scan FALSE ALARM; sed-stripping merge markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most, grep the SYMBOL; 'reorder to dodge conflicts' WRONG; diffLines() has NO callers (=> #337/#507 zero in-repo effect). AUTOINDENT: Enter DOES auto-indent (newline.test.ts 12/0); #656/#662 cite editor.ts:487 = DISPATCH SITE - never conclude from a dispatch site, read the test BODY. METHOD: per-file red/green is BLIND to regressions elsewhere - always finish with a FULL-SUITE run; a tool that gets faster while reporting 0 errors may not be doing the work - inject a deliberate error.",
"OUTAGES": "#659 #661 #657 #677 - causes in #666 + my comments; each needs a human 'gh aw compile', blocked while main frozen. docs-update.md SEPARATE from unbloat-docs.md + glossary-maintainer.md (HEALTHY). Don't refile.",
"DIFFLINES": "DONE 08-04b: added tests/diff/diff-lines.test.ts (NEW file on purpose - #355/#505/#507 all inject describe('diffLines') into tests/diff/diff.test.ts => they mutually CONFLICT and NONE was ever executed). 37 tests, mutation-verified 12/12 injected bugs caught. #337 (len-guard) + #507 (ref fast path) src both PASS my suite, alone AND stacked => now decidable. diffLines still has 0 callers, so perf effect still 0. WART pinned as characterisation: context:0 splits a modification into 2 hunks + absent side reports start 0 (shared buildHunks, so diff() same) - semantics call, not a bug I should silently fix.",
"dead": "T1 dead (0 unlabelled). T6 DRY x4 (bot PRs total_count:0 check runs => nothing pushable; main frozen => no new conflicts; #676 is '[docs]' = can't push). DORMANT: maintainer last spoke 2026-07-06; 0 tracker items ticked; 0 comments on #666. ~137 open PRs, dup 32 (384=386, 453=455, 505=507).",
"last": "08-04b sel T9,T6. T9=see DIFFLINES (PR opened, tests-only, full suite 2302/0). T6=DRY x4, verified not assumed. LESSON: before claiming 'X has zero coverage', grep OPEN PRs too - main had none but 3 PRs did; I had this mis-recorded. Green suite proves nothing until mutation-tested. NEXT: T10/T8 = HIGHLIGHTER_OPEN shared-helper fix, or MULTICURSOR (n^1.5, largest unexplained cost); React19 is the other open dep question."}
