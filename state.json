{"READ_FIRST": "Full state = the OPEN Monthly Activity issue (Aug=#666). Read it EVERY run. This file = ONLY operational lessons #666 lacks.",
"MEMLIMIT": "Limit counts the LIVE dir INCL .git (~6.5KB) => keep state.json <=5.4KB; `rm -f .git/hooks/*.sample` every run; never git gc (it GREW it). The push JOB separately measures the git PATCH (old+new of CHANGED lines) => dump with separators=(',\\n',': ') so each key is ONE line, and change few keys.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST). Hold on SPECULATIVE PRs (bottleneck=REVIEW); CORRECTNESS+SECURITY in scope. Writes via mcp__safeoutputs__* ONLY and one CAN SILENTLY VANISH (08-06a create_issue never landed though its PR did) => never state 'I filed X' in #666; link by SEARCH url, verify next run. PROTECTED: CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt) NOT dispatch selectTo; getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts; createBuffer(id,text); multi-cursor dispatch({type:'addCursor',at:pt}). noUncheckedIndexedAccess => compare whole ARRAYS with toEqual, never index (no `as`).",
"ENV": "GITHUB_TOKEN empty => mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. list_pull_requests caps 100/page => MUST page. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile - from REPO ROOT, ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup to a FILE. lint '1 info' + exit 0 = PASS.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. In an ARCHIVE tree --3way FAILS: use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq.",
"FLAKE": "buffer.test.ts 'line access is O(1)' (:444) ONLY under a FULL run; RECONFIRMED 08-06b: 1 fail then 0 fail on a BYTE-IDENTICAL tree => re-run before blaming your diff. #611 fixes, #673 write-up.",
"NEGATIVE": "DONE, don't redo: fix(+perf( REDGREEN sweeps, MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, bindings.md NYI, 342-vs-449, #345, #333, dep audit, #678, #679, #683, #684, #685, #687, ADDEXCERPT_COW(unopened), CANVAS_RENDERER(#689), DOMRENDERER_CHARWIDTH. Don't re-investigate: dup-test-name scan FALSE ALARM; sed-stripping merge markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most, grep the SYMBOL; Enter DOES auto-indent (#656/#662 cite a DISPATCH SITE).",
"OUTAGES": "#659 #661 #657 #677 - each needs a human `gh aw compile`; blocked while main frozen. Details in #666. docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md (both HEALTHY).",
"dead": "T1 dead (0 unlabelled). T2: ALL human issues already have an RA comment => live supply = sibling BOTS + [aw] issues only (list by CREATED desc, not cursor order; for [aw] read the failing JOB LOG, never the body boilerplate). T6 DRY x11 (total_count:0 on #689/#687/#685, base sha still ce545ec => SAMPLE 2-3, never rescan 123). ~144 open PRs, dup 32.",
"T11_FORMAT": "ONE LINE PER ITEM (#666). Evidence lives on each thread; old bodies in the EDIT HISTORY. Run History ~6 newest, drop the oldest. update_issue body is INLINE => every replace is a full retype.",
"last": "08-06b sel T6,T3. Fixed DomRenderer charWidth ZERO half (dom.ts:865 `return width||8`, copied from canvas.ts:1004) + tests/renderer/dom-charwidth.test.ts, 9 tests (6 red on main), 6/6 mutants. PR# UNKNOWN at write time (max 689) => RECONCILE: #666 links it by SEARCH url (is:pr+is:open+charWidth); swap in the number. Left the EXPLICIT-charWidth half to the maintainer (getCharWidth() JSDoc documents mount-overwrites = real semantics).",
"PERFMETHOD": "CEILING FIRST: stub the fn to a constant to bound the win before coding. ONE ARM PER PROCESS - JIT fakes ~20% gaps between arms that MUST be equal; equal control arms = the validity check. Measure the fast path's MISS. MEASURE YOUR OWN FIX FOR REGRESSION (08-05c v1 was +11%).",
"ORACLE": "NEVER let a fixture be a file you're editing (phantom diff); pin via `git show ce545ec:path`. Output oracles CANNOT see index-only bugs - also assert vs a brute-force linear scan. EQUIVALENT mutants are NOT blind spots. A mutation string occurring TWICE hits the wrong site - assert count==1 first. Re-verify the tree after a sweep.",
"ROPE_CHUNKS": "RECURRING BUG SOURCE: textToChunks breaks at a newline only if one is in the 1024 window => a >1024-unit line spans chunks STARTING MID-LINE. Bugs: byteLength (#545/#546), point<->offset (#687). line()/lines()/lineIterator audited CLEAN. Suspect only code treating prefix[ci] as the line chunk ci starts on.",
"RENDERER_HARNESS": "canvas.ts/webgpu.ts UNTESTABLE not neglected: happy-dom getContext('2d')=NULL => mount() throws. HARNESS TO COPY = tests/renderer/canvas-renderer.test.ts (PR #689); stubs getContext/OffscreenCanvas/rAF(QUEUE)/getComputedStyle. webgpu.ts needs a DEVICE stub. ALSO getBoundingClientRect().width is ALWAYS 0 => DomRenderer tests all run at charWidth 0 and a MEASURED branch needs a stub (else mutant 'return 8' survives): intercept container.appendChild + defineProperty on the probe span; do NOT patch Element.prototype via `this as Element` (TS2352).",
"AUDIT_METHOD": "Where TWO impls of one interface exist, DIFF THEM - cheapest audit found, 3 hits (both charWidth halves + dom/canvas hitTest). Left: dom/canvas render, webgpu. ALSO: a comment EXPLAINING a loose assertion marks a bug the author worked around (dom.test.ts:606 'charWidth is 0 in happy-dom, so just verify it's a number')."}