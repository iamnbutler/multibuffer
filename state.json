{"r":"30699941648","READ_FIRST":"Full state = the OPEN '[Repo Assist] Monthly Activity' issue (Aug=#666; #623=July CLOSED, kept for derivations). Read it each run. This file = ONLY what it lacks. #657: cap 12288B on the WHOLE DIR incl .git (~6.4KB) => keep state.json <=5.8KB AND run the .git stopgap (rm -rf .git/hooks; git reflog expire --expire=now --all; git gc --aggressive --prune=now), then confirm via push_repo_memory.","rules":"main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST; done 08-01c). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS fixes ARE in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs (NOT '[docs]'). CAPS/run: update_issue 1, push_to_pr 4, add_comment 10, create_issue 4.","AUTOINDENT_SETTLED":"CLOSED — do NOT flip again. Enter DOES auto-indent; 3x by EXECUTED test incl. the test BODY (tests/editor/newline.test.ts 12/0). editor.ts:487 -> _insertText special-cases '\\n' on BOTH paths (:604-614, :654-662). #656/#662 claim otherwise, citing editor.ts:487 = the DISPATCH SITE, one call short. RULE: never conclude from a dispatch site; follow the call, prefer an EXECUTED test, read the test BODY not its name.","CI_RUNS":"08-01a from API over ALL 90 ci.yml runs since freeze: EXACTLY 11 at action_required (list in tracker). A RUN is created on push to a bot branch but parks there, never becoming a CHECK RUN on the PR — so '0 check runs' AND 'pushes create runs' are both true.","BINDINGS_DONE":"08-01a docs/bindings.md NYI>Indentation listed 5 bindings that ALL work => PR #665. Other NYI entries CHECKED accurate — do NOT re-audit.","TESTHARNESS":"Scratch gotchas: (a) mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => on Linux use ctrlKey; (b) selections via editor.extendSelectionTo(pt), NOT dispatch selectTo (leaves COLLAPSED); (c) getText = snap.lines(0, snap.lineCount).join('\\n'); (d) DON'T hand-roll excerptRange — it is {context,primary} of POINT ranges, import excerptRange+createBufferId from tests/helpers.ts. Scratch files under tests/; rm before commit.","DONE_DONT_REDO":"REDGREEN, MERGE_TRUTH (all merge clean; conflicts CROSS-PR only), NOSRC_BLOCK, SWEEP, DUP_AUDIT (#468/#469 the ONLY both-twins trap), #468-vs-#479-both-needed: done, numbers in tracker. Redo only if main moves.","NEGATIVE":"Don't re-investigate: dup-test-name scan = FALSE ALARM (#311 fine); union-merge by sed-stripping markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most — grep the SYMBOL; 'reorder to dodge conflicts' WRONG; #655/#656 CLOSED.","OUTAGES":"#659 (PR/Implement dead 2026-03-18), #661 (Docs/Update protected-files), #657 (memory size): root causes in tracker. Both workflow fixes need a human 'gh aw compile' => blocked while main frozen. docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md (HEALTHY). Others healthy — do NOT refile.","ENV":"GITHUB_TOKEN empty => mcp__github__*. Large list_* overflow to a file => parse w/ python3, never read raw. list_issues key='issues'; list_pull_requests payload = content[0].text JSON string (perPage=100). list_workflow_runs caps 30/pg.bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile. suite+tc ~3s. lint '1 info'+exit0=PASS.","git":"NEVER 'git diff ce545ec pr/N' — bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. Clone SHALLOW: git fetch --unshallow origin. Apply w/ 'git apply --3way --index'; undo w/ 'git reset --hard HEAD; git clean -fd'. RED/GREEN: split diff '-- tests/' vs '-- src/'. To A/B a perf fix: git stash push <file>; measure; git stash pop. tsc exit direct, NOT piped. Bash cwd PERSISTS.","FLAKE":"'line access is O(1)' buffer.test.ts:444 host-dependent: 2/7, 2/5, 1/6 fails on CLEAN ce545ec. NEVER blame a PR without re-running BOTH sides >=5x. #611 fixes it. #373/#376 lint-RED; #374/375/377/378 green (human PRs).","dead":"T1 dead (0 unlabelled). T3 fuel = defects found by AUDIT, not the issue list. T2: re-CHECK issues by comment count, don't trust 'all commented'. T6: only NEW bot PRs are real work. T4/5/9 parked. DORMANT: maintainer last spoke 2026-07-06; 0 tracker items ticked as of 08-01c. ~136 open PRs (114 RA), non-dup 80, dup 32. Newest issues: #667, #666 (mine), #664 (FIXED).","last":"08-01c sel T3,T8 — they CONVERGED: the #664 fix is both. Opened PR (branch repo-assist/fix-issue-664-viewport-row-filter, number unknown at write time — LOOK IT UP and replace the head: query link in the tracker). NEXT: (1) audit ~90 docs/glossary.md entries vs EXECUTED behaviour; (2) hunt other direct-mutation sites bypassing Editor.dispatch (the replaceAll shape); (3) new bot PRs = only T6 fuel; (4) if main moves, redo sweeps.","READONLY_HOLE":"08-01b PROVEN by execution: SearchController.replaceAll() calls mb.edit() DIRECTLY => bypasses BOTH readOnly AND {editable:false}. replaceActive SAFE (via dispatch). #537 fixes both; I pushed applied-COUNT + 4 guard tests to it. _isEditCommand (editor.ts:1913) covers all 16 mutating cmds — the dispatch guard is COMPLETE; the hole was public API BYPASSING dispatch.","ISSUE_664_FIXED":"08-01c FIXED+PR'd. resolveResultsInViewport filtered ONLY via resolveAnchorsInViewport = EXCERPT granularity (correctly documented as a PRE-filter, types.ts:183-188) => a doc-spanning excerpt filtered NOTHING. KEY LESSON: the issue's OWN proposed patch was DEFECTIVE — filtering on startPoint.row alone DROPS a multi-line match starting above the viewport that spans INTO it. Correct = row INTERSECTION (start.row<endRow && end.row>=startRow); lower bound needs the end anchor, so _performSearch records _maxResultRowSpan to bound how far back to resolve. 5 tests, 4 RED before; ~2x faster."}
