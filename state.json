{
 "READ_FIRST": [
  "AUTHORITATIVE STATE = the OPEN Monthly issue (Aug=#666). Read ALL of it every run;",
  "if this file disagrees, TRUST #666. Here = pointers + ops facts only.",
  "update_issue body is INLINE: retype whole body, UNESCAPE entities.",
  "Verify the PREVIOUS run's safe output landed."
 ],
 "memory_mechanism_FIXED_2026-08-27": [
  "MY EARLIER NOTE HERE WAS WRONG. Corrected on #677 with the real job log.",
  "The push job does NOT count .git. It scans memory FILES only (found 1: state.json).",
  "What fails is MAX_PATCH_SIZE=10240 (effective 12288 with 20% overhead), NOT file size.",
  "Patch = removed lines + added lines. Old single-line JSON => patch = old + new + ~161.",
  "Run 33059488348 lost a whole run of state this way: 6356+8437+161 = 14954 > 12288.",
  "FIX APPLIED: this file is now MULTI-LINE json => git diffs line-by-line => tiny patches.",
  "KEEP IT MULTI-LINE. Never json.dumps without indent. Keep total under ~5500 bytes.",
  "Prefer arrays of SHORT strings: only changed lines enter the patch.",
  "The in-agent push_repo_memory MCP tool is a DIFFERENT, advisory check (the 39 KB one).",
  "Ceiling raise is #698. Directory-check issue #677 recommended for retitle-or-close."
 ],
 "screening_MUST_DO_BOTH": [
  "search_issues is scoped is:issue and NEVER returns PRs. This caused a duplicate PR.",
  "Every screen must run BOTH search_issues AND search_pull_requests.",
  "For a src/ file, also enumerate the open PRs touching it.",
  "If the finding is 'X is called too often', search the CALLEE's name, not the fix.",
  "My own back catalogue is the likeliest dup: ~153 open RA PRs, none merged.",
  "Other bots: [Perf Improver], [Test Improver]. ENUMERATE PRs; never ls-remote branch",
  "names (bot branches carry a -<hash> suffix => false 'vanished')."
 ],
 "rules": [
  "main FROZEN at ce545ec. PR only if live-on-main + red-on-main + gates green + screened.",
  "TEST-ONLY is excepted. public-API-only => PR ok but STATE 'zero in-repo callers'.",
  "pure-optimisation / dead-code / untestable / SETTINGS => ISSUE, not PR.",
  "Ambiguous semantics or exported-signature change => ISSUE unless a docstring pins it,",
  "OR SPLIT: fix the unambiguous half by PR, hand the rest over. Size findings honestly.",
  "Speculative => gated patch in a COMMENT. Never a dup PR; never push to a batch branch.",
  "ONE comment per thread per run. PROTECTED: CLAUDE.md, package.json, .github/, .claude/.",
  "Caps: update_issue 1/run, comments 10, PRs 4."
 ],
 "env": [
  "bun INTERMITTENT: `which bun || curl -fsSL https://bun.sh/install|bash` then",
  "`export PATH=$HOME/.bun/bin:$PATH` EVERY Bash call. MCP github works; gh CLI does NOT.",
  "list_issues/issue_read/search overflow => parse saved file: raw[raw.find('{'):];",
  "list_issues returns {'issues':[..]}. actions_list caps 30/page. get_job_logs WORKS.",
  "ABSOLUTE paths in scratch scripts; cwd resets. Bash 60s => run_in_background.",
  "RUNNER CHECKOUT IS depth-1: run `git fetch --unshallow -q origin` BEFORE any",
  "merge-base / rev-list / two-dot-diff / 'orphan' claim (this produced false finding #705).",
  "react-dom NOT installed. Edit replace_all matches INDENTATION; re-probe after edits."
 ],
 "gates": [
  "ALWAYS run test + typecheck + lint (bun test does NOT typecheck).",
  "CI also runs `bun run build:playground` FIRST. CLAUDE.md's build:demo does not exist (#671).",
  "BASELINE ce545ec: 2274 run / 2265 pass / 0 fail; tsc 0; lint 1 info.",
  "FLAKE buffer.test.ts 'line access is O(1)' ~27% (#673), covered by #611/#667; do not refile.",
  "noUncheckedIndexedAccess: arr[i] is T|undefined. Branded rows: num() from tests/helpers.ts."
 ],
 "ci_is_parked_not_absent": [
  "#757: ci.yml runs ARE created on bot PRs but sit at action_required. A parked run",
  "dispatches 0 jobs and publishes NO check runs => looks identical to 'CI not configured'.",
  "Cause not determined. T6 has NO CI-failure work until the gate clears."
 ],
 "dead_tasks": [
  "T1: 0 unlabelled. T7: all non-RA PRs are bot/owner authored. T6: structural no-op.",
  "T4 EXHAUSTED, re-verified 08-27: same 4 audit vulns (#675), same outdated set",
  "(8-pkg refresh + #760), 0 dependabot PRs, rest protected. T2: new issues DO appear."
 ],
 "retired_do_not_redo": [
  "Full list in #666. Negatives: resolveAnchor==resolveAnchors; createRenderer and",
  "worker/index.ts docstrings match; wrap-map quadratic = #483+#331; reDiff perf;",
  "uncalled-helper sweep; Math.min sweep; multi-cursor copy order; tile-map (#758)."
 ],
 "this_run_2026-08-27": [
  "Drew T4 (dead) + T2. Commented #756, #677; details in #666. #756 reproduced:",
  "2.59-2.88x, 4/4 rounds; left costs same as down => map built regardless of",
  "direction. Guard safe: _wrapMap read only via _getWrapMap. No PR (pure-opt)."
 ],
 "next_veins": [
  "AUTHORITATIVE list is in #666 'Future Work'. Read there.",
  "V0: a finding from an incomplete local view is a finding about my view (#705).",
  "V0b: absence of a signal != absence of the emitter - check the producer (#757).",
  "V0c NEW: two checks sharing a name may differ - read the failing job LOG (#677).",
  "V1: a clamp is only safe if its bound cannot cross the value it clamps.",
  "V2: a well-covered file is not a checked file - look at what the fixtures hold CONSTANT.",
  "V3: docstring-vs-behaviour vein, now 9+ hits.",
  "V4 NEW: one call site skipping a policy its siblings implement (editor.ts:126 vs the 3",
  "renderers). Sweep for other single-outlier call sites of a shared constructor.",
  "Targets: replace/clear APIs; multi-cursor EDIT paths; react/use-diff-view.ts;",
  "other O(document)-per-edit callers; project/adapter.ts + editor-view.ts docstrings."
 ]
}
