{
"r":"30656282878",
"READ_FIRST":"Full state = ISSUE #623; read each run. This file = ONLY what #623 lacks. SIZE: #657 measures whole DIR incl .git (~6.5KB) vs 12288B cap => keep state.json <=5.6KB. rm -f .git/hooks/*.sample each run; confirm via push_repo_memory.",
"rules":"main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST every run; done 07-31d). NO speculative PRs while frozen (bottleneck=REVIEW not supply). Writes via mcp__safeoutputs__* ONLY. PROTECTED(PR refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pull_request_branch ONLY on '[Repo Assist] ' PRs (NOT '[docs]'). CAPS/run: update_issue 1, push_to_pr 4, add_comment 10, create_issue 4.",
"VERIFY_DEPTH":"07-31d ONE error cost 2 artifacts: read editor.ts:487 'insertNewline -> _insertText(snap,\"\\n\")', concluded 'bare newline, no autoindent', STOPPED. But _insertText SPECIAL-CASES text==='\\n' and inherits /^( +)/ on BOTH paths (:604-614 single-sel, :654-662 multi-sel) => Enter DOES auto-indent. RULE: never conclude behaviour from the dispatch/switch site — follow the call, and prefer an EXECUTED test (tests/editor/newline.test.ts 'Newline auto-indent' 12pass/0fail on ce545ec) over reading.",
"DEFERRED":"push/update/comment return success but apply at workflow END. Do NOT re-read a PR/issue to verify; head SHA + body still old. 07-30c push to #435 DID land.",
"DUP_AUDIT_DONE":"07-31c COMPLETE, all 32 safe to bulk-close; #468/#469 the ONLY both-twins-marked trap. FULL survivor map + both trap write-ups live in #623 — don't duplicate here. Method lesson only: a dup audit misleads BOTH ways (title-cluster false-POS, symbol-grep false-NEG); only diffing src settles it.",
"HARNESS":"sweep: checkout -B x ce545ec; per PR ASCENDING: git diff $(merge-base ce545ec pr/N) pr/N | git apply --3way --index; COMMIT after each OK; on fail 'git reset --hard HEAD; git clean -fd' ('checkout -- .' does NOT undo --3way). RED/GREEN: split diff by '-- tests/' vs '-- src/', tests-only first (must FAIL), then src (must PASS). tsc exit direct, NOT piped. GOTCHA: Bash cwd PERSISTS across calls — a stray cd breaks relative paths AND yields empty merge-base diffs. cd repo root + wc -c the diff.",
"DONE_DONT_REDO":"All written up in #623 — don't re-derive while main sits at ce545ec: REDGREEN (7 bug PRs, 21 assertions red on main), MERGE_TRUTH (all 135 merge clean; the '11 conflicts' are CROSS-PR only), NOSRC_BLOCK (21 zero-src PRs 2375/0), SWEEP (68-PR batch 2442/0), DUP_AUDIT, #468-vs-#479-both-needed, and 07-31d #663 link/staleness verification. Redo only if main moves.",
"NEGATIVE":"Don't re-investigate: (a) dup-test-name scan = 1 hit, FALSE ALARM (#311 fine). (b) union-merge by sed-stripping markers BREAKS syntax. (c) mergeability-vs-main ANSWERED. (d) 'which bug issues lack a fix PR' = none; 'Closes #N' regex MISSES most — grep title/body for the SYMBOL, but see DUP_AUDIT_DONE TRAPS(b). (e) RETRACTED 07-31d — old note 'AUTOINDENT comments editor.ts:1311/1330 are stale' was WRONG; they are CORRECT (see VERIFY_DEPTH). Withdrawn from #623; do NOT refile. (f) 'reorder to dodge conflicts' WRONG — only TRADES which PR rebases.",
"OUTAGES":"#659 PR/Implement dead since 2026-03-18 (91bfd15 edited implementor.md frontmatter w/o recompile => ERR_CONFIG; silent as agent is skipped + conclusion gated on result!='skipped'). #661 Docs/Update names CLAUDE.md but sets no protected-files: => guard rejects WHOLE patch. BOTH need human 'gh aw compile' => blocked while main frozen. NOT contradicted by #662/#663: docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md, which are HEALTHY. Others healthy 07-30b — do NOT refile.",
"ENV":"GITHUB_TOKEN empty => mcp__github__*. Large list_* overflow to payloadPath/file => parse w/ python3, never read raw. list_issues key='issues' (also overflows); list_pull_requests payload = content[0].text JSON string. list_workflow_runs caps 30/page => action_required is a LOWER BOUND. Clone SHALLOW: git fetch --unshallow origin. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile. suite+typecheck ~3s each. lint 'N info/warning'+exit0=PASS.",
"git":"NEVER 'git diff ce545ec pr/N' — bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N.",
"FLAKE":"'line access is O(1)' buffer.test.ts:444 host-dependent: 2/7 fail on CLEAN ce545ec, 2/5 on 07-31b. NEVER blame a PR without re-running BOTH sides >=5x. #611 is the fix. Also: 0 RA PRs have ANY check run (GITHUB_TOKEN loop-prevention) = cause of backlog; #373/#376 lint-RED, #374/375/377/378 green.",
"dead":"T1 dead (0 unlabelled). T3 dead: EVERY open bug issue has a fix PR. T2 EXHAUSTED — all open issues have an RA comment except trackers #623/#624/#626 + noise #86. T6 mostly dead: no check runs to fix + all 135 merge clean => only NEW bot PRs are real T6 work. T4/T5/T8/T9 parked. DORMANT: maintainer last spoke 2026-07-06; 0 of ~150 tracker items ticked (re-checked 07-31d). Counts: 136 PRs (113 RA, 8 human), non-dup RA 80, dup 32. #545 is an ISSUE.",
"last":"07-31d sel T6,T4. T6 -> the 2 NEW [docs] PRs (only real fuel): REVERSED my 07-30d endorsement of #662 — its Auto-Indent rewrite is a REGRESSION (see VERIFY_DEPTH); commented on #662 (drop that hunk; rest incl. real anchor fix at glossary.md:386 is good) + #663 (verified clean). Withdrew the bogus auto-indent item from #623. T4 skipped: parked+frozen. NEXT: (1) AUGUST ROLLOVER DUE — close #623, open '[Repo Assist] Monthly Activity 2026-08' GROUPED, carry only unresolved; (2) re-audit tracker claims resting on READING not executed tests; (3) new bot PRs = fuel."
}
