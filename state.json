{"READ_FIRST": "Full state = the OPEN Monthly Activity issue (Aug=#666). Read it EVERY run. This file = ONLY operational lessons #666 lacks; never duplicate numbers/verdicts here.",
"MEMLIMIT": "TWO checks, both 10KB (12288 effective). (1) push_repo_memory walks the LIVE dir INCL .git (6.4-6.7KB; `rm -f .git/hooks/*.sample` EVERY run; do NOT git gc, it GREW it) => keep state.json <=5.3KB. (2) the push JOB measures the git PATCH = old+new bytes of CHANGED lines => write JSON with separators=(',\\n',': ') so each key is ONE line; a compact one-line dump rewrites everything and blows the cap.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST; done 08-04a). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS + SECURITY in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs (NOT '[docs]').",
"T2_ALIVE": "List issues by CREATED desc, not cursor order. Human issues all have an RA comment; live supply = sibling BOTS + [aw] failure issues. [aw] issues are HIGH VALUE - read the failing JOB LOG, never trust the body's 'permission/config/network' boilerplate.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt) NOT dispatch selectTo; getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts; createBuffer(id,text) NOT (text). Multi-cursor: dispatch({type:'addCursor',at:pt}).",
"ENV": "GITHUB_TOKEN empty => mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. list_pull_requests caps 100/page => MUST page. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile - run from REPO ROOT with ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup + redirect to a FILE, re-read after sleep. lint '1 info' + exit 0 = PASS.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. In an ARCHIVE tree (not a repo) --3way FAILS: use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq. A failed --3way leaves an UNMERGED index => reset --hard, re-apply only what succeeded.",
"HIGHLIGHTER_OPEN": "#672 linear child-scan at highlighter.ts:193 + injection-highlighter.ts:362/:460. My 'SHARED helper' advice was REFUTED - dropping skipChildren mis-highlighted 101/2401 rows; head logic stays DISTINCT, only the LOOP is common. Fix UNWRITTEN. A fence injects ONLY with a language info string, so a BARE fence reaches skipChildren. Test gap closed (#680).",
"FLAKE": "buffer.test.ts:444 line-access-is-O(1) host-dependent: ~25% (7/28), ~29% Perf Improver, 08-05a runner 5/12 (~40%) => rate VARIES BY RUNNER, don't quote one number. NEVER blame a PR (or credit a mutant) for a lone red without re-running BOTH sides >=4x - it faked a 'detection' for mutant M1 this run. #611 fixes it, #673 write-up. #373/#376 lint-RED; #374/375/377/378 green.",
"NEGATIVE": "DONE, don't redo: fix( REDGREEN sweep, perf( sweep (22/22), MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, bindings.md NYI, 342-vs-449, #345, #333, dep audit, TS7(#678), diffLines(#679), excerpt-summary(#683), LAZY EXCERPT SUMMARY(08-05a). Don't re-investigate: dup-test-name scan FALSE ALARM; sed-stripping merge markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most, grep the SYMBOL. AUTOINDENT: Enter DOES auto-indent; #656/#662 cite a DISPATCH SITE - read the test BODY.",
"OUTAGES": "#659 #661 #657 #677 - each needs a human `gh aw compile`; blocked while main frozen. Details in #666. docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md (both HEALTHY).",
"dead": "T1 dead (0 unlabelled). T2: all 38 open issues have an RA comment; live supply = sibling BOTS. T6 DRY x7. ~138 open PRs, dup 32 (384=386, 453=455, 505=507).",
"T11_FORMAT": "ONE LINE PER ITEM is the mandated format (rewrote #666 08-04d; it was ~2 runs from the body cap). Per-item evidence lives on each thread; old bodies in the issue EDIT HISTORY. Run History: ~6 newest entries only. update_issue takes an INLINE body => every replace is a full retype.",
"last": "08-05a sel T6(DRY x7: main ce545ec, ZERO maintainer comments since 07-25, bot PRs total_count:0),T8. SHIPPED perf PR: Excerpt.textSummary LAZY (memoised getter withLazyTextSummary + new exported withExcerptMetadata). multibuffer.ts:1085 was the ONLY Excerpt spread in src/ - a spread INVOKES the getter and defeats deferral: rebuild, don't spread. 2.15x keystroke @500exc, SAME on NEWLINE inserts (beats #362's line-count guard), 4.5x build, MISS +22%. 4/4 mutants invisible to existing suite. NEW FILES ONLY. Suite 2277/0. biome sorts multibuffer.bench BEFORE multibuffer-edit.bench. TRICK: countingSnapshot() delegates BufferSnapshot member-by-member to count lines() - typed, no 'as'. Recommended CLOSING #362.",
"PERFMETHOD": "CEILING FIRST: stub the suspect fn to a constant and measure; bounds the win before you write code (1448->585us stub, 675us achieved => report ACHIEVED not ceiling). ONE ARM PER PROCESS (not just one variant): two bench() calls in one process let JIT warm-up fake a 20% gap between arms that MUST be equal - equal control arms are the validity check. ALWAYS measure the fast path's MISS."}