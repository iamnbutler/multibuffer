{
"READ_FIRST": "Full state = OPEN Monthly issue (Aug=#666); read EVERY run; if it disagrees with this file TRUST #666. Grep #666 for BOTH '#N' AND '/pull/N'. This file = pointers + operational facts ONLY; narrative lives in #666.",
"MEMLIMIT": "Dir 12288 INCL .git; PATCH 10240 = old+new+190 => KEEP state.json <=4900 B (steady state 2N+190<=10240 => N<=5025). NEVER `git add` to measure: it leaves loose objects (26KB last time) that BLOW the dir check. Compute arithmetically; to undo: find .git/objects -type f -not -path '*/pack/*' -delete. rm -f .git/hooks/*.sample. Never git gc.",
"rules": "main FROZEN ce545ec (ls-remote FIRST). HOLD speculative src PRs (bottleneck=REVIEW, 127 open). FLIPS AN ASSERTED behaviour => comment not PR. safeoutputs CAN VANISH (2x) => verify last run FIRST. PROTECTED: CLAUDE.md, package.json, .github/, .claude/ (bun.lock, biome.json, rules/*.grit NOT). Caps: update_issue 1/run (spend on #666), comments 10. Rewrite #666: READ ALL, prepend entry, STRIP footer, FIX contradictions.",
"ENV": "MCP github WORKS (gh CLI does NOT). list_pull_requests/list_issues/big issue_read OVERFLOW to a file => parse in python. curl -sL github.com/O/R/pull/N.diff and api.github.com/repos/O/R/issues/N work unauth (pull #666 into a FILE and patch with python - do NOT retype). bun NOT preinstalled: curl -fsSL bun.sh/install|bash, PATH=$HOME/.bun/bin:$PATH. ABSOLUTE paths; cwd RESETS. Bash 60s => nohup to FILE + sentinel. NEVER git while a bg job holds the worktree.",
"GATES": "bun test does NOT typecheck. ALWAYS test+typecheck+lint. BASELINE ce545ec: 2274 ran/2265/0; lint 'Found 1 info' 168 files; typecheck prints only '$ tsc --noEmit'; bench 118/0. FLAKE buffer.test.ts 'line access is O(1)' ~2 of 4 runs; RERUN 2x before blaming a patch (grep '^\\(fail\\)', not tail).",
"git": "ce545ec is a PARENTLESS ROOT => merge-base EMPTY for the 18 pre-2026-03-23 orphans; `git log $EMPTYVAR` silently reports HEAD. APPLY THE .diff, NEVER judge by checkout. --3way FAILS, use plain `git apply`; revert = `git checkout -- .` PLUS `git clean -fdq tests/ src/ benchmarks/`.",
"SMELLS": "(7) PRIMITIVE REPLACED BY ITS BODY => READ THE CALLEE'S FAILURE RETURN (paid 2x). (13) HOISTED CACHE => check the FALL-THROUGH path AND whether the cached value round-trips. (14) GREP THE HUNK'S REAL FN NAME - diff @@ headers LIE; COUNT CALLERS. 1-line fix in N places => COUNT ALL SITES. Green-on-main test in a red PR = control or mislabel, SAY WHICH. A SWEEP THAT NEVER DRIVES THE CHANGED PATH IS A CONTROL.",
"DUPTRAP": "5 OFFENCES (latest #488=#366's twin, missed for weeks). GREP OPEN PR TITLES FOR THE FUNCTION NAME, not the number, BEFORE filing/recommending. Comment on the SURVIVOR. TWIN-DIFF retires a PR WITHOUT running it: curl both .diff, `grep -v '^index '|md5sum`. DIFFERENT md5 can still be the SAME change (#366/#488). May be SUPERSET (499>420).",
"TESTHARNESS": "tests/helpers.ts: createBuffer(id,text), excerptRange(a,b), mbRow. MB snapshot has NO .text() - use snap.lines(mbRow(0),mbRow(mb.lineCount)). biome BANS `!`. fc.string() v4=ASCII no \\n. happy-dom: rects 0, getContext('2d')=NULL. multiBuffer.edit() DECLINES cross-BUFFER ranges. Editor: dispatch({type:'addCursor',at}).",
"VEINS": "UNEXECUTED_PRS VEIN EXHAUSTED. PROBE DEBT ZERO (#437, #479/#463->PR711, #366/#488 built). TASK 6 IS A NO-OP WHILE main IS FROZEN: NO PR has ANY check run so 'unstable' is NOISE, 0 conflicts in 75 computed, /pulls/N rate-limits ~60 in (EMPTY != conflict). NEXT: Future Work in #666. T2: LEFT 461->431. SKIP 282,260,157,111,672,673,682,696,496. T1 dead. T3: review is the bottleneck. DEPS MEASURED, DO NOT REDO (detail #666): #675 bun.lock-only clears all 4 vulns. BIOME: see GRIT.",
"last": "08-14a T5+T6. T6 VERIFIED no-op: main STILL ce545ec, 0 check runs on newest PR 712, 151 open (128 RA), recent updates all my own bot comments; mergeability CANNOT change while main frozen. T5 -> Biome blocker not a new PR: see GRIT. NO PR, tree clean. HARNESS LESSON (cost a false green): fixture used 1-specifier imports, repo uses multi-specifier - fixtures MUST mirror the REAL shape.",
"SWEEPKIT": "DIFFERENTIAL SWEEP (paid 2x). Untracked script in repo root (rm before commit), import ./src/, print #SUMMARY count of cases that ACTUALLY drove the path. main->file; git apply PR.diff->file; diff. THEN MUTATE the PR's logic: mutant MUST diverge, else green means NOTHING. Layouts: disjoint/adjacent/OVERLAPPING/NESTED, ONE buf.",
"GRIT": "SOLVED-NEGATIVE, DO NOT RETRY the .grit narrowing - IMPOSSIBLE in Biome GritQL 2.5.8. (a) NO node kinds: js_import()/ts_as_expression() are INVALID and the rule then silently matches NOTHING - 0 diagnostics is the tell, there is NO error. (b) {} and $$$ unsupported in import snippets. (c) ONLY `import $_ from $_` matches, ONLY 1-specifier imports; repo's 3 sites are multi-specifier => uncoverable. LEVER: `// biome-ignore lint/plugin/no-type-assertion: expect: ...` above the line (bare `plugin:` does NOT). MEASURED: 2.5.8 3err->0 exit 0; pinned 2.4.4 gains 3 suppressions/unused WARNINGS (exit still 0, baseline 0 warn) => apply WITH the bump, not before (dirties lint vs #559 for no present gain). Patch in #666."
}