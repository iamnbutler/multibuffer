{"READ_FIRST": "Full state = the OPEN Monthly Activity issue (Aug=#666). Read it EVERY run. This file = ONLY operational lessons #666 lacks; never duplicate numbers/verdicts here.",
"MEMLIMIT": "TWO separate checks, both default 10KB (12288 effective). (1) in-agent push_repo_memory walks the LIVE dir INCL .git (~6.6KB after `rm -f .git/hooks/*.sample`, which I must re-run EVERY run) => keep state.json <=5.5KB. (2) the push JOB measures the git PATCH = old+new bytes of CHANGED LINES. So NEVER json.dumps() this file to one line - that makes the patch the whole file twice (measured 12852 = OVER cap). Keep ONE KEY PER LINE and edit few keys. Full derivation in #666.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST; done 08-04a). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS + SECURITY in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs (NOT '[docs]').",
"T2_ALIVE": "List issues by CREATED desc, not cursor order. Human issues are all commented; the live supply is sibling BOTS (Perf/Test Improver) + [aw] failure issues appearing between runs. [aw] issues are HIGH VALUE - read the failing JOB LOG, never trust the body's 'permission/config/network' boilerplate.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt) NOT dispatch selectTo; getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts; createBuffer(id,text) NOT (text). Multi-cursor: dispatch({type:'addCursor',at:pt}).",
"ENV": "GITHUB_TOKEN empty => use mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. list_pull_requests caps 100/page => MUST page. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile - and run it from the REPO ROOT with ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup + redirect to a FILE (piping to tail can flush EMPTY), append EXIT=$?, re-read after sleep. lint '1 info' + exit 0 = PASS. Repo SHALLOW: fetch --unshallow before merge-base.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. In an ARCHIVE tree (not a repo) --3way FAILS: use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq. A failed --3way leaves an UNMERGED index => reset --hard, re-apply only what succeeded.",
"HIGHLIGHTER_OPEN": "#672 linear child-scan at highlighter.ts:193 + injection-highlighter.ts:362/:460 (public API). My 'fix must be a SHARED helper' advice was REFUTED: _collectTokensWithInjectionSkip has 2 extra early-returns (minus_metadata :390, skipChildren :416); a shared helper dropped skipChildren => 101/2401 rows wrong. Head logic stays DISTINCT, only the LOOP is common. Fix UNWRITTEN. ROUTING: a fenced block injects ONLY with a language info string => a BARE fence reaches skipChildren; a language-tagged block with no loaded grammar returns ZERO tokens. 08-04c: TEST gap closed.",
"FLAKE": "'line access is O(1)' buffer.test.ts:444 is host-dependent: me 7/28 (~25%), Perf Improver INDEPENDENTLY 5/17 (~29%) => real, not one host. NEVER blame a PR for a lone red without re-running BOTH sides >=5x. #611 fixes it; #673 is the write-up. #373/#376 lint-RED; #374/375/377/378 green.",
"NEGATIVE": "DONE, don't redo: fix( REDGREEN sweep, perf( sweep (22/22), MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, bindings.md NYI, 342-vs-449, #345, #333, dep audit, TS7(->#678), diffLines tests. Don't re-investigate: dup-test-name scan FALSE ALARM; sed-stripping merge markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most, grep the SYMBOL. AUTOINDENT: Enter DOES auto-indent; #656/#662 cite a DISPATCH SITE - read the test BODY. METHOD: per-file red/green is BLIND to regressions elsewhere - always finish with a FULL suite run.",
"OUTAGES": "#659 #661 #657 #677 - full causes in #666. Each needs a human 'gh aw compile'; blocked while main frozen. docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md (both HEALTHY). Don't refile.",
"DIFFLINES": "DONE 08-04b: tests/diff/diff-lines.test.ts (NEW file on purpose - #355/#505/#507 all inject into tests/diff/diff.test.ts so they mutually CONFLICT and NONE ever ran). 37 tests, 12/12 mutants caught. #337 + #507 src both PASS, alone and stacked => now decidable. diffLines still has 0 callers => perf effect 0. context:0 splitting a modification into 2 hunks is pinned as characterisation, not a bug.",
"dead": "T1 dead (0 unlabelled). T2 dead: all 37 open issues already have an RA comment, no new issues, maintainer silent since 2026-07-06, 0 tracker items ticked. T6 DRY x5 (bot PRs total_count:0 check runs => nothing pushable; main frozen => no new conflicts). ~137 open PRs, dup 32 (384=386, 453=455, 505=507).",
"last": "08-04c sel T2,T6 BOTH verified dry, pivoted to T9: injection-highlighter.test.ts +12, suite 2277/0. MUTATION LESSONS: (1) run each mutant against the OLD test file too - the 3 boundary mutants were ALREADY caught, only 2 were mine. (2) 2 of 4 claims were WRONG: minus_metadata is an EQUIVALENT MUTANT (byte-identical, unkillable via getLineTokens) - reworded the test; startCol was a no-op until I used a fence indented INSIDE A LIST (col 2). (3) Run a CONTROL mutant to prove which class your tests bind to. NEXT: highlighter fix (3 sites, NO shared helper)."}