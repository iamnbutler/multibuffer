{"READ_FIRST":"Full state = the OPEN Monthly Activity issue (Aug=#666); read EVERY run (holds outage list, T11 format, methodology rules). This file = ONLY lessons #666 lacks.","MEMLIMIT":"HARD: dir INCL .git <=12288 B; .git ~6580 => state.json MUST be <=5600 B. stat -c%s AFTER the write, du -sb, then push_repo_memory. Shorten by DELETING clauses. rm -f .git/hooks/*.sample; never git gc.","rules":"main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST). Hold SPECULATIVE PRs (bottleneck=REVIEW); CORRECTNESS+SECURITY in scope. safeoutputs writes CAN SILENTLY VANISH (2x, incl a whole PR, tool said success) => FIRST ACTION each run = verify last run's output. BEFORE ANY FIX: grep #666 for an EXISTING PR on that symbol - 126 open bot PRs, I duplicated #492 (=#693). PROTECTED: CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. update_issue CAP=1/run => spend on #666 => can NEVER retitle a PR.","AUTOINDENT":"SETTLED 08-07e, do NOT re-litigate (full evidence in #666): Enter DOES auto-indent. insertNewline dispatch (editor.ts:487-489) IS a bare _insertText(snap,'\\n'); the auto-indent is ONE LEVEL DOWN in _insertText :604-612 (dup'd in the multi-sel branch). /^( +)/ = SPACES ONLY, tabs get NOTHING. 2 doc PRs wrong by reading the dispatch case only: #662(closed) + #694(commented, revert that 1 hunk).","TESTHARNESS":"mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; multi-cursor via dispatch({type:'addCursor',at}); getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts. noUncheckedIndexedAccess => compare whole ARRAYS with toEqual.","ENV":"GITHUB_TOKEN empty => mcp__github__*. Big list_*/search_* overflow to a file => python3 json.load. Unauth `curl api.github.com/repos/.../pulls/N` WORKS, cheaper. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile from REPO ROOT, ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup to a FILE. lint '1 info'+exit 0 = PASS.","git":"NEVER 'git diff ce545ec pr/N' (bot branches predate it); use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. ARCHIVE tree: --3way FAILS, use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq. RED/GREEN: `git stash push -q src/<file>` keeps the test, reverts the fix.","FLAKE":"buffer.test.ts:444 fires ~29% of full-suite runs - EXPECT it, never blame a patch. Also multibuffer.test.ts:1038 (same warm-ratio shape, #611 fixes both) and :1041 (cold, #690 fixes).","NEGATIVE":"DONE, don't redo: REDGREEN sweeps, MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, 342-vs-449, #333, #345, #678, #679, #683, #684, #685, #687, ADDEXCERPT_COW(unopened), #689, #691, #692, #686, #694, SECAUDIT(08-06c; redo only if deps change; dependabot 403 => list_global_security_advisories). SEARCH_LINEOFFSETS(=#492; dup=#693). RESOLVEINVIEWPORT(no prod callers). BUFFERROPE_ORACLE(08-07d, 44.7k checks: ONLY 2 defect classes on main, BOTH already PR'd = #687(97 fails)+#546(4); they compose to 0 and git-apply together cleanly. DO NOT RE-SWEEP src/buffer). FALSE ALARMS: dup-test-name scan; mergeability; 'Closes #N' regex; dom.ts:511 precedence; clipPoint-vs-clipOffset Math.min.","dead":"T1 dead (0 unlabelled). T2 via ISSUES dead: all 37 open issues have an RA comment (#86=dumping ground, never comment); 0 new issues since 08-05 => live T2 supply = NEW sibling-bot PRs (curl pulls?sort=created&direction=desc; newest=#694). T6 DRY x15 - BEST CHECK = list_workflow_runs ci.yml: every pull_request run `action_required` => NO bot PR EVER had a check run. Never rescan 126.","last":"08-07e sel T6,T2. Verified last run's 3 comments landed. T6 dry(15th). T2 LIVE for once: reviewed brand-new #694 (Doc Unbloat, glossary, opened 35min earlier) - caught its BOLD-WRONG Auto-Indent hunk (see AUTOINDENT), verified its OTHER 5 claims EXACT (UTF-16 offsets buffer/types.ts:22 + real TextSummary.bytes :65; isHunkSeparator renderer/types.ts:126; charWidth optional + gutterMode :52-70; /pr-fix inert - only 5 commands exist: perf-assist,test-assist,implement,repo-assist,unbloat; nextMultiBufferVersion module-level multibuffer.ts:559). Recommended MERGE-after-reverting-1-hunk. Removed STALE #662 from #666. Tree clean ce545ec. NEXT: reviewing NEW sibling-bot PRs on arrival = highest-yield run type, check pulls?sort=created FIRST; then the 3 hitTest paths.","EXPIRY":"gh-aw-expires=48h AUTO-CLOSES UNMERGED. Doc Unbloat PRs ONLY. #662,#663,#669,#686 ALL died so (4 now). LIVE TIMER: #694 expires 2026-08-09T14:44Z. #665 has NO timer. CHECK each run: curl pulls/N | grep gh-aw-expires.","HARNESS_TRAPS":"MUTANTS MUST DEFEAT THE HARNESS: happy-dom rects ALWAYS 0, so 'fallback 8' vs 'always 8' are indistinguishable until the 'MMMMMMMMMM' probe reports real layout (defineProperty on container.appendChild + its getBoundingClientRect). NEVER Element.prototype via `this as Element` (TS2352). Copy tests/renderer/canvas-renderer.test.ts (#689) or webgpu.test.ts (#692). Fixture must never be a file you're editing; pin via `git show ce545ec:path`. PERF: stub the fn to a constant FIRST to bound the win; ONE ARM PER PROCESS.","AUDIT_METHOD":"Where TWO impls of one interface exist, DIFF THEM (4 hits). Where ONE exists, sweep vs a BRUTE-FORCE ORACLE. RENDERER TRIO + BUFFER/ROPE EXHAUSTED => next = the 3 hitTest call paths. A comment EXPLAINING a loose assertion marks a bug worked around (dom.test.ts:606); so does a FIXTURE that dodges the branch (editor.test.ts:410 'HelloWorld'). Reading a dispatch case WITHOUT following into the helper = how 2 PRs got Auto-Indent wrong."}
