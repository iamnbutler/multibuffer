{
 "READ_FIRST": [
  "AUTHORITATIVE STATE = the OPEN Monthly issue (Sept, opened 09-01; #666 Aug is CLOSED).",
  "NEW MONTH => close the old one AND create the new one (update_issue cap is 1/run).",
  "08-30b: I DESTROYED #666's body. issue_read TRUNCATES a big body to the string",
  "'[Content too large, saved to file: X.json]' and THAT FILE DOES NOT EXIST on the runner.",
  "Retyping what you read then writes the placeholder. NEVER write a body starting '[Content'.",
  "FIX: read via `curl -s https://api.github.com/repos/iamnbutler/multibuffer/issues/N` -",
  "UNAUTH CURL WORKS, never truncates, cheap; use it for issues/pulls LISTS too (2x100).",
  "KEEP #666 <10 KB. CURL IS RATE-LIMITED ~60/hr: do the 2 list calls EARLY, then MCP."
 ],
 "memory_mechanism": [
  "in-agent push_repo_memory is ADVISORY, counts .git => ALWAYS fails (#677). IGNORE it.",
  "REAL gate = push job: MAX_FILE_SIZE 10240 AND MAX_PATCH_SIZE 10240 (eff. 12288). BOTH bind.",
  "Patch = removed + added lines => EDIT FEW LINES, never rewrite the file (tried 08-31b:",
  "Keep file <9800 B. Ceiling raise = #698. Use `with open(...)`: unflushed dump CORRUPTS."
 ],
 "screening_MUST_DO_BOTH": [
  "search_issues is scoped is:issue and NEVER returns PRs. That caused dup PR #765.",
  "PROVEN recipe: curl 2x100 /pulls -> numbers -> ONE batched fetch of every head:",
  "  refs=$(awk '{printf \"refs/pull/%s/head:refs/prs/%s \", $1,$1}' openprs.txt)",
  "  git fetch -q --no-tags origin $refs   # 180 heads, <60s",
  "then loop `git diff <merge-base> refs/prs/$n -- <file>` and grep the SYMBOL.",
  "CAUTION: #310-#345 have an ancient base => WHOLE-FILE diffs match every symbol; filter to",
  "+/- lines and read the PR's real function body before believing.",
  "My back catalogue is the likeliest dup: 158 open RA PRs, 0 merged. 08-31 it blocked TWO.",
  "Other bots: [Perf Improver], [Test Improver]. Never ls-remote branch names",
  "(bot branches carry a -<hash> suffix => false 'vanished').",
  "V18 a diff hash MUST include the FILE PATH (#691 vs #692 = same fix, different file)."
 ],
 "rules": [
  "main FROZEN at ce545ec. PR only if live-on-main + red-on-main + gates green + screened.",
  "TEST-ONLY is excepted. public-API-only => PR ok but STATE 'zero in-repo callers'.",
  "pure-optimisation / dead-code / untestable / SETTINGS => ISSUE, not PR.",
  "Ambiguous semantics => ISSUE unless a docstring pins it, OR SPLIT: PR the unambiguous",
  "half and NAME the ambiguous half in the PR body as the maintainer's call.",
  "Speculative => gated patch in a COMMENT. Never a dup PR; never push to a batch branch.",
  "ONE comment per thread per run. PROTECTED: CLAUDE.md, package.json, .github/, .claude/.",
  "Caps: update_issue 1/run, comments 10, PRs 4."
 ],
 "env": [
  "bun INTERMITTENT: `which bun || curl -fsSL https://bun.sh/install|bash` then",
  "`export PATH=$HOME/.bun/bin:$PATH` EVERY Bash call. MCP github works; gh CLI does NOT.",
  "list_issues returns {'issues':[..]}. actions_list caps 30/page. get_job_logs WORKS.",
  "ABSOLUTE paths in scratch scripts; cwd resets. Bash 60s => run_in_background.",
  "RUNNER CHECKOUT IS depth-1: `git fetch --unshallow -q origin` BEFORE any merge-base /",
  "rev-list / two-dot-diff / 'orphan' claim (produced false finding #705).",
  "react-dom NOT installed. Edit replace_all matches INDENTATION; re-probe after edits."
 ],
 "gates": [
  "ALWAYS run test + typecheck + lint (a test+tsc-only sweep MISSED #373's 12 bare `as`).",
  "WORKTREE SWEEPS: `git worktree add` has NO node_modules => EVERY test fails and the whole",
  "sweep is garbage. `ln -sfn $REPO/node_modules $WT/` + `git clean -e node_modules`.",
  "CI also runs `bun run build:playground` FIRST. CLAUDE.md's build:demo does not exist (#671).",
  "BASELINE ce545ec: 2274 run / 2265 pass / 0 fail; tsc 0; lint 1 info (#760).",
  "7 WALL-CLOCK tests fail under LOAD (6 workers/4 CPU): buffer 'line access O(1)','10k<10ms',",
  "'snapshot fast'; mb 'excerptAt O(log n)'x2,'lines<1ms'; SlotMap 'stale O(1)'; TileMgr 'Set O(1)'.",
  "ALWAYS gate MAIN through the SAME harness at the SAME load: 09-01c 111/183 'red' was really 3.",
  "tsc+lint are LOAD-INDEPENDENT (6s+2s) - trust those; re-run test fails capturing NAMES, filter.",
  "tests/fuzz/editor.fuzz.test.ts is UNSEEDED fc.assert: fires ~1/183 and blames an innocent PR.",
  "noUncheckedIndexedAccess: arr[i] is T|undefined. Branded rows: num() from tests/helpers.ts."
 ],
 "ci_is_parked_not_absent": [
  "#757: ci.yml runs ARE created on bot PRs but sit at action_required, dispatch 0 jobs and",
  "publish NO check runs => looks identical to 'CI not configured'. A run may show",
  "conclusion=failure yet dispatch 0 JOBS - CHECK JOB COUNT, not conclusion. Re-verified 08-29."
 ],
 "dead_tasks": [
  "T1: 0 unlabelled. T7: all non-RA PRs are bot/owner authored.",
  "T4 EXHAUSTED (re-confirmed 08-30d): audit=#675, wide refresh settled, rest protected.",
  "T9 V13 population is EXACTLY 2: simulateTokenRendering RETIRED 08-31b, buildDecorationMap",
  "is the LAST (extend #737, no 3rd file). react hooks untestable (react-dom absent).",
  "T6: dups DONE; conflict-free gated 09-01a; CLUSTERED gated 09-01b. No CI work until #757.",
  "T6 merge-vs-MAIN: all 183 open PRs merge CLEAN onto ce545ec. Dead avenue."
 ],
 "retired_do_not_redo": [
  "Full list in #666/#787. Negatives: resolveAnchor==resolveAnchors; createRenderer and",
  "worker/index.ts docstrings match; wrap-map quadratic = #483+#331; reDiff perf; Math.min and",
  "uncalled-helper sweeps; multi-cursor copy order; tile-map (#758); _deleteLine multi-row.",
  "glob.ts shouldTraverseDirectory = perf only (#540); injection getLineTokens => no overlap.",
  "excerpt-shift 5-way SETTLED: merge #369, close #342/#448/#449/#362-as-fix.",
  "09-02: V17 webgpu.ts:438 CLOSED - it feeds decoration?.color ONLY (no bg/gutter painted),",
  "  untestable (no GPU, no test file); in #787 Future Work. Modelled-test vein now EMPTY."
 ],
 "next_veins": [
  "AUTHORITATIVE list is #787 'Future Work' - this is just the index.",
  "V0 doubt the probe; if the real check is affordable DO NOT MODEL IT.",
  "V2 a well-covered file is not a checked file; ask what every fixture holds CONSTANT.",
  "V3 docstring-vs-behaviour (9+). V4 a call site skipping a policy its siblings",
  "  implement - RICHEST, 5 hits; the sibling that is already right is the ORACLE.",
  "V8 two PRs textually independent + semantically incompatible: ask what a PR takes",
  "  ownership of that ISN'T a line of its own file (a global, an invariant, a fixture).",
  "V9 (08-30) a prose rule in a prompt is ADVISORY - an LLM violated a ban 4 days after",
  "  it shipped (#343). An enforceable guard belongs in CONFIG, not instructions.",
  "V10 (08-30) fixing a filter in ONE sweep does not fix its TWIN - grep for the twin.",
  "V12 a docstring/TEST can pin the WRONG side: #704 pins the fix (PR ok), #695 endorses",
  "  the bug (hand over). ASK WHICH SIDE THE ORACLE IS ON before trusting it.",
  "V15 (08-31b) my fixtures hid 2 of 5 mutations: EVERY gap was whitespace, which paints",
  "  nothing, so gap COLOUR was unobservable. ASK WHAT YOUR FIXTURES HOLD CONSTANT.",
  "V16 an EQUIVALENT MUTANT is a finding, not a failure - NAME it in the PR body.",
  "V23 (09-01c) a NARROWED LOCAL TYPE silently eats fields: the excess-property check fires",
  "  only on LITERALS. highlighter.ts ColumnDecoration.style={backgroundColor?} dropped 10 of",
  "  11 DecorationStyle fields dom.ts passed in. GREP locals re-declaring a public subset.",
  "V24 (09-01d) A GREEN SUITE IS NOT EVIDENCE OF A FIX. Run the BEHAVIOUR and diff the",
  "  OBSERVABLE (I printed the excerpt's lines). #362 passed every gate and fixed nothing."
 ],
 "merge_sweep_TOOL_AND_TRAP": [
  "TOOL: `git merge-tree --write-tree --merge-base=<mb> refs/prs/A refs/prs/B` (exit!=0 =>",
  "conflict). Exact, ~30ms, no worktree. Use per-pair `git merge-base A B`, not a fixed base.",
  "TRAP, hit 3x: build each PR's file set from ALL CHANGED PATHS, not src/+tests/, and compare",
  "ALL open PRs, not RA-only. 09-01b map: 1073 sharing pairs, 127 conflicting, 23 clusters/107.",
  "AN INDEPENDENT SET IS BIASED TOWARD DUPS (identical patches never conflict): subtract dups."
 ],
 "canvas_harness_RECIPE": [
  "CanvasRenderer IS mountable under happy-dom: patch win.HTMLCanvasElement.prototype.getContext",
  "-> recording stub + define OffscreenCanvas (canvas.ts has its OWN GlyphAtlas), getComputedStyle,",
  "requestAnimationFrame. Viewport needs `width`; ALSO stub clientWidth/Height on",
  "HTMLElement.prototype or the canvas is 0px and every background fillRect has width 0.",
  "Glyph = drawImage(atlas,..,destX=args[4]) then fillRect tint; col=(destX-gutter)/cw.",
  "SPACES/TABS PAINT NO GLYPH. Live: tests/renderer/canvas-decoration.test.ts + #783 + #689."
 ],
 "this_run_2026-09-02": [
  "RUN5 (33590783929) T6+T3. #787 rewritten (11.2 KB). Maintainer has ticked NOTHING yet.",
  "T6 SWEEP tsc+lint on ALL 162 RA PRs: 159 GREEN / 3 RED. Squares with 09-01's '5 lint':",
  "  #373 #376 #413 #340 #449; I fixed 413/340, 373/376 aren't RA-titled => only #449 left",
  "  (2 noNonNullAssertion, multibuffer.test.ts:1612-13). NOT pushed - #449 is on the CLOSE",
  "  list. #311 #314 tsc=8 but merge-base 51f94a3 fails IDENTICALLY (src/react Theme) =>",
  "  stale base, NOT regression; both added to the close line in #787.",
  "T6 PUSHED 2nd commit to #737: retired decoration.test.ts's modelled buildDecorationMap.",
  "  Its OWN 1st fixture (same row, col 0->10) is INTRALINE for real dom.ts => no row bg,",
  "  while the copy claimed 'painted row'. Moved its 4 unique cases (viewport exclude, clip",
  "  at edge, last-wins, no-style skipped) to the real renderer + col-0 case. -7/+5 tests.",
  "  2280 run 0 fail, tsc 0, lint 1 info. All 5 mutation-verified.",
  "T3 = NO PR, and that IS the result. #536 -> #537 AND #364; #512 -> #513; both threads",
  "  ALREADY carry my linking comments (do NOT re-comment). #674/#761 defer a contract",
  "  choice; #695's docstring pins the bug. Only gap is PR-side: no `Closes #`."
 ]
}