{"READ_FIRST":"Full state = the OPEN Monthly Activity issue (Aug=#666); read EVERY run. This file = lessons #666 lacks.","MEMLIMIT":"HARD: state.json <=5300 B. A full rewrite counts EVERY line twice in the patch-size check, so the real cap is ~half the stated one. stat -c%s AFTER writing; rm -f .git/hooks/*.sample; never git gc.","rules":"main FROZEN ce545ec since 2026-03-23 (verify ls-remote FIRST). Hold SPECULATIVE PRs (bottleneck=REVIEW). Anything that FLIPS AN ASSERTED behaviour = ISSUE not PR. safeoutputs writes CAN SILENTLY VANISH (2x) => FIRST ACTION = verify last run's output. BEFORE ANY FIX: grep #666 for an existing PR on that symbol. PROTECTED: CLAUDE.md, package.json, .github/ .agents/ .claude/ (biome.json is NOT). update_issue CAP=1/run => spend on #666. comment cap 10.","DUPTRAP":"COSTLY 08-09c: a `duplicate` LABEL says NOTHING about whether the fix is real - all 4 bug PRs I ran were labelled duplicate, on the bulk-close list, and all 4 were REAL. FIND THE TWIN FIRST (adjacent number, same title); comment on the SURVIVOR. Twins NOT always equal: 457==458, 430==431 identical; 414 has cleaner tests than 413; 499 SUPERSEDES 420 (also guards _insertText, 2 red vs 1). Method: curl both pull/N.diff, diff <(grep -v '^index ').","TESTHARNESS":"Linux => mod is ctrlKey. dispatch({type:'addCursor',at}). getText = snap.lines(0,snap.lineCount).join(nl). helpers tests/helpers.ts (num,mbRow,mbPoint). noUncheckedIndexedAccess => compare ARRAYS with toEqual. Buffer has insert/delete/replace, NO .edit(). fc.string() v4 = ASCII 0x20-0x7E, NEVER \\n.","DOMPROBE":"WORKS (08-09c): copy the globals block from tests/editor/input-handler-dom.test.ts, PLUS window/getComputedStyle/requestAnimationFrame(cb=>cb(0))/cancelAnimationFrame, and Object.defineProperty(globalThis,'navigator',{value:{userAgent,platform,clipboard:{writeText}},configurable:true}). THEN `await import()` editor-view (AFTER globals). createEditorView(container,text) mounts a textarea; dispatch new win.KeyboardEvent('keydown',{key:'x',ctrlKey:true,bubbles:true,cancelable:true}) on it. happy-dom rects ALWAYS 0.","ENV":"GITHUB_TOKEN empty => mcp__github__*. Big list_*/search_* overflow to a file => python3 json.load. Unauth `curl api.github.com/repos/O/R/pulls/N` WORKS and is cheaper; `curl -sL github.com/O/R/pull/N.diff` = diff. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile from REPO ROOT, ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup to a FILE.","git":"NEVER 'git diff ce545ec pr/N'; use $(git merge-base ce545ec pr/N). --3way FAILS, use plain 'git apply'; revert via 'git checkout -- .'. Split a diff into test/src arms: re.split(r'(?m)^(?=diff --git )'), filter '/tests/' in first line.","FLAKE":"buffer.test.ts:444 'line access is O(1)' fires ~29% of full-suite runs - EXPECT it, never blame a patch. Also multibuffer.test.ts:1038 (#611), :1041 (#690). Baseline ce545ec = 2265 pass/0 fail.","NEGATIVE":"DONE, don't redo: REDGREEN sweeps, MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, 342-vs-449, SECAUDIT, RESOLVEINVIEWPORT, HITTEST_PATHS, DIFFVIEW_PAIR, MULTICURSOR_N15, ADDEXCERPT_COW(#688), NOOP_FAMILY(#381/387/388/391/392), #675, #492, CUT_CLIPBOARD(=#458), DELFWD_GUARD(=#499), MOVELINE_SEP(=#431), SEARCH_SORT(=#414), BIOME_LINT(#559+#535). AUDITED CLEAN: BUFFERROPE_ORACLE(=#687+#546), WRAPMAP, UNDOREDO, EXTENDSELECTION.","dead":"T1 dead (0 unlabelled). T6's REAL form = I am the only CI these PRs get => EXECUTE them (UNEXECUTED_PRS); frozen main makes no NEW conflicts. T2: per open issue (skip 666) GET /issues/N/comments, flag if none says 'Repo Assist'. T3 dry via issues. T4 live form = VALIDATE existing dep/CI/lint PRs (#675,#559,#535 done). LIVE SUPPLY = NEW SIBLING-BOT OUTPUT: poll BOTH issues AND pulls ?sort=created&direction=desc. Newest seen: PR #697, ISSUE #696.","last":"08-09c sel T6,T4. Ran 6 PRs, opened none. FOUND: EditorView cut with a COLLAPSED cursor deletes the line and puts NOTHING on the clipboard (editor-view.ts:153 uses getSelectedText; _cut uses _deleteLine) = data loss, fixed by #458. Tell = TWO IMPLS DISAGREEING: playground/demo.ts calls getCutText() 3x, src/ zero. Red/green: #499 2red->2267/0; #431 2red->2266/0; #414 1red->2266/0; #458 probe 1of2->2/2. #559 => lint ZERO diagnostics; #535 => 168->171 files. Commented 457,420,430,413,499,414,559,535; #666 rewritten.","SMELLS":"HIGHEST YIELD: (1) TWO IMPLS OF ONE BEHAVIOUR => DIFF THEM (#458). (2) PUBLIC METHOD WITH ZERO src/ CALLERS => bug waiting (getCutText, moveExcerpt). (3) A diagnostic ALWAYS present stops being read (#559). ONE impl => INVARIANT/ORACLE (monotonicity, idempotence, round-trip, bounds, any equivalence ASSERTED IN A DOC COMMENT). Also: thin generator; SELF-REFERENTIAL ORACLE. PROFILE, DON'T READ. Probe the guard's BLAST RADIUS (what the early return SKIPS).","EXPIRY":"gh-aw-expires AUTO-CLOSES UNMERGED, Doc Unbloat PRs ONLY; #662,#663,#669,#686,#694 died so. #697 exp 08-10T14:21Z. CHECK: curl pulls/N|grep gh-aw-expires.","UNEXECUTED_PRS":"BEST VEIN, paid 3x running. PRs whose Test Status admits the suite never ran. FIND: fetch open pulls (2 pages, per_page=100), regex bodies for 'bun (is|was)? not|not available|could not run|by inspection|not executed'. YIELD 08-09: 63 of 149. METHOD: find twin (DUPTRAP) -> test-arm alone (expect RED) -> full (expect GREEN) -> typecheck+lint+suite. 'Green together' proves only 'breaks nothing'. Untouched: #364,#513,#518,#526,#449."}