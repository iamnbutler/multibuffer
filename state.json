{"READ_FIRST": "Full state = OPEN Monthly Activity issue (Aug=#666). Read it EVERY run. This file = ONLY what #666 lacks: operational lessons. NEVER duplicate numbers/verdicts/tables here. #657: 12288B cap on WHOLE DIR incl .git (.git alone=6.6KB) => keep this <=5.4KB AND run stopgap (rm -rf .git/hooks; git reflog expire --expire=now --all; git gc --aggressive --prune=now), then push_repo_memory.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST; done 08-03c). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS + SECURITY fixes in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs (NOT '[docs]').",
"T2_ALIVE": "CORRECTED 08-03c - T2_DEAD WAS WRONG. Every HUMAN issue has an RA comment, but sibling BOTS (Perf Improver, Test Improver) FILE NEW ISSUES between runs (#672 #673 filed 08-02, both 0 comments). RULE: each run list issues by CREATED desc, not just cursor order; bot-filed issues are the live T2 supply. Commenting on sibling-bot issues is high-value: they propose work I've already done.",
"DEPAUDIT": "Done 08-03c, first ever. `bun audit` found 4 (3 high) - happy-dom<20.8.9 + transitive ws<8.20.1. Fixed lockfile-ONLY (^20.8.4 already admits 20.11.1) => package.json protection NOT a blocker for in-range bumps. GOTCHA: `bun update <pkg>` rewrites package.json range AND bun.lock's MIRROR of that range; must `git checkout package.json` AND patch the mirror line back, else manifest/lock disagree and a newer bun's --frozen-lockfile may reject. Re-verify both after. Remaining in-range bumps listed in #666 (biome minor can move lint output - keep separate).",
"HIGHLIGHTER_OPEN": "#672 (Perf Improver) reports linear child-scan at highlighter.ts:193 (2-4x/frame real files). VERIFIED + EXTENDED: SAME pattern at injection-highlighter.ts:362 (_collectTokens) and :460 (_collectTokensWithInjectionSkip). InjectionHighlighter is PUBLIC (renderer/index.ts:10). Fix must be a SHARED helper or it covers 1 of 3. No open PR touches it (#377 is near-miss: edits :460 but only drops a dead param).",
"SWEEP_LESSON": "Per-file red/green is BLIND to regressions in OTHER test files. Always finish with a full-suite run.",
"AUTOINDENT_SETTLED": "CLOSED, do NOT flip: Enter DOES auto-indent (newline.test.ts 12/0, 3x). #656/#662 cite editor.ts:487 = DISPATCH SITE. RULE: never conclude from a dispatch site; read the test BODY.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt) NOT dispatch selectTo; getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts; createBuffer(id,text) NOT (text). Multi-cursor: dispatch({type:'addCursor',at:pt}).",
"NEGATIVE": "DONE, don't redo: fix( REDGREEN sweep, perf( sweep (22/22), MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, bindings.md NYI, 342-vs-449, #345, #333, dep audit. Don't re-investigate: dup-test-name scan FALSE ALARM; sed-stripping merge markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most, grep the SYMBOL; 'reorder to dodge conflicts' WRONG; diffLines() has NO callers (=> #337/#507 zero in-repo effect).",
"OUTAGES": "#659 #661 #657 - causes in #666, need a human 'gh aw compile', blocked while main frozen. docs-update.md SEPARATE from unbloat-docs.md + glossary-maintainer.md (HEALTHY). Don't refile.",
"ENV": "GITHUB_TOKEN empty => mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. list_pull_requests caps 100/page => MUST page. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile. Bash cap 60s => nohup+log, ABSOLUTE paths (cwd resets after heredocs). NOTE: `bun test` piped to tail in nohup can flush EMPTY - redirect to a file and append EXIT=$?, then re-read after a sleep. lint '1 info'(biome migrate)+exit0=PASS. Repo SHALLOW: fetch --unshallow before merge-base.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. In an ARCHIVE tree (not a repo) --3way FAILS: use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq. A failed --3way leaves an UNMERGED index => reset --hard, re-apply only what succeeded.",
"FLAKE": "'line access is O(1)' buffer.test.ts:444 host-dependent. MY runs 7/28 (~25%); Perf Improver INDEPENDENTLY 5/17 (~29%) => confirmed real, not one host. NEVER blame a PR without re-running BOTH sides >=5x. #611 fixes it (exact 1-line tolerance align); #673 is the write-up. #373/#376 lint-RED; #374/375/377/378 green.",
"dead": "T1 dead (0 unlabelled). T6 DRY (bot PRs show total_count:0 check runs => nothing pushable). DORMANT: maintainer last spoke 2026-07-06; 0 tracker items ticked; 0 comments on #666. ~136 open PRs, dup 32 (384=386, 453=455, 505=507).",
"last": "08-03c sel T4,T2. T4=dep audit -> opened lockfile-only happy-dom PR (4 advisories cleared, audit now clean, 2265p/0f). T2=commented #673 (fix already exists as #611) + #672 (verified, found 2 MORE sites). Tracker comments 43->45. NEXT: T4/T10 = MULTICURSOR_OPEN or HIGHLIGHTER_OPEN investigation; no measurement backlog left."}
