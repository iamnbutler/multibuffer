{"READ_FIRST": "Full state = the OPEN Monthly Activity issue (Aug=#666). Read it EVERY run. This file = ONLY operational lessons #666 lacks; never duplicate numbers/verdicts here.",
"MEMLIMIT": "TWO checks, both 10KB (12288 effective). (1) push_repo_memory walks the LIVE dir INCL .git (~6.5KB) => keep state.json <=5.4KB; `rm -f .git/hooks/*.sample` every run; never git gc (it GREW it). (2) the push JOB measures the git PATCH = old+new of CHANGED lines => dump with separators=(',\\n',': ') so each key is ONE line.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS + SECURITY in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs.",
"T2_ALIVE": "List issues by CREATED desc, not cursor order. All HUMAN issues have an RA comment; live supply = sibling BOTS + [aw] issues. [aw] = HIGH VALUE: read the failing JOB LOG, never trust the body's boilerplate.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt) NOT dispatch selectTo; getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts (createBufferId/row/point/expectOffset); createBuffer(id,text). Multi-cursor: dispatch({type:'addCursor',at:pt}). tsconfig has noUncheckedIndexedAccess => arr[i] is T|undefined; compare whole ARRAYS with toEqual instead of indexing (no `as` allowed).",
"ENV": "GITHUB_TOKEN empty => mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. list_pull_requests caps 100/page => MUST page. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile - run from REPO ROOT with ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup + redirect to a FILE, re-read after sleep. lint '1 info' + exit 0 = PASS.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. In an ARCHIVE tree --3way FAILS: use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq.",
"FLAKE": "buffer.test.ts 'line access is O(1)' (:444) ONLY reproduces under a FULL suite run (file alone 8/8 green) => always compare FULL-suite vs FULL-suite. Rate VARIES BY RUNNER. #611 fixes, #673 write-up.",
"NEGATIVE": "DONE, don't redo: fix( + perf( REDGREEN sweeps, MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, bindings.md NYI, 342-vs-449, #345, #333, dep audit, #678, #679, #683, #684, #685, #687, ADDEXCERPT_COW(08-05d, unopened), CANVAS_RENDERER(08-06a). Don't re-investigate: dup-test-name scan FALSE ALARM; sed-stripping merge markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most, grep the SYMBOL. AUTOINDENT: Enter DOES auto-indent; #656/#662 cite a DISPATCH SITE - read the test BODY.",
"OUTAGES": "#659 #661 #657 #677 - each needs a human `gh aw compile`; blocked while main frozen. Details in #666. docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md (both HEALTHY).",
"dead": "T1 dead (0 unlabelled). T2: all HUMAN issues have an RA comment; live supply = sibling BOTS only. T3 DEAD (08-05d). T6 DRY x10 (0 check runs on RA PRs + main unmoved => nothing to fix; verify with get_status, don't rescan all 122). ~143 open PRs, dup 32.",
"T11_FORMAT": "ONE LINE PER ITEM mandated (#666 rewritten 08-04d, was ~2 runs from the body cap). Evidence lives on each thread; old bodies in the EDIT HISTORY. Run History ~6 newest. update_issue body is INLINE => every replace is a full retype.",
"last": "08-06a sel T6,T9. T6 DRY x10 (get_status => total_count:0). T9 = canvas-renderer.test.ts, 30 tests tests-only, 10/10 mutants, PR + issue on DomRenderer charWidth. BOTH #s UNKNOWN at write time (safe-outputs create AFTER my turn; max was 688) => RECONCILE next run, replace the 2 placeholder search-links in #666.",
"PERFMETHOD": "CEILING FIRST: stub the fn to a constant to bound the win before coding. ONE ARM PER PROCESS - JIT fakes ~20% gaps between arms that MUST be equal; equal control arms = the validity check. Measure the fast path's MISS. **MEASURE YOUR OWN FIX FOR REGRESSION** (08-05c v1 was +11%, median-of-5 caught it).",
"ORACLE": "Oracles: NEVER let a fixture be a file you're editing (phantom diff); pin via `git show ce545ec:path`. Output oracles CANNOT see index-only bugs - also assert helpers vs a brute-force linear scan. EQUIVALENT mutants are NOT blind spots - say so, don't imply a full kill. A mutation string occurring TWICE hits the wrong site with replace(...,1) - use rfind. ALWAYS re-verify the tree with diff after a sweep.",
"ROPE_CHUNKS": "RECURRING BUG SOURCE: textToChunks breaks at a newline only if one is in the 1024 window => a >1024-unit line spans chunks that START MID-LINE. Bugs: byteLength (#545/#546), point<->offset (#687). 2nd pass DONE 08-05d: line()/lines()/lineIterator CLEAN (they count newline INDICES). Suspect only code treating prefix[ci] as the line chunk ci starts on.",
"RENDERER_HARNESS": "canvas.ts/webgpu.ts UNTESTABLE not neglected: happy-dom getContext('2d')=NULL => mount() throws. WORKING HARNESS = tests/renderer/canvas-renderer.test.ts (branch repo-assist/test-canvas-renderer-behaviour): COPY IT, don't rederive. Stubs getContext/OffscreenCanvas/rAF(QUEUE, don't run)/getComputedStyle(happy-dom's own THROWS). webgpu.ts same gap, needs a device stub.",
"AUDIT_METHOD": "Where TWO impls of one interface exist, DIFF THEM - cheapest audit found, 2 hits. dom-vs-canvas hitTest gave the charWidth bug (details on its issue). Pairs left: dom/canvas render, webgpu."}