{"READ_FIRST": "Full state = the OPEN Monthly Activity issue (Aug=#666); read EVERY run (it also holds the outage list + T11 format). This file = ONLY lessons #666 lacks.",
"MEMLIMIT": "HARD: dir INCL .git <=12288 B; .git ~6580 => state.json MUST be <=5600 B. stat -c%s AFTER the write, du -sb, then push_repo_memory. Shorten by DELETING clauses, not rephrasing. rm -f .git/hooks/*.sample; never git gc.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST). Hold SPECULATIVE PRs (bottleneck=REVIEW); CORRECTNESS+SECURITY in scope. safeoutputs writes CAN SILENTLY VANISH (2x, incl a whole PR, tool said success) => never say 'I filed X'; FIRST ACTION each run = verify last run's output. PROTECTED: CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. update_issue CAP=1/run => always spend it on #666 => you can NEVER retitle a PR => never widen an open PR past its title; open a new one.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt); getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts. noUncheckedIndexedAccess => compare whole ARRAYS with toEqual.",
"ENV": "GITHUB_TOKEN empty => mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. Unauth `curl api.github.com/repos/.../pulls/N` WORKS, cheaper for 1 field. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile from REPO ROOT, ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup to a FILE. lint '1 info'+exit 0 = PASS.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. ARCHIVE tree: --3way FAILS, use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq. RED/GREEN: `git stash push -q src/<file>` keeps the test, reverts the fix; pop after.",
"FLAKE": "3 flakes, 2 SHAPES: (a) COLD: time() (helpers.ts:201) no warmup; multibuffer.test.ts:1041 => #690 fixes. (b) RATIO `X.avgMs<early.avgMs*3+0.001` between 2 WARM benchmarks: buffer.test.ts:444 AND multibuffer.test.ts:1038; #611 widens to *10+0.01 = what :446 ALREADY uses. #673.",
"NEGATIVE": "DONE, don't redo: REDGREEN sweeps, MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, 342-vs-449, #333, #345, #678, #679, #683, #684, #685, #687, ADDEXCERPT_COW(unopened), #689, CHARWIDTH_DOM(#691), CHARWIDTH_WEBGPU(08-07b), #686, SECAUDIT(08-06c; redo only if deps change; dependabot 403 => list_global_security_advisories). FALSE ALARMS: dup-test-name scan; mergeability; 'Closes #N' regex; Enter DOES auto-indent; dom.ts:511 (A&&(B||C)=intended).",
"dead": "T1 dead (0 unlabelled). T2 DEAD: ALL 37 open issues have an RA comment (#86=dumping ground, never comment); 0 new issues since 08-05, 0 new PRs since #691 => live supply = NEW sibling-bot PRs only (list_pull_requests CREATED desc). T6 DRY x12 - BEST CHECK = list_workflow_runs ci.yml: ALL 30 recent runs `action_required` => NO bot PR EVER had a check run. Never rescan 123.",
"last": "08-07b sel T3,T2(dry->rolled into T3 audit). OPENED WebGpuRenderer charwidth PR (webgpu.ts:1129 `width||8` + NEW tests/renderer/webgpu.test.ts, 8 tests/4 red, 4/4 mutants; gates 2273 pass/0 fail). NUMBER UNKNOWN (safeoutputs returned only a patch) => VERIFY IT EXISTS FIRST NEXT RUN (open PRs, search 'webgpu'); if absent, branch repo-assist/fix-webgpu-charwidth-zero-2026-08-07 was LOCAL ONLY, redo it. #666 updated.",
"PERFMETHOD": "CEILING FIRST: stub the fn to a constant to bound the win first. ONE ARM PER PROCESS - JIT fakes ~20% gaps between arms that MUST be equal; equal controls = validity check. Measure the fast path's MISS + your own fix for regression.",
"ORACLE": "Never let a fixture be a file you're editing (phantom diff); pin via `git show ce545ec:path`. Output oracles CANNOT see index-only bugs - also assert vs a brute-force scan. MUTANTS MUST DEFEAT THE HARNESS: happy-dom rects are ALWAYS 0, so 'return 8'=='return width||8' until a test makes the probe report REAL layout; survived draft 1 on BOTH #691 and webgpu.",
"ROPE_CHUNKS": "textToChunks breaks at a newline only if one is in the 1024 window => a >1024-unit line spans chunks STARTING MID-LINE. Bugs: byteLength (#545/#546), point<->offset (#687). line()/lines()/lineIterator CLEAN.",
"RENDERER_HARNESS": "Renderer tests under happy-dom. canvas: copy tests/renderer/canvas-renderer.test.ts (#689) - stubs getContext/OffscreenCanvas/rAF(QUEUE)/getComputedStyle. WEBGPU (tests/renderer/webgpu.test.ts): mount() REJECTS but sets _charWidth :320 BEFORE _initWebGpu :331 => `await r.mount(c).catch(()=>{})` then hitTest works; setSnapshot needs a rAF stub. FORCE a measured charWidth: defineProperty container.appendChild, and when node.textContent==='MMMMMMMMMM' defineProperty its getBoundingClientRect. rect.width ALWAYS 0 otherwise. NEVER Element.prototype via `this as Element` (TS2352).",
"AUDIT_METHOD": "Where TWO impls of one interface exist, DIFF THEM - 4 hits (both charWidth halves + dom/canvas hitTest + webgpu charWidth). RENDERER TRIO EXHAUSTED. Next thinner: Buffer-vs-Rope API, the 3 hitTest call paths. A comment EXPLAINING a loose assertion marks a bug worked around (dom.test.ts:606).",
"EXPIRY": "Doc Unbloat PRs carry gh-aw-expires=48h AUTO-CLOSE UNMERGED (#663 +26min, #669 +77min, both unmerged, both listed in #666 as pending = MY error). No repo-assist PR has one. CHECK: curl pulls/N | grep gh-aw-expires."}