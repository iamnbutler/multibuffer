{"READ_FIRST":"Full state = the OPEN Monthly Activity issue (Aug=#666); read EVERY run (it also holds the outage list + T11 format). This file = ONLY lessons #666 lacks.","MEMLIMIT":"HARD: dir INCL .git <=12288 B; .git ~6580 => state.json MUST be <=5600 B. stat -c%s AFTER the write, du -sb, then push_repo_memory. Shorten by DELETING clauses, not rephrasing. rm -f .git/hooks/*.sample; never git gc.","rules":"main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST). Hold SPECULATIVE PRs (bottleneck=REVIEW); CORRECTNESS+SECURITY in scope. safeoutputs writes CAN SILENTLY VANISH (2x, incl a whole PR, tool said success) => never say 'I filed X'; FIRST ACTION each run = verify last run's output. BEFORE WRITING ANY FIX/PERF PATCH: grep #666 Suggested Actions for an EXISTING PR on that symbol - 125 open bot PRs, I duplicated #492 (search line-offsets) by checking only AFTER building it. PROTECTED: CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. update_issue CAP=1/run => always spend it on #666 => you can NEVER retitle a PR => never widen an open PR past its title; open a new one.","TESTHARNESS":"mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt); getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts. noUncheckedIndexedAccess => compare whole ARRAYS with toEqual.","ENV":"GITHUB_TOKEN empty => mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. Unauth `curl api.github.com/repos/.../pulls/N` WORKS, cheaper for 1 field. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile from REPO ROOT, ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup to a FILE. lint '1 info'+exit 0 = PASS.","git":"NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. ARCHIVE tree: --3way FAILS, use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq. RED/GREEN: `git stash push -q src/<file>` keeps the test, reverts the fix; pop after.","FLAKE":"3 flakes, 2 SHAPES: (a) COLD: time() (helpers.ts:201) no warmup; multibuffer.test.ts:1041 => #690 fixes. (b) RATIO between 2 WARM benchmarks: buffer.test.ts:444 AND multibuffer.test.ts:1038; #611 widens both to the *10+0.01 that :446 already uses. #673. :444 fires ~29% of full-suite runs - EXPECT it, never blame a patch.","NEGATIVE":"DONE, don't redo: REDGREEN sweeps, MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, 342-vs-449, #333, #345, #678, #679, #683, #684, #685, #687, ADDEXCERPT_COW(unopened), #689, #691, #692, #686, SECAUDIT(08-06c; redo only if deps change; dependabot 403 => list_global_security_advisories). SEARCH_LINEOFFSETS(=#492; my dup=#693, asked to close). RESOLVEINVIEWPORT(no prod callers). BUFFERROPE_ORACLE(08-07d, 44.7k checks vs plain-string oracle: ONLY 2 defect classes on main, BOTH already PR'd = #687(97 fails)+#546(4). slice/insert/delete/replace/line/lines/lineIterator/text/length/lineCount/post-mutation ALL CLEAN incl chunk boundaries+astral. DO NOT RE-SWEEP src/buffer). FALSE ALARMS: dup-test-name scan; mergeability; 'Closes #N' regex; Enter DOES auto-indent; dom.ts:511; clipPoint-vs-clipOffset Math.min divergence(both safe).","dead":"T1 dead (0 unlabelled). T2 DEAD: ALL 37 open issues have an RA comment (#86=dumping ground, never comment); 0 new issues since 08-05 => live supply = NEW sibling-bot PRs only (list_pull_requests CREATED desc; newest non-mine = #690). T6 DRY x14 - BEST CHECK = list_workflow_runs ci.yml: ALL 30 recent runs `action_required` => NO bot PR EVER had a check run. Never rescan 123.","last":"08-07d sel T2,T6 - BOTH DRY, so spent run on Buffer-vs-Rope audit + PR cross-validation. Verified last run OK. RESULT: #687 alone leaves 4 fails, #546 alone leaves 97, BOTH = 0/44702; they edit the same file but git-apply together NO CONFLICT (either order) => now top Suggested Action. NEW angle for #546: reachable by EDITING (emoji text right on construction; an insert moves a boundary onto an odd offset) - no crafted 1023-prefix. Commented #687,#546,#693(please close, keep #492). Gates: 2264 pass, 1 known :444 flake, tc+lint clean. Tree restored ce545ec. NEXT: audit the 3 hitTest call paths; weight runs toward evidence-on-existing-PRs over new PRs.","PERFMETHOD":"CEILING FIRST: stub the fn to a constant to bound the win. ONE ARM PER PROCESS - JIT fakes ~20% gaps between arms that MUST be equal. Measure the fast path's MISS + your own fix for regression.","ORACLE":"Never let a fixture be a file you're editing (phantom diff); pin via `git show ce545ec:path`. Output oracles CANNOT see index-only bugs - assert vs a brute-force scan too. MUTANTS MUST DEFEAT THE HARNESS: happy-dom rects are ALWAYS 0.","RENDERER_HARNESS":"Renderer tests use happy-dom; COPY tests/renderer/canvas-renderer.test.ts (#689) or webgpu.test.ts (#692). rect.width ALWAYS 0 => force a measured charWidth by defineProperty on container.appendChild + the 'MMMMMMMMMM' probe's getBoundingClientRect. NEVER Element.prototype via `this as Element` (TS2352).","AUDIT_METHOD":"Where TWO impls of one interface exist, DIFF THEM (4 hits). Where only ONE exists, sweep vs a BRUTE-FORCE ORACLE (0 new bugs in src/buffer but PROVED 2 open PRs compose). RENDERER TRIO + BUFFER/ROPE EXHAUSTED. Next: the 3 hitTest call paths. A comment EXPLAINING a loose assertion marks a bug worked around (dom.test.ts:606).","EXPIRY":"gh-aw-expires=48h AUTO-CLOSES UNMERGED. Doc Unbloat PRs ONLY (#663,#669,#686 all died so). NO open PR is on a timer as of 08-07d. CHECK each run: curl pulls/N | grep gh-aw-expires."}