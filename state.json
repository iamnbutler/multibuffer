{"READ_FIRST": "Full state = the OPEN Monthly Activity issue (Aug=#666). Read it EVERY run. This file = ONLY operational lessons #666 lacks; never duplicate numbers/verdicts here.",
"MEMLIMIT": "TWO checks, both default 10KB (12288 effective). (1) in-agent push_repo_memory walks the LIVE dir INCL .git (.git measured 6408B after `rm -f .git/hooks/*.sample`, re-run EVERY run) => keep state.json <=5.5KB. (2) the push JOB measures the git PATCH = old+new bytes of CHANGED lines, so rewriting every line costs the whole file TWICE. Edit few keys. Derivation in #666.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST; done 08-04a). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS + SECURITY in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs (NOT '[docs]').",
"T2_ALIVE": "List issues by CREATED desc, not cursor order. Human issues all have an RA comment; live supply = sibling BOTS (Perf/Test Improver) + [aw] failure issues. [aw] issues are HIGH VALUE - read the failing JOB LOG, never trust the body's 'permission/config/network' boilerplate.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt) NOT dispatch selectTo; getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts; createBuffer(id,text) NOT (text). Multi-cursor: dispatch({type:'addCursor',at:pt}).",
"ENV": "GITHUB_TOKEN empty => use mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. list_pull_requests caps 100/page => MUST page. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile - and run it from the REPO ROOT with ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup + redirect to a FILE (piping to tail can flush EMPTY), append EXIT=$?, re-read after sleep. lint '1 info' + exit 0 = PASS. Repo SHALLOW: fetch --unshallow before merge-base.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. In an ARCHIVE tree (not a repo) --3way FAILS: use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq. A failed --3way leaves an UNMERGED index => reset --hard, re-apply only what succeeded.",
"HIGHLIGHTER_OPEN": "#672 linear child-scan at highlighter.ts:193 + injection-highlighter.ts:362/:460 (public API). My 'use a SHARED helper' advice was REFUTED - dropping skipChildren mis-highlighted 101/2401 rows; head logic stays DISTINCT, only the LOOP is common. Fix UNWRITTEN. ROUTING: a fence injects ONLY with a language info string, so a BARE fence reaches skipChildren. Test gap closed 08-04c (#680).",
"FLAKE": "buffer.test.ts:444 line-access-is-O(1) is host-dependent: me ~25% (7/28), Perf Improver ~29% (5/17) INDEPENDENTLY => real. NEVER blame a PR (or credit a mutant) for a lone red without re-running BOTH sides >=4x. #611 fixes it, #673 is the write-up. #373/#376 lint-RED; #374/375/377/378 green.",
"NEGATIVE": "DONE, don't redo: fix( REDGREEN sweep, perf( sweep (22/22), MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, bindings.md NYI, 342-vs-449, #345, #333, dep audit, TS7(->#678), diffLines tests, excerpt-summary tests. Don't re-investigate: dup-test-name scan FALSE ALARM; sed-stripping merge markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most, grep the SYMBOL. AUTOINDENT: Enter DOES auto-indent; #656/#662 cite a DISPATCH SITE - read the test BODY. METHOD: per-file red/green is BLIND to regressions elsewhere - always finish with a FULL suite run.",
"OUTAGES": "#659 #661 #657 #677 - each needs a human `gh aw compile`; blocked while main frozen. Details in #666. docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md (both HEALTHY).",
"DIFFLINES": "DONE 08-04b (#679): NEW file on purpose - #355/#505/#507 all inject into tests/diff/diff.test.ts so they CONFLICT and NONE ever ran. 37 tests, 12/12 mutants caught; #337 and #507 pass alone and stacked. diffLines has 0 callers => perf effect 0.",
"dead": "T1 dead (0 unlabelled). T2: all 38 open issues have an RA comment; live supply = sibling BOTS (#682 new this run). T6 DRY x6 (bot PRs total_count:0 check runs; main frozen => no new conflicts). ~137 open PRs, dup 32 (384=386, 453=455, 505=507).",
"T11_FORMAT": "08-04d: REWROTE #666 to ONE LINE PER ITEM (the mandated format; it was ~2 runs from the body cap). Old body is in the issue EDIT HISTORY, per-item evidence is on each thread. KEEP IT COMPACT. Run History: ~6 newest entries only. update_issue takes an INLINE body, so every replace costs a full retype.",
"last": "08-04d sel T9,T6(dry). NEW tests/multibuffer/excerpt-summary.test.ts (13, new file): excerpt textSummary was asserted in ONE place (excerpt.test.ts:305) and that fixture is FULL-RANGE so it takes the :51 fast path - the counting LOOP had no assertions, and excerpt.ts:26-31 (2-byte + surrogate branches) had NEVER executed. 6/6 mutants invisible to the existing suite. FINDING: Excerpt.textSummary has ZERO readers and ExcerptInfo omits it => #682/#362's edit-path change is UNVERIFIABLE via public API (their patch leaves the suite 2278/0); LAZY (#510 treatment) beats conditional-skip. #682 touched-guard HOLE reproduced: editRow is the edit START row, so an edit reaching in from before an excerpt leaves a STALE summary. TOOLING: run mutants SYNCHRONOUSLY - a backgrounded script raced, corrupted src/, gave 6 bogus identical rows; assert patch-applied AND restore. FLAKE TRAP: mutant C's first '1 fail' was #673, not a detection (4 re-runs = 0)."}