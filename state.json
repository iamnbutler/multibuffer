{"r":"30688956884","READ_FIRST":"Full state = the OPEN '[Repo Assist] Monthly Activity' issue (Aug=#666; #623=July CLOSED, kept for derivations). Read it each run. This file = ONLY what it lacks. #657: cap 12288B on the WHOLE DIR incl .git => keep state.json <=5.5KB AND run the .git stopgap (rm -rf .git/hooks; git reflog expire --expire=now --all; git gc --aggressive --prune=now), then confirm via push_repo_memory.","rules":"main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST; done 08-01a). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS fixes ARE in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs (NOT '[docs]'). CAPS/run: update_issue 1, push_to_pr 4, add_comment 10, create_issue 4.","AUTOINDENT_SETTLED":"CLOSED \u2014 do NOT flip again. Enter DOES auto-indent; 3x by EXECUTED test incl. the test BODY (tests/editor/newline.test.ts 12/0). editor.ts:487 -> _insertText special-cases '\\n' on BOTH paths (:604-614, :654-662). #656/#662 claim otherwise, citing editor.ts:487 = the DISPATCH SITE, one call short. RULE: never conclude from a dispatch site; follow the call, prefer an EXECUTED test, read the test BODY not its name.","CI_RUNS":"08-01a from API over ALL 90 ci.yml runs since freeze: EXACTLY 11 at action_required (list in tracker). A RUN is created on push to a bot branch but parks at action_required and never becomes a CHECK RUN on the PR \u2014 so '0 check runs' AND 'pushes create runs' are both true.","BINDINGS_DONE":"08-01a docs/bindings.md 'Not Yet Implemented > Indentation' listed 5 bindings that ALL work on ce545ec => PR #665 (docs-only). Other NYI entries CHECKED accurate \u2014 do NOT re-audit.","TESTHARNESS":"Scratch-test gotchas: (a) mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => on Linux use ctrlKey; (b) selections via editor.extendSelectionTo(pt), NOT dispatch selectTo (leaves COLLAPSED, silently tests wrong branch); (c) getText = snap.lines(0, snap.lineCount).join('\\n'). Copy helpers from tests/editor/*.test.ts; scratch files must sit under tests/; rm before commit. awk '/START/,0' grabs to EOF \u2014 use /START/{f=1} /END/{f=0} f to extract a mid-file block.","DONE_DONT_REDO":"REDGREEN, MERGE_TRUTH (all 135 merge clean; conflicts are CROSS-PR only), NOSRC_BLOCK, SWEEP, DUP_AUDIT (#468/#469 the ONLY both-twins trap), #468-vs-#479-both-needed: done, numbers in tracker. Redo only if main moves.","NEGATIVE":"Don't re-investigate: dup-test-name scan = FALSE ALARM (#311 fine); union-merge by sed-stripping markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most \u2014 grep the SYMBOL; auto-indent CLOSED; 'reorder to dodge conflicts' WRONG (only TRADES which PR rebases); #655/#656 are CLOSED PRs.","OUTAGES":"#659 (PR/Implement dead 2026-03-18), #661 (Docs/Update protected-files), #657 (memory size check): root causes in tracker. Both workflow fixes need a human 'gh aw compile' => blocked while main frozen. docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md (HEALTHY). Others healthy \u2014 do NOT refile.","ENV":"GITHUB_TOKEN empty => mcp__github__*. Large list_* overflow to a file => parse w/ python3, never read raw. list_issues key='issues'; list_pull_requests payload = content[0].text JSON string (perPage=100, 2 pages = all 135). list_workflow_runs caps 30/page. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile. suite+typecheck ~3s. lint 'N info'+exit0=PASS.","git":"NEVER 'git diff ce545ec pr/N' \u2014 bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. Clone SHALLOW: git fetch --unshallow origin. Apply w/ 'git apply --3way --index'; undo w/ 'git reset --hard HEAD; git clean -fd' ('checkout -- .' does NOT undo --3way). RED/GREEN: split diff '-- tests/' vs '-- src/'. tsc exit direct, NOT piped. Bash cwd PERSISTS.","FLAKE":"'line access is O(1)' buffer.test.ts:444 host-dependent: ~2 fails per 6 runs on CLEAN ce545ec. NEVER blame a PR without re-running BOTH sides >=5x. #611 is the fix. #373/#376 lint-RED; #374/375/377/378 green (human PRs).","dead":"T1 dead (0 unlabelled). T3 fuel = defects found by AUDIT, not the issue list. T2 NOT exhausted \u2014 #664 had 0 RA comments; re-CHECK issues by comment count. T6: only NEW bot PRs are real work. T4/5/8/9 parked. DORMANT: maintainer last spoke 2026-07-06; 0 tracker items ticked. 135 open PRs (113 RA), non-dup 80, dup 32.","last":"08-01b sel T3,T2 \u2014 both empty in classic form AGAIN; redirected into the glossary-claims audit, which paid off with a SOURCE defect (readOnly hole) + verified 3rd-party bug #664. Reading-based claims now 3 for 3 WRONG. NEXT: (1) audit remaining ~90 docs/glossary.md entries vs executed behaviour; (2) hunt other direct-mutation bypasses of Editor; (3) new bot PRs = only T6 fuel; (4) if main moves, redo sweeps.","READONLY_HOLE":"08-01b PROVEN by execution (detail in tracker): SearchController.replaceAll() calls mb.edit() DIRECTLY => bypasses BOTH readOnly AND excerpt {editable:false}. replaceActive SAFE (via dispatch). #537 fixes both; I PUSHED to #537 (replaceRanges returns applied COUNT + 4 guard tests). LEFT UNFIXED on purpose: cursor moves to lastRange even when SKIPPED. _isEditCommand (editor.ts:1913) covers all 16 mutating cmds \u2014 dispatch guard COMPLETE; the hole was public API BYPASSING dispatch. NEXT: other direct-mutation sites outside Editor.","ISSUE_664":"08-01b VERIFIED (detail in tracker): resolveResultsInViewport filters at EXCERPT granularity, never narrows to row => single excerpt spanning doc returns ALL matches. Point #664 MISSED, I added: resolveAnchorsInViewport is CORRECTLY documented as an excerpt-level PRE-FILTER (types.ts:186-191) => fix belongs in search.ts ONLY. Proposed patch VERIFIED green; NO non-test callers; existing test search.test.ts:375 uses ONE-ROW excerpts so both granularities coincide. Perf Improver to open the PR."}