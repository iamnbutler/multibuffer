{"READ_FIRST": "Full state = OPEN Monthly Activity issue (Aug=#666). Read it EVERY run. This file = ONLY what #666 lacks: operational lessons. NEVER duplicate numbers/verdicts/tables here. #657: 12288B cap on WHOLE DIR incl .git (~6KB) => keep this <=5.2KB AND run stopgap (rm -rf .git/hooks; git reflog expire --expire=now --all; git gc --aggressive --prune=now), then push_repo_memory.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST; done 08-03d). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS + SECURITY in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs (NOT '[docs]').",
"T2_ALIVE": "Every HUMAN issue has an RA comment, but sibling BOTS (Perf/Test Improver) FILE NEW ISSUES between runs. RULE: list issues by CREATED desc, not just cursor order; bot-filed issues are the live T2 supply and are high-value to answer.",
"TS7": "MEASURED 08-03d, filed as issue. typescript@7.0.2 (=npm latest) typechecks repo CLEAN, 6.14x: tsc --noEmit 4.447s->0.724s median n=5, non-overlapping. Identical 173-file program; 7-diagnostic canary BYTE-IDENTICAL (codes+columns). GOTCHA: TS7 --showConfig OMITS strict-implied flags - looks like drift, is NOT (canary proves they fire). TS7 main export DROPS the JS compiler API ('.'->lib/version.cjs, only ./unstable/*) - repo has ZERO programmatic typescript imports so safe HERE. Install 31MB vs 23MB = BIGGER. BLOCKED: needs package.json ^5->^7 (protected) => issue not PR. React19 unmeasured + touches src/react runtime.",
"DEPAUDIT": "`bun audit` main: 4 advisories (3 high) happy-dom<20.8.9 + transitive ws<8.20.1; PR #675 fixes lockfile-ONLY (^20.8.4 already admits 20.11.1). Re-verified 08-03d: SAME 4, none new, #675 needs no refresh. GOTCHA: `bun update <pkg>` rewrites package.json range AND bun.lock's MIRROR of it; git checkout package.json AND patch the mirror back. 7 other in-range bumps HELD (biome minor moves lint output; 2nd lockfile PR collides with #675).",
"HIGHLIGHTER_OPEN": "#672 reports linear child-scan at highlighter.ts:193 (2-4x/frame). VERIFIED + EXTENDED: SAME pattern at injection-highlighter.ts:362 and :460. InjectionHighlighter is PUBLIC (renderer/index.ts:10). Fix must be a SHARED helper or it covers 1 of 3. No open PR touches it (#377 near-miss: edits :460, only drops a dead param).",
"SWEEP_LESSON": "Per-file red/green is BLIND to regressions in OTHER test files - always finish with a full-suite run. And: a tool that gets much FASTER while reporting ZERO errors may not be doing the work - verify the file list AND inject a deliberate error (that's what made TS7 trustworthy).",
"AUTOINDENT_SETTLED": "CLOSED, do NOT flip: Enter DOES auto-indent (newline.test.ts 12/0, 3x). #656/#662 cite editor.ts:487 = DISPATCH SITE. RULE: never conclude from a dispatch site; read the test BODY.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt) NOT dispatch selectTo; getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts; createBuffer(id,text) NOT (text). Multi-cursor: dispatch({type:'addCursor',at:pt}).",
"NEGATIVE": "DONE, don't redo: fix( REDGREEN sweep, perf( sweep (22/22), MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, bindings.md NYI, 342-vs-449, #345, #333, dep audit, TS7. Don't re-investigate: dup-test-name scan FALSE ALARM; sed-stripping merge markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most, grep the SYMBOL; 'reorder to dodge conflicts' WRONG; diffLines() has NO callers (=> #337/#507 zero in-repo effect).",
"OUTAGES": "#659 #661 #657 - causes in #666, need a human 'gh aw compile', blocked while main frozen. docs-update.md SEPARATE from unbloat-docs.md + glossary-maintainer.md (HEALTHY). Don't refile.",
"ENV": "GITHUB_TOKEN empty => mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. list_pull_requests caps 100/page => MUST page. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile. Bash cap 60s => nohup+log, ABSOLUTE paths (cwd resets after heredocs). `bun test` piped to tail in nohup can flush EMPTY - redirect to a file, append EXIT=$?, re-read after sleep. lint '1 info'(biome migrate)+exit0=PASS. Repo SHALLOW: fetch --unshallow before merge-base. Trial alt toolchains in a SCRATCH dir (bun add in /tmp) so the repo tree stays clean.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. In an ARCHIVE tree (not a repo) --3way FAILS: use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq. A failed --3way leaves an UNMERGED index => reset --hard, re-apply only what succeeded.",
"FLAKE": "'line access is O(1)' buffer.test.ts:444 host-dependent. MY runs 7/28 (~25%); Perf Improver INDEPENDENTLY 5/17 (~29%) => real, not one host. NEVER blame a PR without re-running BOTH sides >=5x. #611 fixes it; #673 is the write-up. #373/#376 lint-RED; #374/375/377/378 green.",
"dead": "T1 dead (0 unlabelled). T6 DRY x2 (bot PRs total_count:0 check runs => nothing pushable; main frozen => no new conflicts; new PR #676 is '[docs]' = can't push). DORMANT: maintainer last spoke 2026-07-06; 0 tracker items ticked; 0 comments on #666. ~137 open PRs, dup 32 (384=386, 453=455, 505=507).",
"last": "08-03d sel T6,T4. T6=DRY, confirmed+reported not faked. T4=measured TypeScript 7 (see TS7) -> filed issue, 6.14x, blocked on protected package.json. Audit re-verified unchanged. NEXT: T10/T8 = HIGHLIGHTER_OPEN shared-helper fix, or MULTICURSOR (n^1.5, largest unexplained cost); React19 is the other open dep question."}
