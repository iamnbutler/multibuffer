{"READ_FIRST": "Full state = the OPEN Monthly Activity issue (Aug=#666). Read it EVERY run. This file = ONLY operational lessons #666 lacks; never duplicate numbers/verdicts here.",
"MEMLIMIT": "TWO checks, both 10KB (12288 effective). (1) push_repo_memory walks the LIVE dir INCL .git => `rm -f .git/hooks/*.sample` EVERY run; do NOT git gc (it GREW it); keep state.json <=5.3KB. (2) the push JOB measures the git PATCH = old+new bytes of CHANGED lines => dump with separators=(',\\n',': ') so each key is ONE line; a compact one-line dump rewrites everything and blows the cap.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST). Hold on SPECULATIVE PRs (bottleneck=REVIEW not supply); CORRECTNESS + SECURITY in scope. Writes via mcp__safeoutputs__* ONLY. PROTECTED(refused): CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/. push_to_pr ONLY on '[Repo Assist] ' PRs.",
"T2_ALIVE": "List issues by CREATED desc, not cursor order. Human issues all have an RA comment; live supply = sibling BOTS + [aw] failure issues. [aw] issues are HIGH VALUE - read the failing JOB LOG, never trust the body's 'permission/config/network' boilerplate.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt) NOT dispatch selectTo; getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts (createBufferId/row/point/expectOffset); createBuffer(id,text). Multi-cursor: dispatch({type:'addCursor',at:pt}). tsconfig has noUncheckedIndexedAccess => arr[i] is T|undefined; compare whole ARRAYS with toEqual instead of indexing (no `as` allowed).",
"ENV": "GITHUB_TOKEN empty => mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. list_pull_requests caps 100/page => MUST page. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile - run from REPO ROOT with ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup + redirect to a FILE, re-read after sleep. lint '1 info' + exit 0 = PASS.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. In an ARCHIVE tree --3way FAILS: use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq.",
"FLAKE": "buffer.test.ts 'line access is O(1)' (tests/multibuffer/buffer.test.ts:444). ONLY reproduces under a FULL suite run (file alone 8/8 green) => always compare FULL-suite vs FULL-suite. Rate VARIES BY RUNNER, don't quote one number (08-05c: 1/4 on branch). #611 fixes, #673 write-up. #373/#376 lint-RED; #374/375/377/378 green.",
"NEGATIVE": "DONE, don't redo: fix( REDGREEN sweep, perf( sweep, MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, bindings.md NYI, 342-vs-449, #345, #333, dep audit, TS7(#678), diffLines(#679), excerpt-summary(#683), lazy excerpt summary(#684), getLineTokens(#685), ROPE LONG-LINE POS-CONV(08-05c). Don't re-investigate: dup-test-name scan FALSE ALARM; sed-stripping merge markers BREAKS syntax; mergeability-vs-main ANSWERED; 'Closes #N' regex MISSES most, grep the SYMBOL. AUTOINDENT: Enter DOES auto-indent; #656/#662 cite a DISPATCH SITE - read the test BODY.",
"OUTAGES": "#659 #661 #657 #677 - each needs a human `gh aw compile`; blocked while main frozen. Details in #666. docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md (both HEALTHY).",
"dead": "T1 dead (0 unlabelled). T2: all open issues have an RA comment; live supply = sibling BOTS. T6 DRY x9 (main unmoved; ZERO maintainer comments on any issue since 2026-07-01; newest of 121 RA PRs is #685, 0 comments). ~143 open PRs, dup 32.",
"T11_FORMAT": "ONE LINE PER ITEM is mandated (#666 rewritten 08-04d, was ~2 runs from the body cap). Per-item evidence lives on each thread; old bodies in the issue EDIT HISTORY. Run History: ~6 newest only. update_issue takes an INLINE body => every replace is a full retype.",
"last": "08-05c sel T3,T6(DRY x9). SHIPPED fix(buffer) rope long-line point<->offset: offsetToLineCol/lineColToOffset/_findLineStartOffset all assumed chunk i STARTS at line _chunkNewlinePrefixes[i] - FALSE for continuation chunks of a >1024-unit line. pointToOffset({row1,col0})=2055 want 7; col RESETS to 0 at each chunk boundary. Fix = _chunkStartsLine(ci) guard + fallback to newline #(line-1). Sweep on main 303/400 + 336/400 docs + 200/200 edits BAD -> 0. Tests NEW file 8red/1green(control) -> 9/9. Suite 2274/0.",
"PERFMETHOD": "CEILING FIRST: stub the fn to a constant to bound the win before writing code. ONE ARM PER PROCESS - JIT warm-up fakes ~20% gaps between arms that MUST be equal; equal control arms = the validity check. Measure the fast path's MISS. **MEASURE YOUR OWN FIX FOR REGRESSION** - 08-05c my v1 correctness fix was +11%/+11% on a hot path; median-of-5-processes caught it, v2 = parity. Restoring a fast path under a correctness guard is how you get parity back.",
"ORACLE": "Oracles: NEVER let a fixture be a file you're editing (phantom diff); pin via `git show ce545ec:path`. Output oracles CANNOT see index-only bugs - also assert helpers vs a brute-force linear scan. EQUIVALENT mutants are NOT blind spots - say so, don't imply a full kill. A mutation string occurring TWICE hits the wrong site with replace(...,1) - use rfind. ALWAYS re-verify the tree with diff after a sweep.",
"ROPE_CHUNKS": "RECURRING BUG SOURCE: textToChunks only breaks at a newline if one is in the 1024 window => a >1024-unit line spans chunks that START MID-LINE. Two bugs so far: byteLength (#545/#546) and point<->offset (08-05c). ANY code keyed off _chunkNewlinePrefixes assuming chunk i begins a line is suspect. line()/lines()/lineIterator already CORRECT."}