{
 "READ_FIRST": [
  "AUTHORITATIVE STATE = the OPEN Monthly issue (Sept = #787). NEW MONTH => close old AND",
  "create new (update_issue cap 1/run). I DESTROYED #666 once: issue_read TRUNCATES a big",
  "body to '[Content too large, saved to file: X.json]' + THAT FILE DOESN'T EXIST; retyping",
  "it writes the placeholder. NEVER write a body starting '[Content'. ALWAYS read/edit the",
  "monthly via `curl -s https://api.github.com/repos/iamnbutler/multibuffer/issues/N` into a",
  "FILE. Unauth curl never truncates but is ~60/hr: do LIST calls EARLY (3 pages!), then MCP."
 ],
 "memory_mechanism": [
  "in-agent push_repo_memory is ADVISORY, counts .git => ALWAYS fails (#677). IGNORE it.",
  "REAL gate = push job: MAX_FILE_SIZE 10240 AND MAX_PATCH_SIZE 10240 (eff. 12288). BOTH bind.",
  "Patch = removed + added lines => EDIT FEW LINES, never rewrite the file. CHECK wc -c AND",
  "`git diff|wc -c` BEFORE finishing; keep file <10100 B. Ceiling raise = #698."
 ],
 "screening_MUST_DO_BOTH": [
  "search_issues is scoped is:issue and NEVER returns PRs. That caused dup PR #765.",
  "PROVEN recipe: curl 2x100 /pulls -> numbers -> ONE batched fetch of every head:",
  "  refs=$(awk '{printf \"refs/pull/%s/head:refs/prs/%s \", $1,$1}' openprs.txt)",
  "  git fetch -q --no-tags origin $refs   # 184 heads, <60s",
  "then loop `git diff <merge-base> refs/prs/$n -- <file>` and grep the SYMBOL, filtering to",
  "+/- lines (#310-#345 have an ancient base => whole-file diffs match every symbol).",
  "V18 a diff hash MUST include the FILE PATH (#691 vs #692 = same fix, diff file).",
  "167 RA PRs, 0 merged => my back catalogue is the likeliest dup. Other bots: [Perf",
  "  Improver], [Test Improver]. Never ls-remote branch names."
 ],
 "rules": [
  "main FROZEN at ce545ec. PR only if live-on-main + red-on-main + gates green + screened.",
  "TEST-ONLY is excepted. public-API-only => PR ok but STATE 'zero in-repo callers'.",
  "pure-optimisation / dead-code / untestable / SETTINGS => ISSUE, not PR.",
  "Ambiguous semantics => ISSUE unless a docstring pins it, OR SPLIT: PR the unambiguous",
  "half and NAME the ambiguous half in the PR body as the maintainer's call.",
  "Speculative => gated patch in a COMMENT. Never a dup PR; never push to a batch branch.",
  "ONE comment per thread per run. PROTECTED: CLAUDE.md, package.json, .github/, .claude/.",
  "Caps: update_issue 1/run, comments 10, PRs 4."
 ],
 "env": [
  "bun INTERMITTENT: `which bun || curl -fsSL https://bun.sh/install|bash` then",
  "`export PATH=$HOME/.bun/bin:$PATH` EVERY Bash call. MCP github works; gh CLI does NOT.",
  "list_issues returns {'issues':[..]}. actions_list caps 30/page. get_job_logs WORKS.",
  "ABSOLUTE paths in scratch scripts; cwd resets. Bash 60s => run_in_background.",
  "RUNNER CHECKOUT IS depth-1: `git fetch --unshallow -q origin` BEFORE any merge-base /",
  "rev-list / two-dot-diff / 'orphan' claim (produced false finding #705).",
  "Edit replace_all matches INDENTATION. `npx playwright install chromium` WORKS (no --deps)."
 ],
 "gates": [
  "ALWAYS run test + typecheck + lint (a test+tsc-only sweep MISSED #373's 12 bare `as`).",
  "WORKTREE SWEEPS: `git worktree add` has NO node_modules => EVERY test fails and the sweep is",
  "garbage. `ln -sfn $REPO/node_modules $WT/` + `git clean -e node_modules`.",
  "CI also runs `bun run build:playground` FIRST. CLAUDE.md's build:demo doesn't exist (#671).",
  "BASELINE ce545ec at IDLE: 2274 run / 0 fail; tsc 0; lint 1 info (#760). Reconfirmed 09-03.",
  "7 WALL-CLOCK perf tests fail under LOAD (#673 = the usual one). Run gates SERIALLY; at idle",
  "  the suite is EXACTLY baseline. Re-run a suspect file in ISOLATION before blaming a PR.",
  "ALWAYS gate MAIN through the SAME harness at the SAME load: 09-01c 111/183 'red' was really 3.",
  "tsc+lint are LOAD-INDEPENDENT (6s+2s) - trust those; re-run test fails capturing NAMES.",
  "noUncheckedIndexedAccess: arr[i] is T|undefined. Branded rows: num() from tests/helpers.ts."
 ],
 "dead_tasks": [
  "T1: 0 unlabelled. T7: all non-RA PRs are bot/owner authored.",
  "T4: deps+TS settled; only NON-protected surface = scripts/ + playground/ + tsconfig.json.",
  "  Flaky route DEAD (#611/#690). DONE 09-03 (#797). REUSABLE: bun.lock NOT protected;",
  "  bun update <pkgs> + `git checkout package.json` + bun install => lockfile-ONLY diff.",
  "  0 Dependabot; biome blocked by #798.",
  "T9 V13 pop. EXACTLY 2: simulateTokenRendering RETIRED, buildDecorationMap in #737.",
  "T6 dead while main frozen + #757 stalls CI: 0 conflicts, 0 jobs. Re-verified 09-06 run24.",
  "  #311/#314 'tsc-red' FALSE (8 errs at their 03-18 base, in src/react = files neither",
  "  touches). V43 GATE THE MERGE RESULT (main+PR), NEVER the bare head.",
  "T2 DEAD 09-06: every peer-bot issue has an RA comment; only an issue NEWER than #812",
  "  reopens it (issues?sort=created&direction=desc). #776 NOT a target (4 rebuilds).",
  "BOTH DRAWN TASKS DEAD => pivot to T3/T5. V30 spent; hunt DUPLICATED BLOCKS (V52).",
  "  NEVER end a run on 'no action'."
 ],
 "retired_do_not_redo": [
  "Full list in #666/#787. Negatives: resolveAnchor==resolveAnchors; createRenderer and",
  "worker/index.ts docstrings match; wrap-map quadratic = #483+#331; reDiff perf; Math.min and",
  "uncalled-helper sweeps; multi-cursor copy order; tile-map (#758); _deleteLine multi-row.",
  "glob.ts shouldTraverseDirectory = perf only (#540); injection no-parser row fixed = #813.",
  "excerpt-shift 5-way SETTLED: merge #369, close #342/#448/#449/#362-as-fix.",
  "V17 webgpu.ts:438 CLOSED (untestable); modelled-test vein EMPTY. SEPARATOR VEIN EXHAUSTED",
  "  (route map in #787): 6 routes guarded/covered, 3 'dup' pairs COMPLEMENTARY. Only #674."
 ],
 "next_veins": [
  "AUTHORITATIVE list is #787 'Future Work' - this is the index.",
  "V0 doubt the probe; if the real check is affordable DO NOT MODEL IT. V2 covered != checked.",
  "V3 docstring-vs-behaviour (9+).",
  "V4 a call site skipping a policy its siblings implement - RICHEST; the right sibling is",
  "  the ORACLE. cursor.ts swept 09-02; renderer/ mined 09-06.",
  "V47 weakened assertions = author-flagged bugs. V48 mine CONSUMERS of a 'spent' module.",
  "V8 two PRs textually independent + semantically incompatible: what does a PR own that",
  "  ISN'T a line of its own file (a global, an invariant, a fixture, a VENDORED ARTEFACT)?",
  "V9 a prose rule in a prompt is ADVISORY (#343); enforceable guards belong in CONFIG.",
  "V12 a docstring/TEST can pin the WRONG side: #704 pins the fix (PR ok), #695 endorses the",
  "  bug (hand over). ASK WHICH SIDE THE ORACLE IS ON.",
  "V24 A GREEN SUITE IS NOT EVIDENCE OF A FIX. Run the BEHAVIOUR, diff the OBSERVABLE.",
  "V26 a GUARD's DEAD-END branch re-admits the state it excludes; the other direction's",
  "  fallback is the ORACLE. Run21's bug = V26+V4: a CLAIMED row with no fallback.",
  "V30 UNTOUCHED-FILE MAP IS NOW EXHAUSTED (run23 read the last two). NEGATIVE: controller",
  "  reDiff() and createUnifiedDiffMultiBuffer are SEMANTICALLY IDENTICAL - vein empty.",
  "  project/types.ts = types only. STILL OPEN: resolveEditorState (editor/types.ts:184,",
  "  V26-shaped 0,0 fallback, 0 in-repo callers). NEXT SURFACE = duplicated BLOCKS (V52)."
 ],
 "merge_sweep_TOOL_AND_TRAP": [
  "TOOL: `git merge-tree --write-tree --merge-base=<mb> refs/prs/A refs/prs/B` (exit!=0 =>",
  "conflict). Exact, ~30ms, no worktree. Use per-pair `git merge-base A B`, not a fixed base.",
  "TRAP, hit 3x: build each PR's file set from ALL CHANGED PATHS, not src/+tests/, and compare",
  "ALL open PRs, not RA-only (09-01b map: 127 conflicting pairs, 23 clusters).",
  "AN INDEPENDENT SET IS BIASED TOWARD DUPS (identical patches never conflict): subtract dups."
 ],
 "canvas_harness_RECIPE": [
  "CanvasRenderer IS mountable under happy-dom. FULL RECIPE = the committed file",
  "tests/renderer/canvas-decoration.test.ts (+ #783, #689). Two traps it won't show you:",
  "stub clientWidth/Height on HTMLElement.prototype or the canvas is 0px and every background",
  "fillRect has width 0; and SPACES/TABS PAINT NO GLYPH (col=(drawImage destX-gutter)/cw)."
 ],
 "biome_25_BLOCKER": [
  "#798 (09-03): biome 2.4.4->2.5.11 breaks lint - no-type-assertion.grit matches IMPORT",
  "ALIASES on 2.5.x (3 false errors). 2 constrained patterns FAILED; a broken .grit EXITS 1.",
  "$schema pins 2.4.6 => a bump needs `biome migrate`. Do NOT guess; leave biome out of dep bumps."
 ],
 "this_run_2026-09-06": [
  "RUN24 (34057730602) drew T10+T6. T6 DEAD AGAIN (#815 = 0 check runs; main frozen => no new",
  "  conflicts possible). RECONCILED: run23's PR = #815, now linked in #787.",
  "V52 DUP-BLOCK SCAN = REUSABLE VEIN FINDER: strip comments/ws, hash every 5-line window over",
  "  src/**/*.ts, keep windows seen 2+ times, extend each group up/down while all copies agree.",
  "  Top hit: editor.ts selection-commit block x4 - GUARDED in _deleteBackward/_deleteLine,",
  "  UNGUARDED in _insertText/_deleteForward.",
  "REAL BUG live on ce545ec: 2 cursors in an editable:false excerpt + insertText|insertNewline|",
  "  insertTab|paste|deleteForward{char,word} => loop skips every edit, _mergeSelections([])",
  "  WIPES ALL selections and the editor is then STUCK (moveCursor returns at its own",
  "  length===0 guard; ONLY selectAll recovers). 6 cmds, not 2: insert*/paste use _insertText.",
  "SCREENING REDIRECTED AGAIN: #499 (APRIL, mergeable clean) IS that 2-site fix. Did NOT dup:",
  "  applied its editor.ts, re-ran the 17-cmd sweep => ALL OK, NO REMAINDER (no follow-up PR",
  "  possible). Merged-tree gates 2276/0, tsc 0, lint 1 info, build ok. Commented; top of #787.",
  "editor.ts _mergeSelections VEIN SPENT: 8 sites = 4 edit (2 guarded, 2 = #499) + 4 movement",
  "  that cannot empty (_addCursorsVertical seeds from _selections). Do not re-mine.",
  "LESSON: the 195-PR back catalogue holds VERIFIED LIVE BUGS I rediscover independently. A",
  "  5-month-old PR is NOT stale evidence - screen BEFORE building, then VERIFY, don't re-file."
 ]
}