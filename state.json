{
 "READ_FIRST": [
  "AUTHORITATIVE STATE = the OPEN Monthly issue (Aug=#666). Read ALL of it every run.",
  "08-30b: I DESTROYED #666's body. issue_read TRUNCATES a big body to the string",
  "'[Content too large, saved to file: X.json]' and THAT FILE DOES NOT EXIST on the runner.",
  "Retyping what you read then writes the placeholder. NEVER write a body starting '[Content'.",
  "FIX: read via `curl -s https://api.github.com/repos/iamnbutler/multibuffer/issues/N` -",
  "UNAUTH CURL WORKS, never truncates, cheap; use it for issues/pulls LISTS too (2x100).",
  "#666 rebuilt to 5.9 KB (was 64 KB); backlog = grouped #numbers. KEEP IT <10 KB."
 ],
 "memory_mechanism": [
  "in-agent push_repo_memory is ADVISORY, counts .git => ALWAYS fails (#677). IGNORE it.",
  "REAL gate = push job: MAX_FILE_SIZE 10240 AND MAX_PATCH_SIZE 10240 (eff. 12288).",
  "BOTH bind. Patch = removed + added lines => EDIT FEW LINES. Keep file <9500 B.",
  "Measure BOTH: wc -c state.json; git diff | wc -c. Ceiling raise = #698.",
  "Use `with open(...)`: unflushed json.dump + append CORRUPTS the file (hit 08-30)."
 ],
 "screening_MUST_DO_BOTH": [
  "search_issues is scoped is:issue and NEVER returns PRs. That caused dup PR #765.",
  "PROVEN recipe: curl 2x100 /pulls -> numbers -> ONE batched fetch of every head:",
  "  refs=$(awk '{printf \"refs/pull/%s/head:refs/prs/%s \", $1,$1}' openprs.txt)",
  "  git fetch -q --no-tags origin $refs   # 180 heads, <60s",
  "then loop `git diff <merge-base> refs/prs/$n -- <file>` and grep the SYMBOL.",
  "CAUTION: #310-#345 have an ancient base => WHOLE-FILE diffs match every symbol; filter to",
  "+/- lines and read the PR's real function body before believing.",
  "If the finding is 'X is called too often', search the CALLEE's name, not the fix.",
  "My back catalogue is the likeliest dup: 158 open RA PRs, 0 merged. 08-31 it blocked TWO.",
  "Other bots: [Perf Improver], [Test Improver]. Never ls-remote branch names",
  "(bot branches carry a -<hash> suffix => false 'vanished')."
 ],
 "rules": [
  "main FROZEN at ce545ec. PR only if live-on-main + red-on-main + gates green + screened.",
  "TEST-ONLY is excepted. public-API-only => PR ok but STATE 'zero in-repo callers'.",
  "pure-optimisation / dead-code / untestable / SETTINGS => ISSUE, not PR.",
  "Ambiguous semantics => ISSUE unless a docstring pins it, OR SPLIT: PR the unambiguous",
  "half and NAME the ambiguous half in the PR body as the maintainer's call.",
  "08-28 worked example: deleteLine's drag-direction dependence is indefensible (PR it);",
  "how many lines a selection deletes is a preference (hand over, same PR).",
  "Speculative => gated patch in a COMMENT. Never a dup PR; never push to a batch branch.",
  "ONE comment per thread per run. PROTECTED: CLAUDE.md, package.json, .github/, .claude/.",
  "Caps: update_issue 1/run, comments 10, PRs 4."
 ],
 "env": [
  "bun INTERMITTENT: `which bun || curl -fsSL https://bun.sh/install|bash` then",
  "`export PATH=$HOME/.bun/bin:$PATH` EVERY Bash call. MCP github works; gh CLI does NOT.",
  "list_issues returns {'issues':[..]}. actions_list caps 30/page. get_job_logs WORKS.",
  "ABSOLUTE paths in scratch scripts; cwd resets. Bash 60s => run_in_background.",
  "RUNNER CHECKOUT IS depth-1: `git fetch --unshallow -q origin` BEFORE any merge-base /",
  "rev-list / two-dot-diff / 'orphan' claim (produced false finding #705).",
  "Merge-test 2 PRs: WORKTREE + `git merge --no-ff -m t` (NOT --no-commit; 2nd merge dies).",
  "react-dom NOT installed. Edit replace_all matches INDENTATION; re-probe after edits."
 ],
 "gates": [
  "ALWAYS run test + typecheck + lint (bun test does NOT typecheck).",
  "CI also runs `bun run build:playground` FIRST. CLAUDE.md's build:demo does not exist (#671).",
  "BASELINE ce545ec: 2274 run / 2265 pass / 0 fail; tsc 0; lint 1 info (#760).",
  "FLAKE buffer.test.ts 'line access is O(1)' ~27% (#673), covered by #611/#667; don't refile.",
  "noUncheckedIndexedAccess: arr[i] is T|undefined. Branded rows: num() from tests/helpers.ts."
 ],
 "ci_is_parked_not_absent": [
  "#757: ci.yml runs ARE created on bot PRs but sit at action_required, dispatch 0 jobs and",
  "publish NO check runs => looks identical to 'CI not configured'. Re-verified 08-29.",
  "A run may show conclusion=failure yet dispatch 0 JOBS - same parked shape. CHECK JOB",
  "COUNT, not conclusion. Cause unknown. T6 has NO CI-failure work until the gate clears."
 ],
 "dead_tasks": [
  "T1: 0 unlabelled. T7: all non-RA PRs are bot/owner authored.",
  "T4 EXHAUSTED (re-confirmed 08-30d): audit=#675, wide refresh settled, rest protected.",
  "T6 HAS REAL WORK: cluster combination tests (c9-c23 left); ALL-PATHS dup sweep.",
  "No T6 CI-failure work until the action_required gate clears.",
  "T9: line-level decoration render tests DONE by #737. react hooks UNTESTABLE",
  "  (react-dom absent) - use-diff-view/use-editor-view stay zero-coverage."
 ],
 "retired_do_not_redo": [
  "Full list in #666. Negatives: resolveAnchor==resolveAnchors; createRenderer and",
  "worker/index.ts docstrings match; wrap-map quadratic = #483+#331; reDiff perf;",
  "uncalled-helper sweep; Math.min sweep; multi-cursor copy order; tile-map (#758);",
  "_deleteLine multi-row selection (fixed 08-28).",
  "08-31: glob.ts shouldTraverseDirectory over-lenient => perf only (#540), not correctness;",
  "injection getLineTokens is if/else => no mixed rows, no overlap (docstring :187 HOLDS);",
  "_collectTokens pushes only LEAF nodes => no parent/child overlap;",
  "dom.ts isColumnDecoration && precedence is odd but classifies CORRECTLY."
 ],
 "next_veins": [
  "AUTHORITATIVE list is #666 'Future Work' - read it there, this is just the index.",
  "V0 doubt the probe; if the real check is affordable DO NOT MODEL IT. And an exact",
  "  test behind a WRONG COMPARISON SET is still wrong - ask what the filter excludes.",
  "V1 a clamp is safe only if its bound cannot cross the value it clamps.",
  "V2 a well-covered file is not a checked file (9 hits); ask what every fixture holds",
  "  CONSTANT, and what NO test in the repo does at all.",
  "V3 docstring-vs-behaviour (9+). V4 a call site skipping a policy its siblings",
  "  implement - RICHEST, 5 hits; the sibling that is already right is the ORACLE.",
  "V5 drag a selection BOTH ways and diff. V6 a non-dup can still CONFLICT.",
  "V7 a detector is only as good as its UNIT of comparison.",
  "V8 two PRs textually independent + semantically incompatible: ask what a PR takes",
  "  ownership of that ISN'T a line of its own file (a global, an invariant, a fixture).",
  "V9 (08-30) a prose rule in a prompt is ADVISORY - an LLM violated a ban 4 days after",
  "  it shipped (#343). An enforceable guard belongs in CONFIG, not instructions.",
  "V10 (08-30) fixing a filter in ONE sweep does not fix its TWIN - grep for the twin.",
  "V12 (08-30) a docstring can pin the WRONG rule. My 'docstring pins it => PR ok'",
  "  holds only when it pins the RIGHT side: #704 docstring correct (PR it), #695",
  "  docstring endorses the bug (hand over). ASK WHICH SIDE THE DOCSTRING IS ON.",
  "Targets: the 21 UNTESTED conflict clusters; _moveLine (after the decision);",
  "replace/clear APIs; react/use-diff-view.ts; editor-view.ts docstrings.",
  "DONE: project/adapter.ts covered; the '6 unexplained fails' vein is CLOSED.",
  "V13 (08-31) MODELLED TESTS: a test that re-implements src logic passes whatever",
  "  src does. Confirmed: decoration.test.ts buildDecorationMap (STALE - predates the",
  "  line/column split, so #737's new file does NOT retire it); canvas.test.ts",
  "V14 (08-31) TWIN EXPORTS: buildHighlightedSpans exists in highlighter.ts (tested,",
  "  used by dom.ts) AND injection-highlighter.ts (exported as ...WithInjection,",
  "  0 callers, 0 tests, LACKS columnDecorations). Parity test = green + drift-proof.",
  "  simulateTokenRendering. Grep tests/ for 'Simulate|Replicat|logic used'."
 ],
 "merge_sweep_TOOL_AND_TRAP": [
  "TOOL: `git merge-tree --write-tree --merge-base=<mb> refs/prs/A refs/prs/B` (exit!=0 =>",
  "conflict). Exact, ~30ms, no worktree. Use per-pair `git merge-base A B`, not a fixed base.",
  "TRAP, hit 3x: build each PR's file set from **ALL CHANGED PATHS**, not src/+tests/.",
  "13 RA PRs touch neither; benchmarks/index.ts is a registry 5 PRs append to; #559==#760 hid",
  "in biome.json. And compare ALL 180 OPEN PRs, not RA-only: 8 RA PRs conflict only with a",
  "non-RA PR. ALWAYS BUILD THE TREE - the sequential merge exposes a bad filter in seconds.",
  "Fresh: 124/1058 all, 94/955 RA-only, 23 clusters/105 PRs, largest 25, 65 conflict-free.",
  "AN INDEPENDENT SET IS BIASED TOWARD DUPS (identical patches never conflict): subtract dups."
 ],
 "this_run_2026-08-30": [
  "DIGEST (detail in #666 Run History): 181-PR conflict map = 23 clusters/105 PRs; c1-c8",
  "combination-tested ALL GREEN. LESSON: an all-green cluster = COMPETING DESIGNS, not",
  "breakage - deliverable flips from 'which is red' to 'which one'; the ORACLE is WHAT MAIN",
  "ALREADY SHIPS, 2nd oracle the maintainer's OWN stale review. Post on the 0-comment PR."
 ],
 "this_run_2026-08-31": [
  "RUN a (33360602138), drew T9+T3. Both delivered; screening blocked 2 DUP PRs.",
  "T3: replaceAll bypasses 3 guards on main (undo #536, readOnly editor.ts:476, non-editable",
  "_edit). ORACLE = replaceActive (via dispatch) respects all 3. TWO PRs already fix it and",
  "they CONFLICT, both green, each missing what the other has: #537 has the readOnly guard but",
  "emits NO events; #364 emits textChange/change but has NO readOnly guard (main emits nothing",
  "either, so #537 is not a regression). Neither references #536. Commented #364. NO 3rd PR.",
  "T9: line-level decoration tests were ALREADY PR #737. Shipped instead TEST-ONLY branch",
  "repo-assist/test-highlighted-spans-parity. 2280 pass/0 fail, tsc 0, lint 1 info, build ok.",
  "4 mutations verified RED before filing - do that for every test-only PR."
 ]
}