{
"READ_FIRST": "Full state = OPEN Monthly issue (Aug=#666); read EVERY run, ALL of it (48KB), and grep it for the PR/issue number before acting; if it disagrees with this file TRUST #666. This file = pointers + operational facts ONLY.",
"MEMLIMIT": "Dir 12288 INCL .git (~6.2KB); PATCH 10240 = old+new+190 => KEEP state.json <=4875 B. NEVER `git add` to measure (loose objects BLOW the dir check); compute arithmetically. rm -f .git/hooks/*.sample. Never git gc.",
"rules": "main FROZEN ce545ec (ls-remote FIRST). HOLD speculative src PRs (REVIEW is the bottleneck, 128 open) - prefer pushing to an EXISTING PR's branch. FLIPS AN ASSERTED behaviour => comment not PR. safeoutputs CAN VANISH (2x) => verify last run FIRST. PROTECTED: CLAUDE.md, package.json, .github/, .claude/. Caps: update_issue 1/run (spend on #666), comments 10.",
"ENV": "MCP github WORKS (gh CLI does NOT). list_pull_requests/list_issues/big issue_read OVERFLOW to a file => parse in python. curl -sL github.com/O/R/pull/N.diff and api.github.com/repos/O/R/issues/N work unauth (pull #666 into a FILE, patch with python). bun NOT preinstalled: curl -fsSL bun.sh/install|bash, PATH=$HOME/.bun/bin:$PATH. ABSOLUTE paths; cwd RESETS. Bash 60s => nohup to FILE + sentinel. NEVER git while a bg job holds the worktree. push_to_pull_request_branch: LOCAL branch name must EQUAL the remote PR head ref (fetch; checkout -b tmp FETCH_HEAD; work; git branch -m <ref>; push).",
"GATES": "bun test does NOT typecheck. ALWAYS test+typecheck+lint, and DELETE probe scripts first (they break typecheck). BASELINE ce545ec: 2274 ran/2265/0; lint 'Found 1 info' 168 files; typecheck prints only '$ tsc --noEmit'; bench 118/0. FLAKE buffer.test.ts 'line access is O(1)' ~2 of 4 runs; RERUN 2x before blaming a patch (grep '^\\(fail\\)', not tail).",
"git": "ce545ec is a PARENTLESS ROOT => merge-base EMPTY for the 18 pre-2026-03-23 orphans; `git log $EMPTYVAR` silently reports HEAD. APPLY THE .diff, NEVER judge by checkout. --3way FAILS, use plain `git apply`; revert = `git checkout -- .` PLUS `git clean -fdq tests/ src/ benchmarks/`.",
"SMELLS": "(7) PRIMITIVE REPLACED BY ITS BODY => READ THE CALLEE'S FAILURE RETURN. (13) HOISTED CACHE => check the FALL-THROUGH path AND whether the cached value round-trips. (14) diff @@ headers LIE - grep the hunk's REAL fn name; COUNT ALL CALL SITES. Green-on-main test in a red PR = control or mislabel, SAY WHICH. A SWEEP THAT NEVER DRIVES THE CHANGED PATH IS A CONTROL. RECIPE: untracked script in repo root (rm before commit), import ./src/, print #SUMMARY of cases that ACTUALLY drove the path; main vs `git apply` PR.diff; THEN MUTATE - the mutant MUST diverge, else green means NOTHING.",
"DUPTRAP": "6 OFFENCES. GREP OPEN PR TITLES FOR THE FUNCTION NAME (not the number) BEFORE filing, AND GREP #666 FOR THE PR NUMBER BEFORE PROBING - 08-14b I re-derived the #479 write-through #666 already had. Comment on the SURVIVOR. TWIN-DIFF retires a PR unrun: curl both .diff, `grep -v '^index '|md5sum`; different md5 can still be the SAME change (#366/#488), may be SUPERSET (499>420).",
"TESTHARNESS": "helpers.ts: createBuffer(id,text), excerptRange(a,b), mbRow. MB snapshot has NO .text() - use snap.lines(mbRow(0),mbRow(mb.lineCount)). biome BANS `!`. fc.string() v4=ASCII no \\n. happy-dom rects 0, getContext('2d')=NULL. multiBuffer.edit() DECLINES cross-BUFFER ranges => spanning excerpts from 2 DIFFERENT buffers edits NOTHING = DEAD CONTROL (08-14b). To span: ONE buffer + hasTrailingNewline:false (separator rows split row groups). addExcerpt opts {hasTrailingNewline, editable}.",
"VEINS": "UNEXECUTED_PRS EXHAUSTED. PROBE DEBT ZERO. RETIRED 08-14b, DO NOT REDO: (a) _mergeSelections unguarded drop UNREACHABLE from the 3 movement sites (352 cases, 0 drove it; mutant -> 76) = DEAD CODE. (b) _dedentLines stranding does NOT repro (190 cases, 0; mutant -> 237). TASK 6 = NO-OP WHILE main FROZEN: 0 check runs on EVERY head => mergeable_state is CACHE NOISE, only `mergeable` counts. NEXT: #666 Future Work (bench #511/#533). T2 LEFT 461->431; SKIP 282,260,157,111,672,673,682,696,496. T1 dead.",
"last": "08-14b T6+T9. T6 no-op again. T9 -> pushed 3 excerpt-boundary regression tests to #479's BRANCH, not a new PR (128 RA PRs queued); gates 2280/2271/0 twice, tc 0, lint 1 info; tests RED on main (5 fail) = real regressions. Then CORRECTED myself on #479: 'clears the last objection' was WRONG - its editability-guard bug was already in #666, still unfixed. 2 comments, 1 push, 1 update_issue. Tree clean.",
"GRIT": "SOLVED-NEGATIVE, DO NOT RETRY the .grit narrowing - impossible in Biome GritQL 2.5.8 (no node kinds; an invalid one matches NOTHING silently; only 1-specifier imports match, our 3 sites are multi-specifier). LEVER: `// biome-ignore lint/plugin/no-type-assertion: expect: ...` above the line. Apply WITH the Biome bump. Patch in #666."
}