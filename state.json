{"READ_FIRST": "Full state = the OPEN Monthly Activity issue (Aug=#666); read it EVERY run. This file = ONLY operational lessons #666 lacks.",
"MEMLIMIT": "HARD: dir INCL .git <=12288 B; .git alone is 6535 => state.json MUST be <=5700 B. Verify with `du -sb` then push_repo_memory. `rm -f .git/hooks/*.sample` each run; never git gc (it GREW it). Dump separators=(',\\n',': ') so each key is ONE line.",
"rules": "main FROZEN ce545ec since 2026-03-23 (re-verify ls-remote FIRST). Hold on SPECULATIVE PRs (bottleneck=REVIEW); CORRECTNESS+SECURITY in scope. safeoutputs writes CAN SILENTLY VANISH - 2x: 08-06a create_issue, 08-06b the ENTIRE charWidth PR (no branch, no PR, tool said success). => never say 'I filed X'; link by SEARCH url; FIRST ACTION each run = verify last run's PR exists. PROTECTED: CLAUDE.md, package.json, safe-outputs cfg, .github/ .agents/ .claude/.",
"TESTHARNESS": "mod = _isMac?metaKey:ctrlKey (input-handler.ts:273) => Linux ctrlKey; selections via editor.extendSelectionTo(pt) NOT dispatch selectTo; getText = snap.lines(0,snap.lineCount).join(nl); helpers tests/helpers.ts. noUncheckedIndexedAccess => compare whole ARRAYS with toEqual.",
"ENV": "GITHUB_TOKEN empty => mcp__github__*. Large list_*/search_* overflow to a file => python3 json.load. bun absent EVERY run: curl -fsSL https://bun.sh/install|bash; export PATH=$HOME/.bun/bin:$PATH; bun install --frozen-lockfile - from REPO ROOT, ABSOLUTE paths (cwd RESETS after heredocs). Bash cap 60s => nohup to a FILE. lint '1 info' + exit 0 = PASS.",
"git": "NEVER 'git diff ce545ec pr/N' - bot branches predate ce545ec; use $(git merge-base ce545ec pr/N). Fetch refs/pull/N/head:refs/remotes/pr/N. ARCHIVE tree: --3way FAILS, use plain 'git apply'. Undo: reset --hard -q ce545ec; git clean -fdq.",
"FLAKE": "3 flakes, 2 SHAPES. (a) COLD: time() (helpers.ts:201) NO warmup; benchmark() warms min(100,iters/10) so benchmark(fn,100) warms only 10 (spread 59% vs 7%). multibuffer.test.ts:1041 cold 0.31ms vs warm 0.007 => #690 fixes. (b) RATIO `X.avgMs<early.avgMs*3+0.001` between two WARM benchmarks: buffer.test.ts:444 (#611 widens to *10+0.01 = what :446 ALREADY uses) AND multibuffer.test.ts:1038 excerptAt, same remedy, unwritten. #673.",
"NEGATIVE": "DONE, don't redo: REDGREEN sweeps, MERGE_TRUTH, NOSRC_BLOCK, DUP_AUDIT, MUTATION_SWEEP, 342-vs-449, #333, #345, #678, #679, #683, #684, #685, #687, ADDEXCERPT_COW(unopened), #689, DOMRENDERER_CHARWIDTH(redone 08-06c). SECAUDIT 08-06c: dependabot DISABLED(403) => list_global_security_advisories affects=pkg@ver. EXACTLY 4 live, ALL devDeps, #675 clears all & pins still latest: happy-dom 20.8.4 (GHSA-6q6h-j7hj-3r64, GHSA-w4gp-fjgq-3q4g); ws 8.19.0 (GHSA-96hv-2xvq-fx4p, GHSA-58qx-3vcg-4xpx). PROD deps CLEAN; no other outdated dep has an advisory. FALSE ALARMS: dup-test-name scan; mergeability; 'Closes #N' regex (grep the SYMBOL); Enter DOES auto-indent.",
"OUTAGES": "#659 #661 #657 #677 each need one human `gh aw compile`; blocked while main frozen; details in #666. docs-update.md is SEPARATE from unbloat-docs.md + glossary-maintainer.md (HEALTHY).",
"dead": "T1 dead (0 unlabelled). T2: ALL 37 open issues already have an RA comment (#86 = no-op dumping ground, never comment) => live supply = NEW sibling-bot PRs/issues only, by CREATED desc; commented #690 08-06c. T6 DRY x11 (0 check runs on #689/#687/#685, base sha still ce545ec => SAMPLE 2-3, never rescan 123). ~145 open PRs, dup 32.",
"T11_FORMAT": "ONE LINE PER ITEM (#666); evidence lives on each thread, old bodies in the EDIT HISTORY. Run History ~6 newest, drop the oldest. update_issue body is INLINE => a replace is a full retype.",
"last": "08-06c sel T2,T4. The 09:02 charWidth PR NEVER EXISTED (max PR #690, no remote branch; the ls-remote hit 'fix/auto-measure-charwidth' is the MAINTAINER's March branch). RECREATED: dom.ts:865 `return width||8` + tests/renderer/dom-charwidth.test.ts, 9 tests, 5 RED on main (x=40 -> NaN; 44/48/60/100 -> col 11), 2/2 mutants; 2274 pass 0 fail. NEXT RUN VERIFY: search is:pr is:open charWidth; if ONLY #689 returns it vanished AGAIN - escalate in #666, do NOT rewrite a 3rd time. Also commented #690, updated #666.",
"PERFMETHOD": "CEILING FIRST: stub the fn to a constant to bound the win before coding. ONE ARM PER PROCESS - JIT fakes ~20% gaps between arms that MUST be equal; equal control arms = the validity check. Measure the fast path's MISS, and your OWN fix for regression.",
"ORACLE": "NEVER let a fixture be a file you're editing (phantom diff); pin via `git show ce545ec:path`. Output oracles CANNOT see index-only bugs - also assert vs a brute-force scan. Re-verify the tree after a sweep.",
"ROPE_CHUNKS": "RECURRING BUG SOURCE: textToChunks breaks at a newline only if one is in the 1024 window => a >1024-unit line spans chunks STARTING MID-LINE. Bugs: byteLength (#545/#546), point<->offset (#687). line()/lines()/lineIterator CLEAN.",
"RENDERER_HARNESS": "canvas.ts/webgpu.ts UNTESTABLE not neglected: happy-dom getContext('2d')=NULL => mount() throws. COPY tests/renderer/canvas-renderer.test.ts (#689): stubs getContext/OffscreenCanvas/rAF(QUEUE)/getComputedStyle; webgpu.ts needs a DEVICE stub. getBoundingClientRect().width ALWAYS 0 => DomRenderer runs at charWidth 0; to reach a MEASURED branch: defineProperty container.appendChild, then defineProperty getBoundingClientRect on the node with textContent==='MMMMMMMMMM'. NEVER patch Element.prototype via `this as Element` (TS2352).",
"AUDIT_METHOD": "Where TWO impls of one interface exist, DIFF THEM - cheapest audit found, 3 hits (both charWidth halves + dom/canvas hitTest). Left: dom/canvas render, webgpu. ALSO a comment EXPLAINING a loose assertion marks a bug worked around (dom.test.ts:606)."}