{
"READ_FIRST": "Full state = OPEN Monthly issue (Aug=#666); read EVERY run, ALL of it (~54KB), and grep it for the PR/issue number before acting; if it disagrees with this file TRUST #666. This file = pointers + operational facts ONLY. #666 IS NEAR THE 64KB CEILING - compress oldest Run History entries BEFORE adding the next one.",
"MEMLIMIT": "Dir 12288 INCL .git (~6.2KB); PATCH 10240 = old+new+190 => KEEP state.json <=4875 B. NEVER `git add` to measure (loose objects BLOW the dir check); compute arithmetically. rm -f .git/hooks/*.sample. Never git gc.",
"rules": "main FROZEN ce545ec (ls-remote FIRST). HOLD speculative src PRs (REVIEW is the bottleneck, 128 open) - prefer pushing to an EXISTING PR's branch. FLIPS AN ASSERTED behaviour => comment not PR. safeoutputs CAN VANISH (2x) => verify last run FIRST. PROTECTED: CLAUDE.md, package.json, .github/, .claude/. Caps: update_issue 1/run (spend on #666), comments 10.",
"ENV": "MCP github WORKS (gh CLI does NOT). list_pull_requests/list_issues/big issue_read OVERFLOW to a file => parse in python. curl -sL github.com/O/R/pull/N.diff and api.github.com/repos/O/R/issues/N work unauth (pull #666 into a FILE, patch with python). bun NOT preinstalled: curl -fsSL bun.sh/install|bash, PATH=$HOME/.bun/bin:$PATH. ABSOLUTE paths; cwd RESETS BETWEEN Bash calls (persists WITHIN one). Bash 60s => nohup to FILE + sentinel. NEVER git while a bg job holds the worktree. push_to_pull_request_branch: LOCAL branch name must EQUAL the remote PR head ref (fetch; checkout -b tmp FETCH_HEAD; work; git branch -m <ref>; push).",
"GATES": "bun test does NOT typecheck. ALWAYS test+typecheck+lint, and DELETE probe scripts first (they break typecheck). BASELINE ce545ec: 2274 ran/2265/0; lint 'Found 1 info' 168 files; typecheck prints only '$ tsc --noEmit'; bench 118/0. FLAKE buffer.test.ts 'line access is O(1)' ~2 of 4 runs; RERUN 2x before blaming a patch (grep '^\\(fail\\)', not tail).",
"git": "ce545ec is a PARENTLESS ROOT => merge-base EMPTY for the 18 pre-2026-03-23 orphans; `git log $EMPTYVAR` silently reports HEAD. APPLY THE .diff, NEVER judge by checkout. --3way FAILS, use plain `git apply`; revert = `git checkout -- .` PLUS `git clean -fdq tests/ src/ benchmarks/`.",
"SMELLS": "(7) PRIMITIVE REPLACED BY ITS BODY => READ THE CALLEE'S FAILURE RETURN. (13) HOISTED CACHE => check the FALL-THROUGH path AND whether the value round-trips. (14) diff @@ headers LIE - grep the hunk's REAL fn name; COUNT ALL CALL SITES. Green-on-main test in a red PR = control or mislabel, SAY WHICH. A SWEEP THAT NEVER DRIVES THE CHANGED PATH IS A CONTROL. RECIPE: untracked script in repo root (rm before commit), import ./src/, print #SUMMARY of cases that ACTUALLY drove the path; main vs `git apply` PR.diff; THEN MUTATE - the mutant MUST diverge, else green means NOTHING.",
"DUPTRAP": "6 OFFENCES. GREP OPEN PR TITLES FOR THE FUNCTION NAME (not the number) BEFORE filing, AND GREP #666 FOR THE PR NUMBER BEFORE PROBING. Comment on the SURVIVOR. TWIN-DIFF retires a PR unrun: curl both .diff, `grep -v '^index '|md5sum`; different md5 can still be the SAME change (#366/#488), may be SUPERSET (499>420). For RIVAL IMPLS (not twins): diff SORTED ADDED LINES + added API (`grep -E 'set[A-Z]|get[A-Z]|private _'`), then apply A and `--check` B to prove mutual exclusion.",
"VEINS": "UNEXECUTED_PRS EXHAUSTED. PROBE DEBT ZERO. BENCH DEBT ZERO (511/533 measured 08-14c). RETIRED, DO NOT REDO: (a) _mergeSelections unguarded drop UNREACHABLE (352 cases, mutant->76) = DEAD CODE. (b) _dedentLines stranding does NOT repro (190 cases, mutant->237). (c) 08-14c canvas triple #324/#378/#379. TASK 6 = NO-OP WHILE main FROZEN: 0 check runs on EVERY head => mergeable_state is CACHE NOISE. T7 = NO-OP: every stale non-RA PR is bot- or MAINTAINER-authored => a 'need help?' nudge is spam; execute them instead. T3 = DRY: every open bug issue has a queued fix PR, or is an escalated semantics call (#695,#704,#674), or a protected path (#659,#661,#671). T2 LEFT 461->431; SKIP 260,157,111,672,673,682,696,496,282(done 08-14c). T1 dead.",
"last": "08-14c T7+T3, both adapted. T7: nudges declined as spam (stale non-RA PRs are all bot- or maintainer-authored); EXECUTED the 3 rival #282 impls instead - #324 and #379 2279/2270/0, #378 2284/2275/0, all tc0 + lint baseline, MUTUALLY EXCLUSIVE (378 applied => 324/379 stop applying). Recommend #378 BUT port #379's getCursorBlink() first (378 NOT a superset). #324 odd one out (setBlinkInterval; tests assert nothing). T3 dry => ran the owed benches: #533 118->124 (bracket worst case 0.104ms = 10x under budget => NO scan-loop opt warranted), #511 118->123 (cost ~ TOTAL LINES not file count; isEqual 67x cheaper => all cost in diff()). 3 comments (#282,#511,#533), 1 update_issue. No PR. Tree clean.",
"GRIT": "SOLVED-NEGATIVE, DO NOT RETRY the .grit narrowing - impossible in GritQL 2.5.8 (no node kinds; an invalid one silently matches NOTHING; only 1-specifier imports match, our 3 sites are multi). LEVER: `// biome-ignore lint/plugin/no-type-assertion: expect: ...`. Apply WITH the Biome bump. Patch in #666."
}
