mine:#310#339#565#575(all clean/draft/no-conflict 0810) mo:Aug#670 main:ce545ec last:2026-08-10 run:31416897264 hold-reinstated(maint-only) bun-via-curl
issues-filed:#664(0731)#672(0802)#673(0802)#682(0804)#688(0805)#700(0809 fixture,LINKED)+0810-harness-slack(LINK IT NEXT RUN)
**DIVISION OF LABOUR: I identify+measure+VERIFY, RepoAssist writes PRs (#672->#685, #682->#684). Keep filing issues + verifying its PRs, NOT writing PRs.**
**0808: found keystroke path is O(doc) via byteLength() on every edit; ALREADY fixed by open PR#510 - verified it (50x@16K, main misses <1ms target) and commented instead of filing a dup. **0809: NOT insert-only - whole editor command surface 4-23x. See notes.md.**
bl:GA/IH(#377)/undo(#435)/bench-fixture/flaky(#673->PR#611 just merge)/addExcerpt(#688,dormant)
REJECTED-do-not-reinvestigate: undo-shift(0802,0.083us); diff/patch.ts(0804,healthy); renderer dom.ts:533(0807,2% frame budget); **multicursor-quadratic(0808: it is LINEAR ~150us/cursor, NO defect - only AMPLIFIES per-edit cost by N)**; **dedent-slow(0809: 100% fixture artifact)**; glob.ts+wrap-map.ts(0809 already optimized); input-handler.ts+anchor.ts/resolveAnchor(0810: pure switch / already lazy-cached+binsearch+viewport-culled)
cmds:bun1.3.14 typecheck0 lint0(1 info) bench118/118(120 w/#510) test2265/0 ; harness.ts:78 gates avg ONLY(p95 unenforced) ; bun test FLAKY buffer.test.ts:444 = 10/36(28%)
commented:#492(0730)#668(0801)#672(0803)#510(0808) - NO human comments EVER, all other actors are bots => T5 is a repeat no-op. 0809: deliberately did NOT re-comment #510(3 comments already, no human reply = spam)
0811:T3+T4+T5+T7 run:31520841489. **DURABLE RULE - ALWAYS `git merge-base pr/N main` WHEN VERIFYING PRs; `mergeable_state:clean` DOES NOT DETECT ORPHANS.** Bulk: `git fetch origin '+refs/pull/*/head:refs/remotes/pr/*'` then loop merge-base. 18 open orphans, ALL <=#354 = ONE history rewrite (#705). Detail in #670.
0811-LESSON(6th attribution catch): my "live typing" fixture appended chars INSIDE the last line => line counts EQUAL => the length-mismatch fast path was NEVER REACHED; it measured pure overhead and showed a fake 5% regression. Rebuilt to append whole LINES. Residual 5% NOT CAUSAL - PR adds ONE int compare, cannot cost 0.45ms. **Check your fixture actually reaches the branch you are measuring.**
0811: #703 = the 0810 harness-slack issue, NOW LINKED in #670. notes.md is AT 10240 cap - do NOT append there, use memory.md.
0813:T1+T2+T4+T7 run:31728939383. main STILL ce545ec, SA-list 81 items ALL still open (paged 519 PRs + closed-issue search; 0 ticked, 0 merged since 0804). Flake FIRED 1/4 runs today => cumulative 11/40=27.5%.
**0813-NEW AXIS: keystroke cost is LINEAR IN EXCERPT COUNT, not just doc size.** Fixture holds TOTAL ROWS+BYTES CONSTANT, varies excerpt count. TWO variants and the contrast is the whole point: A shared(N excerpts slice ONE buf)=0.317/0.612/0.980ms @100/500/1000; B distinct(N excerpts, N own bufs)=0.048/0.160/0.312. In B the EDITED buffer SHRINKS 10x as n grows 10x yet cost rose 6.5x => growth is bookkeeping, NOT doc size. A is dominated by #682/#684 textSummary (already tracked); B is NEW.
**0813-ATTRIB: excerptDataIndex + excerptInfoIndex each rebuilt EXACTLY 1x/keystroke, 2 SEPARATE passes producing IDENTICAL template-string keys = 55/68/67% of keystroke @100/500/1000.** MultiBuffer.snapshot() = EXACTLY 5.0 calls/keystroke, constant in n (PR#453 body guesses "3-4" - corrected it there). Filed as new issue (LINK IT NEXT RUN - number unknown at write time, SA entry has a bare /issues link).
0813-MEASURED FIXES: single-pass _buildIndexes(~18 lines) = -13.5%@500 -18.0%@1000, 3/3 rounds, 2265pass. PR#453/#455 = -10.0%@500 -10.2%@1000, 12/12 paired, 2265pass. Snapshot MEMOIZATION prototype (cache+clear in _markDirty) passes 2265 but adds only ~5%@1000 over #453 => NOT worth filing alone; the per-keystroke index rebuild is inherent to "new snapshot per edit", memoization cannot remove it. **Prototyping BEFORE filing stopped me overselling it.**
0813-DUP TANGLE: #453 and #455 are BYTE-IDENTICAL diffs; **#454 is an ISSUE not a PR** (pulls/454=404, issues/454=200) carrying the same PR body with no code. Old SA said keep#455/close#453 - I KEPT that ordering (no flip-flop) but my comment is ON #453; SA now says so explicitly. #337 also ORPHAN + overlaps #310 (both edit diffLines equality check, not identical).
0813-LESSON(7th): **the grep-before-filing rule paid off AGAIN** - searched "snapshot in:title" FIRST and found #453/#454/#455 already existed; I'd have filed a dup. Also CORRECTED 2 of my own records: (a) 0810 gap-list claim "no bench covers many-excerpt edits" is WRONG (insertText 100 excerpts x10 exists) - real gap is it's ONE POINT at 3.7x slack; (b) 0810 "MultiBufferSnapshotImpl cleared/already optimised" holds for LOOKUPS but NOT CONSTRUCTION. **Lazily-cached-per-snapshot != cheap when every keystroke makes a new snapshot.**
0813-METHOD THAT WORKED (reuse): to separate "scales with doc" from "scales with structure", build 2 fixtures where the suspected driver varies while EVERYTHING else is pinned, and make the control variant move the OPPOSITE way (edited buf shrinks as count grows). Growth surviving that is unambiguous. Also: interleave A/B/C rounds (not A-then-B) - ordering/GC bias is real at these magnitudes.
22	0814:T3+T5+T6+T7 run:31826906717. main STILL ce545ec. #713 = the 0813 index-rebuild issue, NOW LINKED in #670. **NEW ROUTINE THAT PAID OFF: measure competing/adjacent PRs AGAINST EACH OTHER, not one at a time.** #510/#684/#453 each had a solo number, nobody knew if they overlap or what order to merge. File-level checkout works when diffs are disjoint(510=buffer.ts; 684=excerpt.ts+multibuffer.ts; 453=multibuffer.ts) - for 684+453 use `git merge-tree --write-tree pr/A pr/B` then `git checkout <tree> -- paths`. All 3 share merge-base w/ main (NOT orphans).
23	0814-RESULTS(3 interleaved rounds, 1 variant/process, monotone 3/3): shared@100 main239.7 +510=172.4(-28%) +684=132.1(-45%) both=51.4(-79%); shared@1000 main671.4 621.1(-7.5%) 464.6(-31%) 388.4(-42%). **SUPER-ADDITIVE: combined saving EXCEEDS sum of individual (188 vs 175us@100).** distinct@100/1000: ALL variants flat within +-2% = ZERO effect. Full stack 510+684+453: shared100 4.73x, shared1000 1.86x, distinct1000 1.12x(all of it #453). Stack: typecheck0 lint-clean 2265pass/0fail, NO flake this run(cumulative now 11/42=26.2%).
24	**0814-KEY INSIGHT: the distinct(multi-file) shape is untouched by the ENTIRE keystroke PR cluster** => #713 orthogonality, which I had argued from attribution only on 0813, is now EMPIRICALLY confirmed. Residual ~184us = the index rebuild. Mechanism stated honestly in the comment: at 1000 distinct excerpts each buf is 1 ROW so a lazy summary has nothing to skip - that is mechanical, NOT evidence the PRs are weak.
25	0814-T5: commented ONCE, on #684 (had NO Perf Improver comment yet, only 1 RepoAssist). Did NOT re-comment #510(3 comments) or #453(commented 0813) - no human replies anywhere, still 0 human comments EVER. 0814-T6: bench gap(no distinct-buffer edit fixture, edit benches stop at 1 excerpt count) ALREADY in #713 => filed NOTHING. Restraint held.
26	0814-FIXTURE REUSE: /tmp fixture = createMultiBufferEditor + editor.dispatch({type:"insertText"}), NOT handleCommand; snap.lineCount is a PROPERTY not a method. shared=N slices of 1 buf, distinct=N bufs. 200 warmup+400 iters, median. Rebuild it each run, it is not committed (PR hold).
27	0815:T1+T2+T5+T6+T7 run:31898606911. main STILL ce545ec, 84 SA items ALL unticked, still 0 human comments ever. tests 2265/0 NO flake (cum 11/43=25.6%), bench 118/118, typecheck0 lint1info.
28	**0815-NEW AXIS: BYTES, not ms. Nobody had ever measured memory here.** Per keystroke: 52.6KB@100exc / 213.4KB@500 / 854.5KB@2000 = marginal 437.7 B/excerpt/keystroke (index pair 348.8 + 5x snapshot arrays 88.9). Idle: ~726 B/exc shared, ~1653 B/exc distinct, text held constant. shared==distinct to 0.03% => PURELY structural, 3rd independent confirmation #713 is orthogonal. Byte axis reproduces to 0.1% over 3 rounds - FAR quieter than timing, would make a better gate than #703 wall-clock targets.
29	**0815-TOOL LESSON (BIGGEST): process.memoryUsage().heapUsed IS BROKEN in bun 1.3.14 - returned the IDENTICAL constant 175406 for empty process / built 2000-excerpt editor / 200 retained snapshots. USE `import {heapStats} from "bun:jsc"` -> heapSize+extraMemorySize, with Bun.gc(true) x3 both sides. extraMemorySize alone is 26% of Map cost - heapSize-only under-reports.** My first pass showed a fake SUBLINEAR curve above n=500; it was the instrument. Reproduced the same dip in a plain new Map() microbench w/ zero repo code => proved it was never the code. CHECK THE INSTRUMENT BEFORE BELIEVING THE SHAPE (8th attribution overturn).
30	0815-FILED: harness.measureMemory has ZERO callers repo-wide AND under-reports 4.9-6.2x (0.0KB for a 1000-entry Map costing 139KB). 3 causes: Bun.gc() w/o true is not sync; no gc before the after-read; heapUsed omits extraMemorySize. Issue number UNKNOWN at write time - LINK IT IN #670 NEXT RUN (SA entry has a bare /issues link).
31	0815-MAP COST CONSTANTS (reuse): Map entry w/ FRESH template key = 173.8 B/entry; w/ PRECOMPUTED shared key = 131.1 B/entry => the key string is 42.7 B = 24.5%. Cross-val: 2 x 173.8 = 347.6 vs 348.8 B/exc measured thru real code path, 0.3% agreement. So RepoAssist single-pass (shares key across the 2 maps) saves 42.7 B/exc/keystroke=12.2% of index garbage; caching key on the excerpt saves 85.4 B=24.5%. That ANSWERS the "plus the string-allocation saving" it left open - commented #713 (its only prior comment was RepoAssists).
32	0815-MEM FIXTURE: retain K=200 snapshots then force BOTH indexes via snap.resolveAnchors([anchor]) (resolveAnchors([]) early-returns, builds nothing); mb.createAnchor({row,column},"left"). ONE n PER PROCESS. TOTAL_ROWS pinned 2000.
33	0815-ACCEPTED RepoAssist correction on #713: distinct-shape growth is 3.2x NOT my 6.5x (my 100-exc baseline was an outlier). Said so plainly. #670 body was 51.3KB->55.4KB; I condensed 08-08+08-09 into the condensed block to buy headroom (cap 65.5KB, ~2KB/run growth).
