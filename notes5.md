SIZE-WATCH: memory.md 10206, notes.md 9976, notes2.md 8468, notes3.md 8588, notes4.md 9840 — ALL at/near the 10240 cap. APPEND HERE. Start notes6.md when this nears ~9KB. Do NOT trim the older five: deletions cost patch bytes and the 10KB PATCH cap is the binding limit.

0823:T3+T4+T5+T7 run:32655145678. main STILL ce545ec, still 0 human comments EVER, 26 SA boxes ALL unticked. typecheck0, lint1info, bun 1.4.0.

**0823-BEST RESULT: applied the 0814 "measure adjacent PRs AGAINST each other" routine to the SELECTION/ANCHOR cluster and it found an ANTAGONISM nobody could see one-PR-at-a-time.** 7 open PRs, 3 files, all based on mainHEAD (no orphans): selection.ts #371==#459 (semantic dup, comment wording only), cursor.ts #384==#386 (BYTE-identical src diff), editor.ts #366/#437/#488 (3 different diffs; #366==#488 semantically). **=> 6 PRs = 3 distinct changes.**

**0823-THE INSTRUMENT IS THE WHOLE STORY: I used resolveAnchor CALL COUNTS, not wall-clock. Deterministic, ZERO variance, byte-identical across repeats.** Timing could NOT grade these PRs (effect ~1.3% of keystroke vs my own 0821 noise floor p50 4.7%) — my first timing run showed EVERY arm slower than main, which is mechanically impossible for pure fast-path adds; cause = fixed arm order within each round + upward drift, main always ran first. **When the expected effect is below the noise floor, STOP TIMING AND COUNT THE OPERATION.** Same lesson-shape as 0815 (bytes quieter than ms).
0823-COUNTING TOOL (reuse): resolveAnchorRange (anchor.ts:63-64) DELEGATES to snapshot.resolveAnchor, so wrapping ONE method captures every path direct+indirect. Wrap mb.snapshot() and re-wrap resolveAnchor on each returned snapshot. insertText@50cursors = 349 calls across 152 snapshots.

0823-RESULTS, resolveAnchor calls/keystroke @2000rows/50cursors (insertText / deleteBackward / moveCursor):
main 349/349/349 | #371==#459 249/249/249 | **#437 299/349/349** | #488==#366 251/251/251 | #371+#437 299/249/249 | #437+#488 201/251/251 | **#371+#488 151/151/151 (BEST)** | #371+#437+#488 201/151/151.
=> **#437 saves NOTHING on deleteBackward/moveCursor, and ADDING it to #371+#488 takes insertText 151->201 (+33%). It is a NET REGRESSION in combination.** RepoAssist had measured #437 SOLO and called it "no detectable effect, take it as a simplification" — correct in isolation, wrong in combination. Recommended CLOSE.
0823-MECHANISM (per collapsed selection in _resolveSelectionsBottomUp): main = isCollapsed(2 resolves)+headAnchor(1) = 3; +#371 = 0+1 = 1; +#437 = resolveAnchorRange(2), no isCollapsed = 2. #437 replaces a path #371 already cut to 1 with one costing 2. #488 saves 2*(n-1)=98 (the lastSel re-resolve in the merge loop). **Model predicts 7/7 measured deltas EXACTLY — that is what kills the fixture-artifact objection. Always state the arithmetic cross-check.**

**0823-2nd BIG RESULT: MERGE ORDER changes this cluster's measurable worth ~20x.** @50 cursors main=8.23ms (**8.2x OVER the <1ms CLAUDE.md target**); +#510 alone = 0.371ms (14.3x, 4/4, PASSES). The pair saves the SAME ~0.11ms absolutely, but that is ~1.3% of main and **29.2% on top of #510 (1.412x, 9/9 ABBA rounds, UNANIMOUS direction)**. Only the denominator moved. => quote both denominators or the number is meaningless. Reinforces #510 as merge-first (now 3rd independent line of evidence).
0823-ABBA IS THE FIX for ordering bias: alternate which arm runs first each round (round%2). 9/9 unanimity + 0821's "direction unanimity is the discriminator" is what makes a 1.4x claim safe.

0823-TWO OF MY OWN INSTRUMENT ERRORS CAUGHT (both before anything was published):
(a) **"#371 fails a test" — IT DOES NOT.** One arm-run showed 1 fail; re-ran 6/6 clean. It was the known buffer.test.ts flake (#673). Nearly attributed a flake to a PR. **Always re-run a single failure before attributing it.** Flake now 12/65 = 18.5% cumulative (was 11/49).
(b) **"deleteBackward crashes on clean main with 1 cursor" — IT DOES NOT.** granularity is REQUIRED in the EditorCommand union (types.ts:67); my dynamic-import fixture bypassed typechecking so `{type:"deleteBackward"}` passed undefined -> moveCursor returned undefined -> TypeError. **Dynamic `await import()` fixtures get NO type checking — a "bug" found that way must be re-checked against the declared type before filing.** Would have been a false bug report.

0823-TEST-COUNT MYSTERY RESOLVED (do not re-investigate): `bun test` reports "2274 tests across 76 files" but **2265 pass + 3 skip + 6 todo = 2274**. My recorded 2265 is the PASS count and is UNCHANGED. My grep `^\s*(pass|fail)` matched nothing because the format is "<n> pass" (number FIRST). Correct grep: `[0-9]+ (pass|fail|skip)`.
0823-VALIDATED: #371+#488 = 2265 pass/0 fail 3/3 runs, typecheck0, lint 1info. All 5 single arms also 2265/0.

0823-#688 IS NOT A MEASUREMENT DEBT (my memory said "dormant", which was misleading): it is thoroughly measured (4.2s @8000 excerpts, per-excerpt table 6.8x->86.4x, two-way attribution) + RepoAssist confirmed. "Dormant" meant NO listener is ever registered repo-wide, so the O(n^2) path is dead code in practice. Checking cost 1 API request and stopped me re-doing finished work.
0823-T4: #310+#339 STILL orphans (no merge-base) - maintainer call #705, did not churn. #565 mergeable_state=**unstable but check-runs total_count=0** = CI NEVER RAN (same as 0805/0817 pattern) => NOT a failure of mine, nothing to push. #575 clean. No pushes this run.
0823-T5: 2 comments — **#437** (had ZERO Perf Improver comments; my conclusion CONTRADICTS RepoAssist's solo measurement, highest-value placement) and **#488** (the recommended keeper). Posted NOTHING on #371/#459/#366: RepoAssist ALREADY flagged #371==#459 and recommends keeping #459 — **I aligned with its ordering rather than flip-flopping** (0813 lesson) and had nothing non-duplicative to add.
0823-T7: #670 08-22 entry WAS present, body 13365B intact (0822 lean rebuild survived — the 0821 destruction did NOT repeat). 13365->14585B after adding the cluster SA item + 08-23 entry and condensing 08-22. **Held to the ~15-20KB durable ceiling.** #739 NOW LINKED (was a bare search link).
0823-FIXTURE (rebuild each run, not committed; PR hold intact): copy makeEditor from benchmarks/editor.bench.ts VERBATIM — createBuffer(id,text) takes TWO args, excerpt range is {context:{start,end},primary:{start,end}} with BufferRow points, NOT a byte range. N cursors via `for i<N: dispatch({type:"addCursorBelow"})`. Worktree + `ln -s node_modules`, arm switch by `git checkout pr/N -- <file>`; #437+#488 both touch editor.ts and merge CLEANLY via `git merge-tree --write-tree pr/437 pr/488` then `git checkout <tree> -- <file>`. Assert `git status --porcelain` empty + `git worktree prune` at the end.
0823-COST CONSTANTS (reuse): resolveAnchor ~0.54us warm (same snapshot, cache hit) / ~0.79us cold (fresh snapshot, minus 0.093us snapshot() overhead). Whole cluster addresses 349*0.79 = ~276us = only **3.4% of main's 8.23ms multi-cursor keystroke** — the other ~96% is the already-tracked #510/#684/#713 work. Said so plainly in both comments rather than overselling.
