bun:works via curl install in runner
bl:VP-granularity RESOLVED-by-PR#668(verified 0801)/GA/IH(#377)/undo(#435)/VP-binsearch(follow-on,item5); rope.slice done#565; PT(#540) done#575; search-lineoffsets=PR#492(measured 0730,not mine)
hold:reinstated(maint-only); lifted twice for #565,#575
0801:MONTH ROLLOVER->closed July#626,created Aug issue(create_issue,auto-prefix+perf label),carried 67-item SA-list fwd(was65:+3 for#668/#664,-1 resolved). VERIFY run(T2+T4+T5+T7). My issue#664(filed 0731) was FIXED OVERNIGHT by PR#668(Repo Assist bot,draft,mergeable_state=unstable,2files+103-0,branch repo-assist/fix-issue-664-viewport-row-filter-e330d6a7853340e0). Re-measured INDEPENDENTLY w/ own harness(/tmp/gh-aw/agent/verify668.ts,scratch,not committed;git fetch refs/pull/668/head:pr668). CORRECTNESS reproduces exactly:50/50 both layouts(main=5000/60);added NEW case neither issue nor PR covers=mid-doc viewport[2000,2050)@5K=50 all-in-range(main=5000,rows out of range). COST reproduces:1.80->1.05/9.88->5.08/57.55->28.80ms @1K/5K/20K=1.71x/1.94x/2.00x(PR claimed 1.91/2.04/2.13=runner diff,same shape). Sign-flip holds:1.05-1.07x SLOWER before,0.53-0.63x after. Many-excerpt unchanged 0.16x both. Suite on branch:2270pass/0fail/3skip/6todo,typecheck 0,lint 0(1 pre-existing info). **MY 0731 PROPOSED PATCH WAS WRONG**-start.row-only filter DROPS multi-line match spanning INTO viewport from above(rows0-2,vp[2,4):truth=1,naive=0);main wrong opposite direction(vp[4,6):truth=0,main=1);only row-range INTERSECTION(start.row<endRow && end.row>=startRow) correct both ways-verified empirically w/ 2nd harness naive.ts. #668 bounds the backward end-anchor search w/ _maxResultRowSpan(recomputed in _performSearch on every textChange,reset in find/clear/dispose). CAVEAT I RAISED in comment:PR trade-off note says stale value "cannot drop a visible result" but danger direction is too-SMALL(narrows window);only exposure=call between edit and recompute(when _results stale anyway);could not construct breaking case. Commented on #668(1 comment this run). Residual O(total matches) still bl item5(both sides agree=separate change). Since 0731:docs-only(#662 closed,#669 opened)+bot pushes,NO human comments=>no other T5. #626 had 0 comments its whole lifetime,no ticks. All 60 SA PRs + 5 tracked issues(#417/#496/#446/#540/#664) re-verified OPEN via 6-page paging(pulls?state=all&sort=created&asc&per_page=100,max PR seen=669). Main ce545ec,repo clean,pr668 branch deleted after.
(0730-0731: the resolveResultsInViewport thread. 0730 found it while surveying unbenched modules + measured PR#492 line-offsets 5.0-20.4x and commented there instead of opening a dup. 0731 measured the VP defect and filed issue#664. Both superseded by the 0801 entry above, which records the verified outcome. Durable lessons kept: rope.ts already optimal-stop re-checking it; _performSearch is wired to textChange so it reruns every keystroke; search.test.ts:375 used three 1-ROW excerpts which is why excerpt-vs-row granularity was never caught.)
0729:maint+T1 FULL TOOLCHAIN revalidation(1st since 0523,bun1.3.14): test 2265pass/0fail/3skip/6todo, typecheck 0, lint 0(1 pre-existing biome schema info), bench 118pass/0fail. No rot. Details in #626.
(0727-0728 maint-only dailies folded into #626 run-history: all4 PRs clean/unchanged, main ce545ec, SA-list fully verified across both, no human comments.)
(0726 maint-only daily folded into #626 run-history: all4 PRs clean/unchanged, main ce545ec, SA-list verified, no human comments.)
(0709-0723 daily entries identical maint-only pattern; folded into #626 run-history. Removed here to keep notes lean.)
WATCH:when bumping run-id in the monthly-issue footer, do targeted replace - global s/oldrun/newrun/ corrupts prior run-history entry links. Cleanest: rewrite whole body via update_issue replace, omit trailing footer (safe-output appends one).
NOTE:body had 2 dup 'Generated by' footers historically; submit body w/o footer, let safe-output append.
NOTE:push_repo_memory ALWAYS errors: reports ~33KB constant(.git/, 188K on disk)+actual content. 0801: reported 41KB at 7.8KB content, 38KB at 5.4KB content => tracks content but with huge fixed offset, so it can NEVER pass. Verified real content w/ `find . -type f -not -path './.git/*' | xargs cat | wc -c` = 5404B, under 10KB. Auto-push has handled the real .md files fine every prior run. Do NOT delete good notes chasing this; just verify real byte count and report the error honestly.
NOTE:single-PR state fastest via curl https://api.github.com/repos/.../pulls/N (unauth works for public read). issues/N has comments count field.
NOTE:closed-since check: curl .../pulls?state=all&sort=updated&direction=desc&per_page=15 + filter updated_at>=lastrun.
NOTE:perf-labeled issues: #540+#496+#446+#417+#664 + the current monthly (Aug; #626/#588/#530 now closed).
NOTE:main HEAD via curl commits/main (returns JSON sha) or branches/main.
NOTE:>100 open PRs so pulls?state=open page1 doesn't show #310/#339; query them directly.
NOTE:unauth API budget is only 60 req/hr (check /rate_limit first). Bulk-verifying 40+ SA items one-by-one blows it. Instead page pulls?state=all&sort=created&direction=asc&per_page=100 and build a number->state dict: as of 0801 SIX pages cover PRs #1-669 (496 PRs total), so whole SA-list costs 6 requests + ~5 for tracked issues.
