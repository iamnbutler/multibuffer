bun:works via curl install in runner
bl:VP-granularity(MEASURED 0731,filed as issue)/GA/IH(#377)/undo(#435)/VP-binsearch(follow-on); rope.slice done#565; PT(#540) done#575; search-lineoffsets=PR#492(measured 0730,not mine)
hold:reinstated(maint-only); lifted twice for #565,#575
0731:MEASURE run(T2+T6). Resolved 0730's unmeasured VP item. REAL DEFECT=GRANULARITY not allocation: resolveAnchorsInViewport(multibuffer.ts:346) rejects by EXCERPT row-span(info.startRow/endRow), so 1 excerpt spanning whole doc filters NOTHING. Ground truth @5K lines/50-row vp=50 visible; current returns 5000(single-excerpt) / 60(250x20-line excerpts). Cost single-excerpt=1.03-1.05x SLOWER than resolveResults()(1.72/9.51/55.78ms vs 1.80/9.79/58.43ms @1K/5K/20K) - docstring claims "more efficient", FALSE. Many-excerpt case works fine(0.13-0.19x). FIX=2-line row check on resolved startPoint before end-anchor resolve: returns exactly 50 both setups, 1.88x/1.95x/1.99x @1K/5K/20K. Why never caught: test search.test.ts:375 uses THREE 1-ROW excerpts=>excerpt-level==row-level there. CORRECTED 2 wrong 0730 claims: (a) NO non-test callers(only tests/editor/search.test.ts:394) so NOT per-frame today; (b) line356 end-resolve only runs for pre-filter survivors(=all of them in single-excerpt case). FILED AS ISSUE not PR(hold + behaviour change needs signoff); repo untouched, git clean, harness=throwaway /tmp script. Residual: still O(total matches)-> new bl item VP-binsearch(_results position-sorted). Since 0730: docs only(#658 closed unmerged,#662/#663 open)+#468 pushed by github-actions[bot](NOT human=>no T5). Main ce545ec. All4 PRs clean/mergeable/draft. #626 0 comments,no ticks. Updated #626:replace body,prepended 0731,dropped 0727,added VP issue as SA item#1(SA now 65),rewrote backlog item1 w/ measurements+corrections,added item5 VP-binsearch.
0730:ANALYSIS run(broke maint-only monotony,no code PR-hold respected). T2:surveyed unbenched modules. rope.ts already optimal(binsearch prefixes,indexOf scans,fwd-scan lines())-NO opportunity,stop re-checking it. FOUND search.ts hot path:_performSearch wired to text-change sub(search.ts:532-537)=>reruns EVERY keystroke;_computeLineOffsets(fullText) scans doc char-by-char. MEASURED main-perChar vs PR#492-prefixSum: 5.0x/17.1x/13.9x/20.4x @100/1K/10K/50K lines,byte-identical. main costs 0.92ms@10K,4.65ms@50K per keystroke vs <1ms target. **PR#492 ALREADY FIXES THIS**-did NOT open dup PR;posted bench table as comment on #492(had 0 comments in 3mo,no measurements,author=github-actions[bot],created 0422,clean). NEW backlog item:resolveResultsInViewport(search.ts:347) maps ALL _results->startAnchors every call despite being viewport-optimised path;per-frame;cacheable(_results only changes in _performSearch/clear/dispose);line356 resolves end anchors 1-by-1 unbatched. UNMEASURED-needs bench(#501 suite is natural home). Since 0729:only docs(#656 closed unmerged,#662 new)+#435/#537 touched by github-actions[bot](NOT human=>no Task5 re-engage). Main unchanged ce545ec. All4 PRs clean/mergeable/draft/0-comments. #626 0 comments. Updated #626:replace body,prepended 0730,dropped 0726,enriched #492 SA line w/ measurement,added #492 comment-check item(SA now 64),backlog renumbered+added unbenched-modules survey line.
0729:maint+T1 FULL TOOLCHAIN revalidation(1st since 0523,bun1.3.14): test 2265pass/0fail/3skip/6todo, typecheck 0, lint 0(1 pre-existing biome schema info), bench 118pass/0fail. No rot. Details in #626.
(0727-0728 maint-only dailies folded into #626 run-history: all4 PRs clean/unchanged, main ce545ec, SA-list fully verified across both, no human comments.)
(0726 maint-only daily folded into #626 run-history: all4 PRs clean/unchanged, main ce545ec, SA-list verified, no human comments.)
(0709-0723 daily entries identical maint-only pattern; folded into #626 run-history. Removed here to keep notes lean.)
0701:MONTH ROLLOVER->closed June#588,created July#626(create_issue,auto-prefix+perf label),carried 59-item SA-list fwd. Details folded into #626.
(0630-0705 daily entries all identical maint-only,unchanged; folded into #626 run-history. Removed here to keep notes lean.)
WATCH:when bumping run-id in #588 footer, do targeted replace - global s/oldrun/newrun/ corrupts prior run-history entry links. Cleanest: rewrite whole body via update_issue replace, omit trailing footer (safe-output appends one).
NOTE:body had 2 dup 'Generated by' footers historically; submit body w/o footer, let safe-output append.
NOTE:push_repo_memory reports phantom ~31-36KB regardless of content=counts .git/(172K); ignore, auto-push handles actual .md files (real total ~5KB, under 10KB limit).
NOTE:single-PR state fastest via curl https://api.github.com/repos/.../pulls/N (unauth works for public read). issues/N has comments count field.
NOTE:closed-since check: curl .../pulls?state=all&sort=updated&direction=desc&per_page=15 + filter updated_at>=lastrun.
NOTE:perf-labeled issues are #588+#540+#496+#446+#417 only.
NOTE:main HEAD via curl commits/main (returns JSON sha) or branches/main.
NOTE:>100 open PRs so pulls?state=open page1 doesn't show #310/#339; query them directly.
NOTE:unauth API budget is only 60 req/hr (check /rate_limit first). Bulk-verifying 40+ SA items one-by-one blows it. Instead page pulls?state=all&sort=created&direction=asc&per_page=100 and build a number->state dict: pages1-4 cover PRs #1-546, so whole SA-list costs 4 requests.
