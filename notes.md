0804:T2+T4+T6+T7. **NEW FIND: MultiBuffer.edit() re-summarises EVERY excerpt on the edited buffer, every keystroke.** _refreshExcerptsForBuffer(multibuffer.ts:1096, ONLY 1 call site, always gets editRow+lineDelta) rebuilds each excerpt via createExcerpt->computeExcerptSummary(excerpt.ts:42)=buffer.lines()+per-char UTF8 scan => keystroke=O(chars across all excerpts of that buffer). **LAYOUT-DEPENDENT, why it hid**: 1-buf-per-exc is FLAT(0.01-0.03ms @1-500); all-exc-on-ONE-buf is LINEAR(0.010/0.065/0.313/0.637/1.599/3.214ms @1/10/50/100/250/500). Only shape 2 matters(search-in-file, single-file diffs). FIX=when lineDelta===0 and editRow NOT in excerpt, reuse textSummary + struct-spread rebind (same dodge updateExcerptMetadata:1083 already uses). 2.03-2.45x isolated @10-500 BUT **REAL FILES only 1.16-1.78x** end-to-end. Correctness 0 mismatches/120 mixed edits x4 configs; 1507pass/0fail targeted. Reverted(hold), filed as issue. CAVEATS stated openly: constant-factor NOT complexity; 100exc real=0.24ms already INSIDE 1ms budget(no live regression); only lineDelta===0. Independent of PR#455.
0804-LESSON(2nd time): **first hypothesis WRONG, attribution caught it.** Assumed the O(n) MultiBuffer.snapshot() copy(5x/keystroke) drove the growth; it is only **1-2%** of keystroke time. Attribute, never infer from correlation. AGAIN synth(2.4x) vs real(1.16-1.78x) - same trap as 0802 highlighter(271x synth vs 2-4x real).
0804-REJECTED: **diff/patch.ts HEALTHY, do NOT re-survey.** 729 LoC zero-bench but LINEAR: us/patch-line FALLS 2.10->0.97 as patch grows 632->5072 lines. Real 8-file/485-hunk: parse 1.04 / buildBuffers 1.78 / excerpt-loop 1.57ms; snapshot() negligible; addExcerpt lazy(only rebuilds if listeners, none during construction). Still lacks benchmarks.
0804-T4: #310/#339/#565/#575 clean+draft+no conflicts. SA list re-verified by paging pulls?state=all asc per_page=100 x6(501 PRs,max #681): 61 distinct PRs + 7 issues ALL open, 0 closed. main ce545ec. No human comments anywhere(Repo Assist bot is only other actor)=>no T5.
**WATCH-CRITICAL: repo-memory limits are 10KB MAX FILE and 10KB MAX PATCH per push. The 0803 run's memory NEVER LANDED**(last memory commit=run 30759073794=0802); that run was recoverable ONLY from issue #670 run-history. => keep notes.md SMALL, append-and-trim, never rewrite; ALWAYS mirror findings into #670, the reliable store. Old 0801/0730-31 entries trimmed for space - they live in #670.
0802:BIG FIND run(T1+T2+T4+T6+T7,no PR-hold respected). **getLineTokens() re-descends from tree.rootNode PER VISIBLE ROW**; all 3 renderers call it per-row(dom.ts:579,canvas.ts:520,webgpu.ts:481). _collectTokens loops i<node.childCount and MATERIALISES every child via node.child(i)(WASM FFI+Node alloc) THEN early-returns=>O(fanout)/level per row, viewport=O(rows x fanout). REAL FILES(50-row vp,median of 9,5 warmup): canvas.ts 3.005->0.702ms(4.3x), dom.ts 4.111->1.024(4.0x), editor.ts 2.743->0.959(2.9x), multibuffer.ts 4.227->1.673(2.5x), rope.ts 3.684->1.814(2.0x). FLAT/GENERATED src is QUADRATIC: 358/757/1591/3364 us-per-row @500/1K/2K/4K lines(doubles per doubling) vs binsearch FLAT 11.0/12.5 => 70.5x@1K, 270.9x@4K(169ms per viewport=~10 dropped frames). FIX=binary-search child span(children ordered+non-overlapping): first idx w/ endPosition.row>=target, walk fwd while startPosition.row<=target. Confined to _collectTokens, NO API/renderer change. CORRECTNESS: token-identical(startColumn/endColumn/color) on EVERY row of every doc=0 mismatches in 6375 real rows+5000 synthetic. Also measured+REJECTED batch getLineTokensRange alt(19-43x flat-only): binsearch is faster AND smaller. Filed as ISSUE(no PR-hold). **LESSON: ALWAYS re-measure against REAL nested source** - synthetic flat fixture said 271x, real code says 2-4x; I nearly reported 271x as the headline.
0802-T6: repo's OWN highlighter bench is unrepresentative - generateLargeSource()=1000 FLAT top-level stmts=max root fanout => reports 0.759ms/row vs ~0.055ms/row real nested TS(~14x overstatement). It's the ONLY getLineTokens bench + measures ONE row so per-viewport multiplication never shows. Passes its <1ms target, which is exactly why this hid. Suggested 50-row vp bench over real fixture. Filed in same issue as secondary.
0802-T1: **bun test is FLAKY on clean main** - "Buffer Performance > line access is O(1)" fails 5/17 full-suite runs(~29%) but 0/20 ISOLATED(needs whole-suite GC pressure, can NOT repro alone). buffer.test.ts:444 expect(middle.avgMs).toBeLessThan(early.avgMs*3+0.001); captured Expected<0.00231 Received 0.00277 = 20% overshoot on 2-3 MICROsecond values. Neighbouring 'late' assert uses *10+0.01(10x more generous) and never fails. Filed as 2nd issue. typecheck 0, lint 0(1 pre-existing info), bench 118/118 pass.
0802-REJECTED: undo-stack Array.shift() past _MAX_HISTORY=100 cap(7 duplicated push+shift sites in editor.ts) measured 0.083us/keystroke=0.008% of 1ms budget. NOT worth ring buffer. Do NOT re-investigate.
0802-T4: 4 PRs all clean/draft/no conflicts. #565 got INDEPENDENT verification from Repo Assist bot(344x/54x/2.4x, recommends merge, flags conflict w/ #333 and says take #565). Bot comments only, NO human comments anywhere => no T5. Main still ce545ec. Monthly #670(Aug), SA-list now 69(was 67,+2 new issues).
WATCH: `bun test -t "name with (parens)"` - the parens are REGEX, matched 0 tests and exit!=0 => looked like 15/15 fail. Escape or pick a paren-free substring.
bun:works via curl install in runner
bl:VP-granularity RESOLVED-by-PR#668(verified 0801)/GA/IH(#377)/undo(#435)/VP-binsearch(follow-on,item5); rope.slice done#565; PT(#540) done#575; search-lineoffsets=PR#492(measured 0730,not mine)
hold:reinstated(maint-only); lifted twice for #565,#575
(0709-0723 daily entries identical maint-only pattern; folded into #626 run-history. Removed here to keep notes lean.)
WATCH:when bumping run-id in the monthly-issue footer, do targeted replace - global s/oldrun/newrun/ corrupts prior run-history entry links. Cleanest: rewrite whole body via update_issue replace, omit trailing footer (safe-output appends one).
NOTE:body had 2 dup 'Generated by' footers historically; submit body w/o footer, let safe-output append.
NOTE:push_repo_memory ALWAYS errors: reports ~33KB constant(.git/, 188K on disk)+actual content. 0801: reported 41KB at 7.8KB content, 38KB at 5.4KB content => tracks content but with huge fixed offset, so it can NEVER pass. Verified real content w/ `find . -type f -not -path './.git/*' | xargs cat | wc -c` = 5404B, under 10KB. Auto-push has handled the real .md files fine every prior run. Do NOT delete good notes chasing this; just verify real byte count and report the error honestly.
NOTE:single-PR state fastest via curl https://api.github.com/repos/.../pulls/N (unauth works for public read). issues/N has comments count field.
NOTE:closed-since check: curl .../pulls?state=all&sort=updated&direction=desc&per_page=15 + filter updated_at>=lastrun.
NOTE:perf-labeled issues: #540+#496+#446+#417+#664 + the current monthly (Aug; #626/#588/#530 now closed).
NOTE:main HEAD via curl commits/main (returns JSON sha) or branches/main.
NOTE:>100 open PRs so pulls?state=open page1 doesn't show #310/#339; query them directly.
NOTE:unauth API budget is only 60 req/hr (check /rate_limit first). Bulk-verifying 40+ SA items one-by-one blows it. Instead page pulls?state=all&sort=created&direction=asc&per_page=100 and build a number->state dict: as of 0801 SIX pages cover PRs #1-669 (496 PRs total), so whole SA-list costs 6 requests + ~5 for tracked issues.
