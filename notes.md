**WATCH-CRITICAL: repo-memory limits are 10KB MAX FILE and 10KB MAX PATCH per push. The 0803 run's memory NEVER LANDED**(last memory commit=run 30759073794=0802); that run was recoverable ONLY from issue #670 run-history. => keep notes.md SMALL, append-and-trim, never rewrite; ALWAYS mirror findings into #670, the reliable store. Old 0801/0730-31 entries trimmed for space - they live in #670.
0802-T6+0808-T6 SAME PATTERN TWICE: **a bench that PASSES its target is exactly how O(n) hides.** highlighter bench=max-fanout fixture(~14x overstatement); 'Insert character - 10K buf' sat at 0.550ms vs a <1.5ms target while hiding a full-doc scan. When a target has >=3x slack it cannot tell O(1) from O(n).
0802-T1: **bun test is FLAKY on clean main** - "Buffer Performance > line access is O(1)" fails 5/17 full-suite runs(~29%) but 0/20 ISOLATED(needs whole-suite GC pressure, can NOT repro alone). buffer.test.ts:444 expect(middle.avgMs).toBeLessThan(early.avgMs*3+0.001); captured Expected<0.00231 Received 0.00277 = 20% overshoot on 2-3 MICROsecond values. Neighbouring 'late' assert uses *10+0.01(10x more generous) and never fails. Filed as 2nd issue. typecheck 0, lint 0(1 pre-existing info), bench 118/118 pass.
WATCH: `bun test -t "name with (parens)"` - the parens are REGEX, matched 0 tests and exit!=0 => looked like 15/15 fail. Escape or pick a paren-free substring.
bun:works via curl install in runner
bl:VP-granularity RESOLVED-by-PR#668(verified 0801)/GA/IH(#377)/undo(#435)/VP-binsearch(follow-on,item5); rope.slice done#565; PT(#540) done#575; search-lineoffsets=PR#492(measured 0730,not mine)
hold:reinstated(maint-only); lifted twice for #565,#575
(0709-0723 daily entries identical maint-only pattern; folded into #626 run-history. Removed here to keep notes lean.)
WATCH:when bumping run-id in the monthly-issue footer, do targeted replace - global s/oldrun/newrun/ corrupts prior run-history entry links. Cleanest: rewrite whole body via update_issue replace, omit trailing footer (safe-output appends one).
NOTE:body had 2 dup 'Generated by' footers historically; submit body w/o footer, let safe-output append.
NOTE:push_repo_memory ALWAYS errors: reports ~33KB constant(.git/, 188K on disk)+actual content. 0801: reported 41KB at 7.8KB content, 38KB at 5.4KB content => tracks content but with huge fixed offset, so it can NEVER pass. Verified real content w/ `find . -type f -not -path './.git/*' | xargs cat | wc -c` = 5404B, under 10KB. Auto-push has handled the real .md files fine every prior run. Do NOT delete good notes chasing this; just verify real byte count and report the error honestly.
NOTE:single-PR state fastest via curl https://api.github.com/repos/.../pulls/N (unauth works for public read). issues/N has comments count field.
NOTE:closed-since check: curl .../pulls?state=all&sort=updated&direction=desc&per_page=15 + filter updated_at>=lastrun.
NOTE:perf-labeled issues: #540+#496+#446+#417+#664 + the current monthly (Aug; #626/#588/#530 now closed).
NOTE:main HEAD via curl commits/main (returns JSON sha) or branches/main.
NOTE:>100 open PRs so pulls?state=open page1 doesn't show #310/#339; query them directly.
NOTE:unauth API budget is only 60 req/hr (check /rate_limit first). Bulk-verifying 40+ SA items one-by-one blows it. Instead page pulls?state=all&sort=created&direction=asc&per_page=100 and build a number->state dict: as of 0801 SIX pages cover PRs #1-669 (496 PRs total), so whole SA-list costs 6 requests + ~5 for tracked issues.
0805-T1: flake re-sampled 4/15 => **cumulative 10/36 (28%)**, matches RepoAssist 7/28. NOTE first 3-run sample read 2/3 and I nearly reported "worse than before" - WIDEN THE SAMPLE before claiming a rate moved.
0805-NOTE: #684/#685 show mergeable_state=unstable but have **ZERO recorded check-runs** (total_count=0), i.e. CI never ran - do NOT attribute that to the buffer.test flake.
0805-MEMORY-PUSH: #677 explains the 0803 loss - the cap is on the **git PATCH** (a modified line counts twice), not content. Fix is a max-patch-size frontmatter bump RepoAssist cannot write. MITIGATION USED THIS RUN: compress 2 resolved entries + append, never rewrite whole file. Keep doing this.
0808:T1+T2+T3verify+T4+T5+T6+T7. **BIGGEST WIN YET - and I VERIFIED an EXISTING PR instead of filing a dup.** computeTextSummary(buffer.ts:21) calls rope.byteLength()=per-CHAR scan of WHOLE doc on EVERY insert/delete/replace => keystroke is O(doc). Open PR#510(since 0427) already fixes it. Applied+reverted on clean main: 1.071->0.021ms@16K(50.1x), 0.535->0.023@8K; LINEAR before, FLAT ~0.02ms after. **main MISSES <1ms target @16K.** Repo's own bench 0.550->0.0099(55.7x). Commented #510, promoted to #1 in SA. Full detail in #670.
0808-LESSON(4th time in a week): **attribution overturned the hypothesis AGAIN.** Expected multi-cursor QUADRATIC; it is LINEAR(~150us/cursor flat 25->400) - no defect, filed nothing. First 2 suspects(snapshot(), per-excerpt summary) measured 0.2us FLAT. Found real cause ONLY by attributing down to buf.insert(). NEVER infer from code shape.
0808-RULES-LEARNED: (a) ALWAYS grep SA-list+open PRs for the file/function BEFORE filing - I nearly filed a dup of #510. (b) Re-measuring someone else's PR on the RIGHT axis is high-value: #510's own bench measured cold/warm bytes access, NOT keystroke latency, which is why its worth stayed invisible 3 months. (c) An 'amplifier' finding (multi-cursor x N) can PROMOTE an existing PR(#508) without filing anything.
0807-RUN(recovered from #670; memory push failed again): surveyed src/renderer, REJECTED dom.ts:533. Found PR#693==#492 dup(identical test blob 14246eac). Linked #688.
NOTE-0808 PROOF push_repo_memory counts .git not content: content SHRANK 11318->10161B yet the tool went 46KB->52KB, because a 'git add -A' I ran in between wrote new .git objects. It can never pass; verify real size with find+wc instead, keep the git PATCH under ~10KB, and mirror everything into #670.
0809:T2+T4+T6+T7. **REJECTED: dedentLines NOT slow.** bench dedent0.450 vs indent0.088(5.1x) is 100% FIXTURE: makeIndentedEditor(1000,220) indents ALL lines=232KB vs makeEditor=32KB. 2x2(op x size): indent-on-BIG 0.619 == dedent-on-BIG 0.621; dedent-on-SMALL 0.095 == indent-on-SMALL 0.142. Cost follows DOC not OP. 5th attribution overturn. Filed fixture issue(LINK NEXT RUN).
0809-PAYOFF: the rejection EXPOSED the real signal - 1-line indent/dedent scales with WHOLE-DOC BYTES(0.142@32KB->0.619@232KB) = the #510 byteLength scan; +#510 => FLAT ~0.03ms. **#510 NOT insert-only: repo-bench avg main->510 dedent22.5x insertText10K22.2x dupLine9.4x insertLineBelow7.5x indent6.3x moveLine4.0x = WHOLE editor command surface.**
0809-RESTRAINT: did NOT re-comment #510 despite new evidence(already 3 measurement comments, no human reply => 4th bot comment = spam). Evidence -> #670 instead. 0809-CLEARED: glob.ts(cached matcher, +#575 covers it) & wrap-map.ts(prefix-sum+ASCII) already optimized, do not resurvey.
0809-NEW: harness.ts:78 gates avgMs ONLY, p95 NEVER enforced - 'diff 1K full rewrite' avg33.9 passes while p95 51.2 EXCEEDS <50ms target. #670 body now 47KB vs GitHub 65KB cap(+2KB/run) => compressed 0801-0804 entries there.
SIZE-WATCH(0809): notes.md ~8.9KB of 10240B. **PATCH cap 10240B is the binding limit: deletions cost patch bytes too. A trim+append that felt safe measured 12.9KB and would have FAILED. Delete ONE big stale line max per run, keep appends <1KB, never rewrite memory.md wholesale.**
0810:T1+T2+T4+T6+T7, see #670. **harness.ts:78 gates avgMs ONLY(p95 never gated); 116 targets median 20x slack, only 4%<2x.** Insert LINEAR ~102us/1Kln, 1.68ms@16K vs <1ms, both benches still PASS; ratio gate <1.4/doubling fails all 4. Filed issue(LINK NEXT RUN). 0809 diff-p95 breach did NOT repro=noise. CLEARED: input-handler.ts, MultiBufferSnapshotImpl.
**0810-PATCH LESSON: git-diff CONTEXT dominates the 10KB patch cap - lines here are 300-600B so each scattered edit/delete costs ~2KB/hunk. Attempts hit 14.3/12.4KB(would FAIL like 0803/0807). SAFE SHAPE = zero deletions + ONE append at EOF, file<10240. `git diff | wc -c` first.**
