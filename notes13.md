SIZE-WATCH: **THIS FILE (notes13.md) IS THE APPEND TARGET.** memory.md + notes.md + notes2-12 are all at/near the 10240 PER-FILE cap. `wc -c` BEFORE appending; keep appends <1KB or start notes14.md.

0903:T1+T2+T3+T4+T7 run:33798536844. main STILL ce545ec. 0 closed PRs since 08-25, `commenter:iamnbutler updated:>2026-08-01`=0 => still 0 human comments EVER, ALL SA carried. bun1.4.0 test 2265/0 2.76s NO flake (cum 12/75=16.0%) typecheck0 lint1info bench118/118. **#796 = the 0902 glyph-atlas issue, NOW LINKED.** New intraline issue number UNKNOWN - **LINK IT NEXT RUN** (its SA item is the top one, bare search link).
**0903-FINDING (a guardrail that cannot fire): `computeIntralineDiff` docs say "aborts if computation exceeds timeBudgetMs" (diff.ts:305-307) but the check at :389 runs BEFORE myersCharDiff at :391 and myersCharDiff has NO time check inside.** Only work between startTime(:344) and the check = prefix/suffix scan. Intraline default ON in BOTH controller.ts:78 and multibuffer.ts:92.
0903-THE DECISIVE EXPERIMENT (reuse this shape for any "is this guard real?" question): **sweep the option over 12 orders of magnitude.** On a pair costing 0.71ms: budget 0/1e-6/1e-5/5e-5/1e-4 => 0.0005ms + coarse 1d/1i; budget 0.001/0.01/0.5/**2(default)**/100/**1e9** => 0.70-0.73ms + fine 43d/46i. **Cliff at the PREFIX-SCAN cost (1e-4..1e-3), not at the WORK cost.** 1 microsecond == 11 days. Also default/1e9 ratio = 1.018x.
0903-NUMBERS (reuse): per-pair ms by differing-middle len, FLAT across 10/100/400 pairs: 20ch 0.015, 60ch 0.084, 120ch 0.295, 200ch 0.77. **NON-MONOTONE: mid=200 1.29ms, mid=201 0.0002ms (CHAR_DIFF_THRESHOLD=200 cliff, ~6000x) - the BIGGER change is cheaper.** e2e createDiffController 1000ln/400 pairs mid200: on 316 / off 7.3 = 43.5x (fwd) 42.1x (rev). **LIVE reDiff (150ms debounce!) 315.4/314.1ms vs 7.5 = 42.1x/36.6x.**
0903-HONEST BOUND (led with it): real renames in THIS repo = 1.09x(editor.ts) 1.35x(canvas.ts) 1.80x(bun.lock) - a rename leaves a tiny middle. Needs a SUBSTANTIALLY REWRITTEN MODERATELY-LONG line. Realistic bad cases 3/3 rounds <2% spread: lockfile 100 regenerated sha512(88ch b64) 22.8 vs 0.50 = **45x**; markdown 100 reworded ~130ch paragraphs 28.0 vs 0.46 = **61x**; at 400 lines 90.7/114.2ms (ratio falls to 16-20x because the line-level baseline rises to 5.7ms). Repo line profile: 62.5% <=40ch, 37.1% 41-120, 0.24% 121-200, 0.13% 201-1000, longest 565 (docs/glossary.md).
0903-ATTRIBUTION KILLED THE OBVIOUS FIX (10th): per-char CharEdit objects + 2x .filter() = **2.7%**; forward search = **97.3/97.4%** (both orders). => NO output-preserving win exists; every real lever (diff-wide budget / lower threshold / cap D) changes output => filed as a DECISION not a PR. Same class as #792's D-cap.
0903-PROTOTYPE (~20 lines, 3 files, measured before filing): shared `deadline` on IntralineDiffOptions, set once per diff = now + timeBudgetMs(16 default). reDiff 315->25.5ms; 400-line cases 92/114 -> 21.6/21.1 (= budget 16 + line-diff 5.8, exactly the right shape). Overshoot bounded by ONE pair => checking once per call is CORRECT once the deadline is shared, so the placement bug stops mattering. Fidelity: 100-hash case keeps char-level ranges on 72 rows, rows 0..135 of 0..199 => **degrades the OFF-SCREEN BOTTOM**. 2265/0 typecheck0 lint0.
**0903-DUPLICATION FOUND BY MEASUREMENT NOT READING (best method note): my first prototype patched ONLY controller.ts - reDiff moved 12x, construction moved 0%.** Because `src/diff/multibuffer.ts:202-209` is a near-verbatim COPY of `controller.ts:225-232`; construction goes through createUnifiedDiffMultiBuffer. Already drifted: controller batches setExcerpts ("avoids N+1 rebuilds"), multibuffer.ts still addExcerpt-per-entry (ties to #688). **When a prototype moves one path and not another, suspect a second copy.**
0903-8th COVERAGE AXIS (flag->usage->data->input->output->**UNTESTED DOCUMENTED OPTION**): timeBudgetMs has ZERO tests; 2265 pass whether the guard works or not, AND pass again after a prototype that changes cost 12x and visible output. Added to #739 as case 8.
0903-FIXTURE (/tmp/gh-aw/agent/intraline, NOT committed, PR hold intact): main-arm.ts = sed'd standalone copy of diff.ts (types-only import, same trick as 0901). e2e/live/real arms import repo src by ABSOLUTE path - **NO template literals in import specifiers, bun rejects them; use plain strings.** **Buffer has insert/delete/replace(offset), there is NO .edit().** Column(intraline) decorations are distinguished by range.start.column!==undefined. Arm select for the prototype = patch src, measure, `git checkout -q HEAD -- src/`.
0903-T4: #565 mergeable_state=unstable, #575=clean, both open+draft, #310/#339 unchanged - parked CI gate, NOT my changes => NO pushes (3rd run running). 0903-T5: SKIPPED this run (round-robin; T5 has been a deliberate no-op 8 runs running - 0 human comments repo-wide, every candidate carries mine).
