SIZE-WATCH: memory.md 10206/10240, notes.md 9976/10240, notes2.md 8468/10240 are all at/near cap. APPEND HERE. New file = cheapest patch shape (no context lines, no deletions). Start notes4.md when this nears ~9KB. Do NOT trim the older three.

0819:T3+T4+T5+T7 run:32282599784. main STILL ce545ec, 92 SA boxes ALL unticked, still 0 human comments EVER (iamnbutler has PR #378 open but has never commented on any Perf Improver artefact). tests 2267/0 (2265 base + my 2 new) NO flake (cum 11/46=23.9%), typecheck0, lint1info, bench 121/121 on #565 branch.

**0819-FIRST TIME I PUSHED A FIX TO MY OWN OPEN PR (Task 4, not a new PR — hold intact).** RepoAssist commented on #565 (0819 13:28) flagging a latent correctness bug I introduced: `clampedStart` had Math.max(0,..) but `clampedEnd = Math.min(end,_length)` had NO lower clamp, so negative `end` reached String.prototype.slice and was reinterpreted as offset-from-end. Verified independently, fixed (one line + 2 tests), pushed via push_to_pull_request_branch. **Branch: perf-assist/rope-slice-binsearch-e022bb39f4a33523; safe-output push worked (patch 2829B/72 lines).**

0819-METHOD (reuse for any "is this behaviour-preserving" claim): differential A/B of the two implementations in ONE process. 6 corpora x 14 offsets INCLUDING NEGATIVES = 1176 triples -> 38 mismatches ALL in one class `start<end<0`; separate dense NON-NEGATIVE sweep 3150 slices -> 0. **Classifying mismatches into named classes (not just counting) is what made it one sentence instead of a list.** rope.ts has ZERO imports so `git show main:src/buffer/rope.ts > main.ts` + pr.ts + fixed.ts load side-by-side, no checkout needed (same trick as 0816).

**0819-SCOPE DISCIPLINE PAID: my sed hit 3 `clampedEnd` sites, not 1.** Checked each before widening: `delete` guards via `if(clampedStart>=clampedEnd) return this`, `replace` via `clampedEnd>0?clampedEnd-1:0` — BOTH already safe AND both unchanged from main by this PR's diff. Left them alone. **`git diff main...pr/N -- file` to confirm what the PR actually touches before "fixing" adjacent code.**

0819-MUTATION CHECK ON MY OWN TESTS (do this every time I add a regression test): revert the fix, confirm the new tests FAIL. Both did; the 5000-char case returned ~4990 chars of wrong text — that output IS the evidence of "fails open", much stronger than asserting it. `cp` the fixed file aside first, sed the line, run, restore.

**0819-BLOCK TIMING IS NOW MY DEFAULT BELOW ~100ns.** Adding Math.max to a hot path needed a cost check, but slice-1-char is ~36ns = BELOW the 40.7ns harness floor I measured 0818 — the repo harness literally cannot see it. Own harness: ONE timer pair per 20000 iters, accumulate `.length` into a sink, 9 interleaved rounds, round 0 discarded, 3 separate processes. main/fixed 48.2/51.7/48.0x, control (offset 0) 0.94/0.95/1.01x. **Live confirmation of #703: PR#565's own bench reports 0.06us for a 36ns op.** Cited that in the comment as a concrete instance.

0819-HONESTY CALL: fixed-vs-pr came out 1.01-1.18x FAVOURING the version with an EXTRA instruction. Mechanically impossible => reported as noise, explicitly NOT as a speedup. Resisted the free-looking win.

0819-CORRECTED RepoAssist (minor, stated in one line not a paragraph): it said the buggy behaviour "matches neither main nor JS slice". Partly wrong — where `start <= -length` the PR DOES match JS, because clampedStart floors to 0 exactly as JS does. Diverges from JS only for `-length < start < end < 0`. Conclusion unchanged, so I gave it one sentence and moved on.

0819-#670: 08-18 entry WAS present (0818's write landed; the 0817 loss did not repeat). Body 59256B -> 58967B: condensed 08-17+08-16 run entries into the condensed block and renamed its header to "08-01 → 08-17", which BOUGHT headroom while adding a full run entry. **WATCH: the condensed block uses `- 📊 **08-17** — ` single-line bullets with NO blank line after the `###` header. My first replace matched "header\n\n" and silently did nothing — I nearly deleted 2 entries without inserting their summaries. ALWAYS assert on every replace, and re-grep the headings after.** Now 6569B headroom (~3 runs).

0819-PR ORPHAN RECHECK (durable rule held): #310 NO merge-base = orphan, #339 orphan, #565 + #575 based on ce545ec. Did NOT rebase the orphans — one history rewrite (#705), maintainer call, already tracked; churning them adds nothing while 92 items sit unticked.
