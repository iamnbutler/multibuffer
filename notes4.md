SIZE-WATCH: memory.md 10206, notes.md 9976, notes2.md 8468, notes3.md 8588 — all at/near the 10240 cap. APPEND HERE. Start notes5.md when this nears ~9KB. Do NOT trim the older four: deletions cost patch bytes and the 10KB PATCH cap is the binding limit.

0821:T3+T4+T5+T7 run:32509478986. main STILL ce545ec, 93 SA boxes ALL unticked, still 0 human comments EVER. tests 2265/0 NO flake (cum 11/48=22.9%), typecheck0, lint1info, bench 118/118. bun 1.4.0.

**0821-BEST GROUND-TRUTH METHOD SINCE 0818's INJECTED REGRESSION, AND IT GENERALISES: to test a DETECTOR, feed it two runs of IDENTICAL code.** Every regression it reports is definitionally false — no attribution risk, no fixture risk. Applied to PR#506 (`--compare`): 4 runs of `bun benchmarks/index.ts --json` on ce545ec, wrapped as history.jsonl entries w/ distinct fake shas, **all 6 pairwise comparisons exit 1**, 23–34 false regressions + 20–39 false improvements out of 118. The PR's ±5% threshold sits AT the median of the noise.

0821-NOISE CONSTANTS (REUSE THESE — they bound every before/after claim I can make in this repo): identical code, same machine/runtime, pooled over 6 pairs: **p50 4.7%, p90 23.3%, max 84.5%; 94/118 (80%) move >=5%, 35 (30%) move >=20%.** Threshold needed for ZERO false positives = **133%**. medianMs quieter mid (mean FP 28.9->21.2) but tail 309%; p95Ms worse than both. **=> never quote a <1.3x before/after without interleaved repeats + a direction-consistency count.**

**0821-CONTROL THAT MADE IT STICK (pattern: find the same effect in the repo's OWN data).** bench-data has **36 consecutive same-composition pairs pushed within 120s** on GitHub's runners in March under a different bun: **6–66 benches over 5% each**, pooled p50 5.8% p90 27.4%. Different machine, month, runtime — agrees with mine within **1.2x**. Two independent sources agreeing turns "my runner is noisy" into "the harness is noisy".

0821-EXPECTED-FIX REJECTED BY ITS OWN DATA (say so explicitly, it is the strongest part of the comment): I assumed noise was the ~30% floor benches (0818). **It is not** — p50 by magnitude bucket 7.0/15.6/9.9/15.3/9.4% for <0.1us / 0.1-1 / 1-10 / 10-100 / >100us, and >100us benches still have p90 38%; only 39% of false regressions are sub-microsecond. Per-bench calibrated thresholds from history: **Spearman rho only 0.43** (lower bound — CI pairs include real code change). Helpful, not sufficient.

0821-TOOL: to run a script that reads `git show origin/bench-data:history.jsonl` against SYNTHETIC data with **zero patching of the script**, commit your file in a scratch repo then `git update-ref refs/remotes/origin/bench-data <commit>`. Runs the PR verbatim.
0821-WATCH: `git fetch origin bench-data` leaves only FETCH_HEAD — `git show origin/bench-data:...` then silently yields **0 bytes**. Use the full refspec `+refs/heads/bench-data:refs/remotes/origin/bench-data`.
0821-CORRECTION to my 0820 note: all **92 bench-data entries have DISTINCT shas** ("frozen at ce545ec" meant only the LATEST is). So the `entries.find(startsWith)` duplicate-sha concern I nearly filed does not apply — checked before writing.

**0821-SHELL TRAP (cost me a mangled memory write): backticks inside a double-quoted `python3 -c "..."` are COMMAND SUBSTITUTION.** `\`bench.yml\`` became empty and bash printed "bench.yml: command not found" — the text landed silently corrupted. **Write the script to a .py file whenever the content has backticks/markdown.** Assert on the composed string too (`assert 'bench.yml' in summ`).

**0821-LOOP BUG, SAME CLASS AS 0820's PATH BUG: an unfetched ref made me measure main and label it pr/575.** `git checkout pr/575 -- ...` failed ("invalid reference"), the `||` fallback ran, and the loop happily printed 2265 pass under the pr/575 heading. Caught it because 575 is a perf PR and 2265 was byte-identical to main's line. **Assert the ref resolves BEFORE the loop; a fallback that silently substitutes the baseline is worse than a crash.**

0821-STALE REMINDER (2nd time, cf 0820 #725): my note said fix a "6.7x-19.9%x" typo in #670 — grep found NO match, a prior run already fixed it. **Re-grep before acting on any "fix it next run" reminder.**
0821-T7: #670 08-20 entry WAS present (no silent loss). Body 62356->62313B after adding the 08-21 entry, condensing BOTH 08-19 and 08-20 into the condensed block, and stripping the stale trailing "Generated by" footer (safe-output appends its own). Headroom ~3.2KB (~2 runs).
0821-T5: 1 comment, on **#506** (it had ZERO comments). Restraint held elsewhere: posted nothing on #703/#731 despite this being the same root cause — the finding lives in #506 where it is actionable, and both issues already carry my comments.

0822:T1+T2+T6+T7 run:32588091269. main STILL ce545ec, still 0 human comments EVER. tests 2265/0 NO flake (cum 11/49=22.9%->22.4%), typecheck0, lint1info, bench 118/118, bun 1.4.0.

**0822-CATASTROPHE FOUND: THE 0821 update_issue DESTROYED #670's BODY.** Fetched body = 579 BYTES reading `[Content too large, saved to file: b70acb14...json]` + footer. 62,313B of run history + 93-item SA list GONE. updated_at 2026-08-21T18:01:40Z, run 32509478986 = mine. **=> the safe-output update_issue has a size ceiling I crossed; my "condense to buy ~2KB headroom each run" strategy was steering INTO it for weeks.** NEW DURABLE RULE: **keep #670 body ~15-20KB, PRIORITISED not exhaustive. Never rebuild the 93-item list.** Rebuilt lean this run (~14KB) from the API (search `is:open "Perf Improver" in:title` = 16 issues + 4 PRs, ONE request) + these notes. GitHub EDIT HISTORY retains the original - said so in the body so the maintainer can recover it.

**0822-2nd SILENT LOSS: my 0817 comment on #685 NEVER LANDED either.** issues/685/comments returns exactly 1, RepoAssist 08-19. notes2.md line 17 records "Commented ONCE". So the 0817 T7 loss AND the 0817 #685 comment loss are the same run => **suspect whole-run safe-output loss, not per-artifact.** Did NOT claim this publicly (noise); just reposted the evidence.

**0822-BIGGEST FINDING (T6): FIRST-EVER benchmark REACHABILITY MAP. 31/64 src files = 46% of loc never imported by ANY bench; excl. barrels 22 files/8940 loc.** canvas.ts 1310 + webgpu.ts 1298 = 2608 loc of renderer backend, ZERO coverage. project/ worker/ react/ = 0%. renderer 49%, diff 49%, editor 68%, buffer 100%, multibuffer 99%. **Stated as a LOWER BOUND** (dom.ts counts "reached" via computeSelectionRects but its render loop is unmeasured - same shape as #725). **VALIDATION THAT SOLD IT: the map independently re-derives 3 hand-filed gaps (#377 IH, #496 search, #540 ProjectTree) without being told.** Script /tmp covmap.ts ~70 lines, regex `(?:from|import)\s*["']([^"']+)["']` + resolve ./x -> x.ts|x/index.ts, multibuffer/y -> src/y/index.ts. Filed NEW ISSUE (number unknown at write time - **LINK IT IN #670 NEXT RUN**, SA entry has a bare search link).

**0822-PROOF CASE (T2), the thing that made the coverage issue land: InjectionHighlighter.getLineTokens is O(doc) PER ROW and NOTHING measures it.** injection-highlighter.ts is in the never-imported set BUT is a public export (renderer/index.ts). PR#685 changes it (+19-16, symmetric fix to BOTH _collectTokens and _collectTokensWithInjectionSkip) yet #685's own new benches import ONLY `Highlighter` => half its diff shipped ungraded. Measured (0817 side-by-side trick, both variants one process, interleaved, round0 discarded): **row-20 on #377's OWN fixture 171.7/363.0/804.9/1710.2us at body 500/1K/2K/4K = 2.11x,2.22x,2.13x per doubling, PERFECTLY LINEAR, while returning the IDENTICAL 15 TOKENS at every size** (built-in control - growth buys nothing); PR flat 14.6->16.1us = 11.7x/24.1x/51.5x/**106.5x**. 50-row viewport 0.784/1.052/1.964/3.744ms main vs 0.620/0.350/0.457/0.431 PR = 1.26/3.01/4.30/**8.69x**, 6/6 rounds at n>=1000, 2nd process 1.37/3.18/6.64/8.89x. Correctness 1006 rows incl 6 out-of-range, 3246 tokens, **0 mismatches**. Mechanism: main loops i=0..childCount and pays node.child(i) WASM crossings even though the recursion early-returns; PR binary-searches the start AND breaks.

**0822-SHARPEST RESULT: #377's benches PASS ON MAIN. 6 passed/0 failed in BOTH arms (main src vs +#685 src), same worktree, PR bench file verbatim.** Its 3 getLineTokens benches sample rows **3, 20, 5**; rows 3 and 5 land INSIDE injected sub-trees (few children) so the defect is STRUCTURALLY invisible - they moved 1.2x/1.4x SLOWER = noise. Only "markdown body row" sees it (0.194->0.014ms = 13.9x) and still keeps 5.2x slack. **At body=500 slack is 5.8x; ONE doubling to 2K -> 1.2x; two -> FAILS.** Recommended: point that bench at the already-built markdownFrontmatter2k, pin the row BY CONTENT at the midpoint, add one 50-row viewport bench. **This is the 0802 heuristic ">=3x slack hides O(n)" firing on a bench that did not exist yet.**

0822-#377 IS A PR (pulls/377=200), state open, **draft=false**, mergeable_state clean, merge-base == main HEAD (checked per durable rule, NOT an orphan), 3 files +162-3, adds benchmarks/injection-highlighter.bench.ts. Open PI PRs are exactly #310(orphan) #339(orphan,recommended CLOSE) #565 #575.

0822-TOOL: to run a PR's bench file through the REPO harness without dirtying main - `git worktree add -q --detach $WT main`, `ln -s <repo>/node_modules $WT/node_modules`, `git show pr/N:benchmarks/x.bench.ts > $WT/benchmarks/`, then a 3-line driver calling runBenchmarks([suite]) (index.ts has NO name filter). Swap arms with `git checkout pr/685 -- src/...` INSIDE the worktree. **`git worktree remove -f` + `git worktree prune` + assert `git status --porcelain` empty at the end.**
0822-T5 not scheduled but Task2 step6/Task6 step5 authorise it: 2 comments (#685 zero PI comments; #377 one). Posted NOTHING on #672/#703/#713 - same root cause but already carry my comments.
