SIZE-WATCH: **THIS FILE (notes8.md) IS THE APPEND TARGET.** memory.md + notes.md + notes2-7 are ALL at/near the 10240 PER-FILE cap (notes7=8159). Do NOT trim the older eight — deletions cost patch bytes and the 10KB PATCH cap is the binding limit. 0828: appending to an 8.1KB file costs ~8-9KB of patch, so I started a NEW file instead (new small file = patch ~= file size). `wc -c` BEFORE appending; keep appends <1KB.

0828:T3+T4+T5+T7 run:33132976466. main STILL ce545ec, still 0 human comments EVER (verified repo-wide: `commenter:iamnbutler updated:>2026-07-01` = **0 results**, strongest check yet — previously I only checked individual issues). ALL SA boxes unticked, no PR merged/closed since 0804. bun1.4.0 typecheck0 lint1info tests 2265/0 NO flake (cum 12/69=17.4%), bun test 2.79s.

**0828-T3 = FIRST EVER survey+measurement of src/diff/multi-file.ts (backlog item 6).** Two costs:
(1) createMultiFileDiff ctor runs a FULL Myers per file, UNCONDITIONALLY (the `for (const entry of options.files)` stats loop at :294 precedes ANY lazyRender check). No-DOM numbers (ctor == stats pass only), median-of-7 interleaved: 200lines x 10/50/200/500 files = 0.81/3.00/7.22/17.26ms; 1000lines x 10/50/200 = 5.13/20.22/**80.92**ms. diff() share = **98.9%**.
(2) The DiffResult is DISCARDED (computeFileDiffStats keeps 2 ints, drops .hunks) => expanding re-runs the identical diff inside createDiffController. diff() vs createDiffController, median-of-15 interleaved: 200ln 0.116/0.437=26.5%; 1000ln 0.789/1.698=46.5%; 4000ln 6.133/8.486=**72.3%**. **Share GROWS with file size.** Clean win: cache it, inputs are immutable strings so results equal by construction.
0828-CONTROL THAT MADE IT STICK: identical content (diff()'s oldText===newText fast path) => 500 files 0.55ms vs 17.26ms = **31x**. Proves Myers, not per-file scaffolding/Map/object cost.

**0828-10th FIXTURE-BIAS CATCH (same trap as 0811): under plain bun `document` is UNDEFINED so createMultiFileDiff SKIPS mountFiles()+initializeFiles() ENTIRELY => my lazyRender true-vs-false arms were byte-identical because NEITHER MOUNTED. Measured nothing.** Re-ran under happy-dom (declared dep, `new Window()` + assign globalThis.document/window/HTMLElement): lazy=true **1/50** initialized, lazy=false **50/50**; ctor 73.4 vs 468.2ms @50x500. **INSTRUMENT: md.files[i].initialized is PUBLIC and is exactly the "built a controller / paid a 2nd diff" signal — deterministic, no wall clock.** Generalise: anything gated on `typeof document !== "undefined"` is SILENTLY SKIPPED under bun; check the gate before trusting an A/B.
0828-DEFLATED MY OWN HEADLINE: the 468ms lazy=false ctor is dominated by initializeFileDiff building ONE div PER LINE (:254-272, zero virtualization), NOT diffing (~50 of 468ms). Said so in the issue instead of quoting 468ms. (Cannot monkeypatch ESM namespace to count diff() calls — `Object.defineProperty` on a module namespace throws "Cannot replace module namespace object's binding"; use a public state flag instead.)

**0828-GREP-BEFORE-FILING PAID OFF 2x AGAIN (now 4 runs running):** (a) **PR #511 (Repo Assist, open since 04-28, mergeable clean) ALREADY benchmarks this exact stats phase — including an identical-files control equivalent to the one I'd independently rebuilt.** It was MISSING from #670's SA list; added it. #511 measures but does NOT attribute (no 98.9% share, no duplicate-diff). Its header comment claims DOM paths are "covered by browser e2e tests" — FALSE, but Test Improver already corrected that on #667, so I did NOT re-report. (b) My 1/50 result reproduces Test Improver's **08-27 #667 correctness defect** (default lazyRender leaves files 2..N expanded-but-blank; expandFile/expandAll/scrollToFile ALL gate on `collapsed` which is already false => no public API can populate them). **Credited, did not re-file.** Note this makes cost (2) mostly moot on the DEFAULT path — only file 0 ever initializes.
0828-REJECTED-do-not-reinvestigate: mergeDecorations (editor-view.ts:108/:255 + use-diff-view.ts:158 `[...ctrl.decorations]`, 2N array copies/frame) — verified `ctrl.decorations` is a PLAIN CACHED-ARRAY GETTER (controller.ts:303), no recompute. Genuine copies but ~ns/element; likely small. Backlog 7 now says "measure before filing", NOT filed.

0828-T4: 4 PI PRs all open, no conflicts — #310/#339/#575 mergeable_state **clean**, #565 **unstable** w/ 0 check runs (infra, not my change; memory rule: do NOT push for infra-only). No pushes.
0828-T5: **0 comments, deliberately.** 0 human comments repo-wide; every relevant issue already carries mine. Repo Assist corroborated #756 on 08-27 (2.6-2.9x, 4/4 rounds) and ADDED a genuinely better argument: **editor.ts:126 is the ONLY `new WrapMap` in src/ with NO size threshold — dom.ts:813/818, canvas.ts:1046/1051, webgpu.ts:1156/1161 all use `{lazy:true}` above 5000 rows.** It offered to write either fix. That is a MAINTAINER DESIGN CALL => routed to #670 Suggested Actions (upgraded the #756 item to a 2-option decision), NOT a reply comment. Reply would have been spam.
0828-T7: #670 body 18408B -> ~19KB, full replace. Added 08-28 entry (top), condensed 08-26 into the condensed block, added the new multi-file SA item (bare search link — **LINK ITS NUMBER NEXT RUN**), added **#511** to SA, upgraded #756 SA item, rewrote backlog 6 (surveyed) + 7 (mergeDecorations verdict), added happy-dom quirk to Discovered Commands, updated flake to 12/69. Held the 15-20KB ceiling. Dropped the stale trailing "Generated by" footer + firewall warning block again — safe-output appends its own. Tree clean, `git status --porcelain` empty, no PR (hold intact).
0828-FIXTURE (rebuild each run, /tmp/gh-aw/agent/mfbench, NOT committed): SRCROOT-placeholder template + `sed` into run.ts (avoids the dynamic-import typecheck trap); `ln -s <repo>/node_modules`. makeFile(i,lines,changeEvery) -> FileDiffEntry{filename,oldContent,newContent}; container = `{style:{},appendChild(){},removeChild(){},addEventListener(){}}` cast to HTMLElement. resetMultiFileDiffCounter() between arms.
