SIZE-WATCH: **THIS FILE (notes9.md) IS THE APPEND TARGET.** memory.md + notes.md + notes2-8 are ALL at/near the 10240 PER-FILE cap (notes8=6065 but a 4.9KB append blew it to 10929 — I caught it with wc -c and split into this file instead of trimming). notes8 header still says it is the target; ignore it, I skipped that edit because editing a 6KB file costs ~6KB of patch and the 10KB PATCH cap is the binding limit. `wc -c` BEFORE appending; keep appends <1KB or start notes10.md.

0829:T1+T2+T6+T7 run:33225037624. main STILL ce545ec, 0 human comments EVER (`commenter:iamnbutler updated:>2026-07-01`=0), ALL SA unticked, 0 PRs merged/closed since 0804. bun1.4.0 typecheck0 lint1info bench118/118 tests 2265/0 NO flake (cum 12/70=17.1%), 2.83s. 178 open PRs (2 pages). #770 = the 0828 multi-file issue, NOW LINKED in #670.

**0829-BIGGEST: REVERSED MY OWN #1 SA ITEM. New PR#766 (rope.ts, cache utf8 bytes/chunk) SUPERSEDES #510 (buffer.ts, lazy TextSummary.bytes getter).** Disjoint src files => 4-arm file-level checkout (main/510/766/both), 3 interleaved rounds, order reversed on even rounds, 3/3 monotone.
0829-NUMBERS (reuse), ms/keystroke insertText+deleteBackward @1k/4k/16k: **A .bytes NEVER read**: main .1267/.4476/1.7499; 510 .0424/.0348/.0220; 766 .0510/.0274/.0260; both .0318/.0278/.0261 => INTERCHANGEABLE (79.5x vs 67.3x@16k), **both == 766 alone, stacking adds NOTHING.** **B consumer reads buf.snapshot().textSummary.bytes each edit**: main .1137/.4378/1.7492; 510 .0612/.2247/.8864 (**only 1.97x@16k**); 766 .0136/.0180/.0250 (**70.0x**). **510 DEFERS the O(n) scan, 766 REMOVES it => 510's win is contingent on nobody reading the field it makes lazy.** 766 also needs no API change (510 turns TextSummary into a getter object).
0829-OBJECTION I RAISED AGAINST MYSELF AND KILLED (do this every time): per-chunk countUtf8Bytes should penalise chunk-rebuild workloads. It does NOT — createBuffer 4k/16k main .389/1.559 vs 766 .379/1.550 (FLAT, noise); create+paste100/1000/4000 main .662/.744/1.022 vs 766 .411/.494/.768 = **766 1.3-1.6x FASTER**, 3/3. Mechanism: same chars scanned either way, 766 folds the count into the pass that already builds the chunk instead of a 2nd full-rope pass. Tests 2265/0 + typecheck0 for 766 alone AND both.
0829-T5-placement: commented ONCE, on **#766** (ZERO PI comments). Did NOT re-comment #510 (3 PI comments, no human reply = spam) — routed the flip through #670 SA instead. Same routing call as 0828.

**0829-NEW FINDING (filed, number UNKNOWN — LINK IT IN #670 NEXT RUN, SA has a bare search link): bracket-match.ts scanForward:81-83 / scanBackward:117-119 call snapshot.lines(row, row+1001) UP FRONT, so MAX_SCAN_LINES bounds the BEST case too.** Attribution: that one call = **91.4/98.1/98.0/97.4%** of findMatchingBracket @2k/5k/10k/20k. lines(row,row+1)=.0004ms. Direct cost .0091/.0154/.0294/.0294/.0289 @200/1k/2k/5k/10k = RISES then FLAT at 2k (the 1000-row cap saturating — that shape IS the proof). Distance sweep @10k: d=0/1/10/100/500/999 -> .0288/.0290/.0304/.0378/.0734/.1177 => same-line == 10-rows-away. no-bracket control flat .0004 (70x cheaper). E2E bracketMatching off/on: 1.27x@200-2k, 1.05x@10k (denominator grows, absolute add FLAT .063 = 2 scans; 2x model matches @1k/5k/10k within 10% but **NOT @200 (.0182 pred vs .0089) — flagged, unexplained**).
0829-PROTOTYPE (chunked CHUNK=64, in-fixture copy, NOT a worktree patch — imports only the snapshot iface so no repo edit needed): identical results on 10 cases, then 4 interleaved rounds. fwd d=0/1/10/100/500/999 -> **51.9x/11.7x/7.7x/2.5x/1.19x/0.94x**; bwd 62.7x/10.9x/2.9x/0.93x. **HONEST DOWNSIDE: 6% slower at full budget, 13% slower unmatched (0/4 both). Break-even ~500 rows. CHUNK=64 untuned.** Negative control (no bracket) 1.03x = untouched. Line length barely matters: .0290->.0321 for 30->200 chars (**I predicted inflation, was WRONG, said so** — so .03ms is honest, not an underestimate). Sizing stated plainly: ~3% of the <1ms budget, opt-in, NO multi-cursor amplification (uses this._cursor only). 31 unit tests already guard it.

**0829-T6 = CORRECTION TO MY OWN #739 METRIC, the durable lesson: FILE REACHABILITY != EXERCISE.** bracket-match.ts is NOT in the uncovered list (editor.ts imports it) yet no benchmark runs a line of it. **EditorOptions has exactly 3 opt-in fields (bracketMatching/readOnly/wrapWidth) and ALL FOUR `new Editor(...)` in benchmarks/ pass NO options** (editor.bench.ts:44,63,162,191). wrapWidth appears in benchmarks ONLY in renderer.bench.ts/wrapmap.bench.ts = handed to the renderer/WrapMap, NEVER to an Editor. **2 of 3 flags gate per-keystroke cost, neither exercised, BOTH now yielded a defect (#756 + the bracket one) = 2/2.** Put it in the new issue + #670 backlog item 1; did NOT re-comment #739 (I commented 0826, no human reply). My recompute said 31 files/10501 loc vs #739's 22/8940 — rule difference (I excluded index.ts barrels), NOT repo drift; did not quote a count.
0829-FIXTURE: /tmp/gh-aw/agent/bmbench, SRCROOT/WTROOT placeholder + sed (static imports). NO public mb.buffers() — hold the Buffer ref from createBuffer. PR arms: `git fetch --depth=1 origin +refs/pull/N/head:refs/remotes/pr/N` then worktree + `git checkout HEAD -- src/` to reset, `git checkout pr/N -- <file>` to apply. worktree removed, prune, porcelain EMPTY.
