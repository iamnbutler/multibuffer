SIZE-WATCH: **THIS FILE (notes12.md) IS THE APPEND TARGET.** memory.md + notes.md + notes2-11 are all at/near the 10240 PER-FILE cap (notes11 hit 10388 when I appended this run's block there — caught it with `wc -c` and split instead of trimming). notes11's header still names itself; ignore it. `wc -c` BEFORE appending; keep appends <1KB or start notes13.md.

0901:T2+T3+T5+T7 run:33552233963. main STILL ce545ec. `commenter:iamnbutler updated:>2026-08-01`=0 => still 0 human comments EVER, ALL SA carried over unactioned. bun1.4.0 test 2274/2265pass/0fail 2.79s NO flake (cum 12/73=16.4%) typecheck0 lint1info bench118/118. **#785 = the 0831 long-line issue, NOW LINKED.** NEW MONTH: closed #670 (append+status:closed in ONE update_issue - the 1-update budget) and created the Sept monthly (number UNKNOWN - **LINK IT NEXT RUN**, its top SA item has a bare search link).
**0901-BIGGEST (largest single-call cost yet): `diff()` gives an ADDED/DELETED file the FULL Myers search.** myersDiff(diff.ts:78) guards ONLY `max===0`. With n=0 the snake at :118 can never advance (`x<n` false) so the search runs to d=N. EXACT counts (analytic, not timed): n=1000 -> D=1000, 500,501 inner iters, 1000 Int32Array trace slices = 3.8MB; n=4000 -> D=4000, **8,002,001 iters, 61MB** trace. Control: same file, 1-line change = D=2, **5 iters**, 32B.
0901-NUMBERS (reuse): isolated diff("",text) main->fix 0.335/0.017=19.2x@250, 5.52/0.056=97.9x@1000, **85.84/0.251=341.7x@4000**, 3/3 each; deleted side 101.5x/213.1x. CONTROLS one-line + scattered 0.94-1.10x, direction 1-2/3 = noise (branch unreachable there = exactly right). RSS 20 diffs@4000: 326MB vs 36MB. E2E createMultiFileDiff (order-reversed twice): 20 modified x400ln = 1.32/0.71ms; +5 added x400 = 5.17/5.31 -> 0.95/1.02; **+3 added x2000 = 66.96/65.81 -> 1.14/1.88**. LIVE DiffController.reDiff() @2000ln: control 0.50ms, after clearing edited buffer **26.0 -> 0.41ms** (controller.ts:108 passes newSnap.text() straight in).
**0901-THE BOUND (say it first, it is most of the finding): ONE LINE of old content defeats the fast path COMPLETELY - 22.6ms WITH and WITHOUT.** Also live: clear-then-type-1-char = 21.5 main / 29.3 fix = same magnitude, branch bypassed. Root property = **D has NO CAP**, so any low-similarity pair is quadratic (repo's own `1K full rewrite` bench = 31.9ms, passes on 1.5x headroom). Capping D changes output => filed as a DECISION, not folded into the fix. Fix = 19 lines in myersDiff (covers diff() AND diffLines()); verified by **1318 paired comparisons byte-identical** (9x9 edge grid x ctx 0/1/3 + 400 random + empty-side 1/2/50/300), 2265/0, typecheck0, lint0, bench118/118, 16 diff benches 0.95-1.36x = noise BY CONSTRUCTION (no empty side in any of them).
0901-COVERAGE = 6th blind-spot axis (flag -> usage -> data shape -> **INPUT shape**): `oldContent:""` is documented + unit-tested (`multi-file.test.ts` "handles new files"/"handles deleted files") but the fixtures are **3 LINES**, so the quadratic is invisible. ZERO benchmarks anywhere use an empty side.
0901-T5: commented ONCE, on **#511** (0 prior PI comments, the ONLY proposed MultiFileDiff bench suite). Measured with ITS OWN helper so the ask was concrete: makeEntries(10,med1k,scattered)=2.19ms vs makeEntries(10,"",med1k)=**56.46ms**, 26x from one argument => its 5 scenarios would NOT detect this before or after a fix. Did NOT re-comment #770/#785 (carry mine, no human reply). 6th run in a row declining a re-comment.
0901-FIXTURE (/tmp/gh-aw/agent/diffbench, NOT committed, PR hold intact): **src/diff/diff.ts imports ONLY types => copy it standalone like rope.ts**, `sed 's|from "./types.ts"|from "<abs>/src/diff/types.ts"|'` gives main-arm.ts, python-patch a copy for fix-arm.ts, both importable in ONE process for true interleaving. E2E/live arms import repo src by ABSOLUTE path, so the arm is selected by what is IN src/ (patch, measure, `git checkout -q HEAD -- src/`). multi-file container stub = `{} as any` (DOM paths gated on `typeof document!=="undefined"`, false under bun). **WARM THE WHOLE SUITE BEFORE THE FIRST TIMED CASE** - my first e2e read made the +5-added case look FASTER than the 20-modified baseline; it was cold-start on entry 1. Then ALWAYS re-run with the case order reversed.
0901-LESSON: heapStats read **0.00 MB** for a retained 3.8MB Int32Array trace at n=1000 and over-read 90.8MB vs 61MB analytic at n=4000 => **do not trust bun:jsc for typed-array backing stores; RSS + an EXACT analytic count is the pair that holds up.** The analytic count is the strongest number in the whole finding because it cannot drift.
