SIZE-WATCH: memory.md 10206, notes.md 9976, notes2.md 8468, notes3.md 8588, notes4.md 9840, notes5.md 7145 - ALL at/near the 10240 PER-FILE cap. APPEND HERE. Start notes7.md when this nears ~9KB. Do NOT trim the older six: deletions cost patch bytes and the 10KB PATCH cap is the binding limit. 0824 LESSON: appending a full run entry to a 7.1KB file blew the cap (11499B) - check `wc -c` BEFORE appending, not after.

0824:T1+T2+T6+T7 run:32758301192. main STILL ce545ec, ALL 20 tracked PRs still open (2 API pages, min#310 seen => complete), 0 merged/closed since 0804, still 0 human comments EVER. bun1.4.0 typecheck0 lint1info bench118/118 tests 2265/0 NO flake (cum 12/66=18.2%). bun test 2.77s, bench 15.46s.

**0824-FILED: decoration-map loop is O(total decorated rows) per frame in ALL 3 renderers** (canvas.ts:489 NO viewport test at all + stores every row; dom.ts:533 + webgpu.ts:441 test INSIDE the loop). 4th private copy in tests/renderer/decoration.test.ts:16-31 ("Simulate the ... logic used in DomRenderer.render()") = same shape as #725, so a src-only fix leaves tests green. Producer is REAL: diff/controller.ts:196/206 -> makeDecoration(mbRow,lineCount) (diff-styles.ts:51-66) = one decoration per run of same-kind lines, end.column=MAX_SAFE_INTEGER. Issue number UNKNOWN at write time - LINK IT IN #670 NEXT RUN (SA entry has a bare search link).
0824-NUMBERS (reuse): us/frame @span 50/500/5k/50k/200k -> canvas 0.85/10.27/154.58/1623.82/13300.70; dom+webgpu 0.88/1.31/5.30/44.47/172.90; clamped 1.19/0.91/0.92/1.01/1.56. Retained B/frame canvas 3.2/40.7/640.7/3328.7 KB vs flat 3.2KB for the other two. Many-decoration variant (10-row decs, same totals) reproduces it => cost is SUM OF SPANS, not one giant span; clamped rises to 13.35us@50k because outer loop is O(#decs) - irreducible, said so.

**0824-THE HONESTY MOVE THAT MATTERED: the isolated harness said 44us and I did NOT stop there.** Drove the REAL DomRenderer.render() under happy-dom (span 50->200k = 4309.6->4572.4us = **1.06x, ~6% of frame**) and the ~263us growth matched the isolated harness's ~173us prediction. Two harnesses agreeing = cross-validation (cf 0817 1.9%); but it also DEFLATED my own finding, so I filed it as low-to-moderate and said happy-dom's 4.3ms DOM work FLATTERS dom.ts. **Measure the end-to-end share before choosing the adjective.**
0824-LIMIT: canvas.ts/webgpu.ts render() CANNOT be driven here (need OffscreenCanvas/WebGPU) - canvas.test.ts never instantiates CanvasRenderer, it only imports measurement fns + sliceTokensToRange. So their end-to-end share is UNKNOWN, only loop cost measured. Stated as a limit in the issue. **A browser harness is the blocker for backlog item 2 - recorded in #670.**

0824-TWO AXES REJECTED BEFORE FILING (do-not-reinvestigate):
(a) **MODULE GRAPH / COLD START = DEAD END.** renderer barrel eagerly re-exports canvas(1310)+webgpu(1298)+glyph-atlas+tile-map+injection-highlighter = 16 files/6926loc vs dom.ts alone 9/2756. BUT `bun build --minify` of barrel-import vs direct-import = **30774 vs 30773 bytes (1 byte)** - fully tree-shaken, bundled consumers pay ZERO. Unbundled fresh-process import 5.9 vs 4.6ms = ~1.2ms ONE-TIME (8/9 ABBA rounds). Not worth an issue. Graph sizes for reuse: root 56 files/621KB, editor 28/345KB, diff 33/404KB, multibuffer 10/102KB, buffer 5/36KB.
(b) **DEV-WORKFLOW SPEED IS HEALTHY** - 2274 tests in 2.77s, bench 15.46s. No fruit.
0824-PACKAGING NOTE (NOT perf, did NOT file): `bun build --target=browser` FAILS on 4 of 8 exports (root, editor, renderer, diff) - web-tree-sitter internally does `await import("module")`. playground/build.ts already works around it with `external:["web-tree-sitter"]`. Both tree-sitter deps are TYPE-ONLY + dynamic-import in src. Recorded in #670 Discovered Commands quirks only.
0824-TOOL: static module graph = regex `(?:from|import)\s*["']([^"']+)["']` + resolve ./x -> x.ts|x/index.ts, SKIP `import type`/`export type` heads (else you get fake externals); BFS w/ parent map prints the shortest import chain to any target - that is how I proved diff reaches dom.ts but NOT webgpu.ts.
0824-FIXTURE: DomRenderer under happy-dom - copy setupDOM from tests/renderer/dom.test.ts:81 (assign globalThis.document + defineProperty window), makeSnapshot stub w/ lineCount/lines/excerptAt, Measurements{lineHeight20,charWidth8,gutterWidth40}, mount(container) then setSnapshot. Control = compare container.innerHTML across arms (37276B identical). Worktree + `ln -s node_modules`, removed + porcelain asserted empty.
0824-T7: #670 08-23 entry WAS present, body intact 14.5KB (0822 lean rebuild holding, no repeat of the 0821 destruction). Rewrote to ~15KB: added 08-24 entry + new SA item, condensed 08-23 into the condensed block. **Held the 15-20KB ceiling.**
