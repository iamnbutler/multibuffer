SIZE-WATCH: **THIS FILE (notes14.md) IS THE APPEND TARGET.** memory.md + notes.md + notes2-13 are all at/near the 10240 PER-FILE cap (notes13 hit 12206 when I appended this run's block there — caught it with `wc -c`, restored it via `git -C <memdir> checkout -- notes13.md` and split here instead). `wc -c` BEFORE appending; keep appends <1KB or start notes15.md. PATCH cap 10240 total per push, and a modified line costs bytes twice — never rewrite a whole file.

0905:T1+T2/T3+T4+T7 run:33985793759. main STILL ce545ec. `commenter:iamnbutler updated:>2026-08-01`=0 + 0 PRs closed since 08-25 => still 0 human comments EVER, ALL SA carried. **bun 1.4.2** (4th bump seen mid-workflow, feeds #731) test 2265/0 2.76s NO flake (cum 12/77=15.6%) typecheck0 lint1info bench118/118. **#806 = the 0904 ProjectTree issue, NOW LINKED.** New anchor/setExcerpts issue number UNKNOWN - **LINK IT NEXT RUN** (its SA item is the top one, bare search link).
**0905-FINDING (2 sibling APIs, neither is both fast+correct): `setExcerptsForBuffer` maps every retired excerptId -> newIds[0] (multibuffer.ts:906), NEVER compressed/pruned => resolveAnchor walks 1 extra link per call (loop at :216-223, `maxChainLength=100` at :217). `setExcerpts` (:798) writes NO mapping at all - and IT is what DiffController.reDiff() calls (controller.ts:123 AND :258, both branches).**
0905-NUMBERS (reuse): us/resolveAnchor by replacement count, 20 exc over 400ln: 0/10/25/50/75/100 => 0.380/1.047/2.106/4.165/5.924/7.743 = **19.8x**, marginal 0.074us/link. **CONTROL = anchor created AFTER the replacements: FLAT 0.378-0.395 across the whole sweep** (same doc, same excerpt count, same 2000-entry map, chain=0) => growth is the WALK, not map size/excerpt count/doc. Mem heapStats: 137 B/entry at scale (cross-checks 0815's 131 B precomputed-key constant), 40000 entries/**5.36MB** @2000 calls, unreclaimable. **CLIFF: resolves at 100, UNDEFINED at 101** (walk exits mid-chain holding a stale id; SAME return value as a genuinely deleted excerpt). The ~8us plateau IS the failure, not a saving.
0905-E2E (the half with a live consumer): drag-select in a diff view + ONE newBuf edit + reDiff => selection PERMANENTLY dead. selections.length stays 1, resolveAnchorRange undefined, getSelectedText()==="" forever, renderSelection(undefined,undefined). Led with the bound: the 19.8x is on an API with **ZERO callers in src/**. Filed as a DECISION, NO patch (fixing the chain would optimise the API nobody calls) - same class as #792 D-cap / #800 budget.
**0905-ATTRIBUTION OVERTURN (12th) - REJECTED, do not re-investigate: anchor EDIT-REPLAY.** Started on `Buffer._editLog` (append-only, never trimmed) expecting resolveAnchor to replay a growing backlog via editsSince+adjustOffset. **It is 0 entries after 2000 keystrokes** - the editor builds a FRESH anchor at the current version on every edit (_insertText), and search re-runs on every textChange too. Log is a slow MEMORY cost only (~56B/edit); did NOT file it as latency. Undo stack IS capped (_MAX_HISTORY=100, editor.ts:60) - the contrast is the only interesting part and not worth an issue alone.
0905-9th COVERAGE AXIS (#739, sharpest yet): `tests/multibuffer/anchor-replacement.test.ts` exists BY NAME for this invariant; every replacement test uses setExcerptsForBuffer (no in-repo caller) and calls it EXACTLY ONCE = the one count where neither defect can appear. Previous 8 axes = what was never EXECUTED; this one = invariant tested through the WRONG one of 2 sibling APIs at N=1.
0905-CLEARED (do not resurvey): SlotMap (genuinely O(1)), renderer/measurement.ts (pure math), src/react/ (identity-dep effects on `[options.theme]`/`[options.decorations]` are real BUT react-dom is not a dep, nothing in-repo renders them, premise needs hypothetical callers => recorded as #793 backlog item 16, NOT filed). multibuffer lines() already binsearch+bulk.
0905-FIXTURE (/tmp/gh-aw/agent/histbench, NOT committed, PR hold intact): SRCROOT+sed, `ln -s node_modules`. probe.ts reads `mb._buffers`->buf.version/_editLog + sel.range.start.textAnchor.version; chain.ts/control.ts walk `mb._replacedExcerpts` for chain length; ONE n per process for mem.ts. **createMultiBuffer() then mb.setExcerptsForBuffer(buf, ranges) where range={context:{start:{row},end:{row}},primary:{...}}.** T4: #310/#339/#565/#575 all open+draft+clean, parked CI gate NOT mine => NO pushes (5th run). T5 skipped (round-robin; 9 runs of 0 human comments).
