# Benchmark History

This branch stores benchmark results. Each line in history.jsonl is a JSON object with:
- `sha`: commit hash
- `timestamp`: ISO timestamp
- `suites`: array of benchmark suite results
