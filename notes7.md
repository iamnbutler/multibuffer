SIZE-WATCH: **SUPERSEDED 0828 — notes8.md IS NOW THE APPEND TARGET, not this file.** memory.md/notes.md/notes2-6 are ALL at/near the 10240 PER-FILE cap (notes6=9106). **This file (notes7.md) WAS the append target — notes6's own header still says otherwise, ignore it; I skipped that edit to save patch bytes.** Do NOT trim the older seven: deletions cost patch bytes and the 10KB PATCH cap is binding. `wc -c` BEFORE appending. 0826: this file alone was a 9469B patch WITH a 1-line notes6 edit, 8355B without — the margin is thin, keep appends <1KB.

0826:T1+T2+T6+T7 run:33004241844. main STILL ce545ec, still 0 human comments EVER, ALL SA boxes unticked, no PR merged/closed since 0804. bun1.4.0 typecheck0 lint1info bench118/118 tests 2265/0 NO flake (cum 12/68=17.6%). bun test 2.88s, bench 14.69s.

**0826-BIGGEST: A BROWSER BENCH HARNESS WORKS HERE. My 0824 note "a browser harness is the blocker for backlog item 2" was WRONG — I never tried.** Playwright is ALREADY a devDep (@playwright/test 1.58.2) w/ playwright.config.ts + tests/e2e/. `./node_modules/.bin/playwright install chromium` SUCCEEDS (~111MB, warns "OS not officially supported", downloads ubuntu24.04 fallback, installs chromium-1208 + chromium_headless_shell-1208).
**0826-TRAP: `bunx playwright install` pulls playwright-core 1.62.1 from bun's cache and installs build 1208, then launch expects 1234 => "Executable doesn't exist".** ALWAYS use `./node_modules/.bin/playwright`. Second half of the same trap: a runner script living in /tmp resolves `@playwright/test` to bun's install cache, NOT repo node_modules => `ln -s <repo>/node_modules /tmp/.../node_modules` in the fixture dir.
0826-BUNDLING: `bun build --target=browser --external web-tree-sitter --external tree-sitter-typescript src/renderer/canvas.ts` => 10 modules, 48.42KB, **ZERO residual import statements** (canvas.ts only TYPE-imports the highlighter, so the externals are never reached). Confirms 0824's packaging note and turns it from a blocker into a one-flag workaround.

**0826-FIRST-EVER end-to-end CanvasRenderer.render() numbers (0824 filed #750 with canvas share explicitly UNKNOWN).** 2 bundles differing ONLY at canvas.ts:487 decoration loop (main vs viewport-clamped), 50-row viewport pinned, 6 ABBA rounds x 30 iters x 10 warmup, median-of-medians.
0826-NUMBERS (reuse), ms/frame @50-row vp, single span: rows 50/500/5k/50k/200k -> main 17.700/17.500/17.600/19.700/26.800; clamped 17.400/17.200/17.100/17.300/17.400; delta 0.300/0.300/0.500/**2.400**/**9.400**; direction 2/6, 5/6, 6/6, 6/6, 6/6. **Loop share of a REAL frame = 2.8% @5k, 12.2% @50k, 35.1% @200k.** chunk=10 variant (20,000 separate 10-row decs): delta 0.100/1.700/9.600 @5k/50k/200k, 4/4 => **cost = SUM OF SPANS, reproduced in a real browser.** Clamped arm FLAT 17.1ms even w/ 20k decs => outer O(#decs) loop is NOT the problem.
**0826-CORRECTED MY OWN 0824 RATING: canvas is ~6x dom's share (35% vs 6%), so the isolated harness UNDERSTATED it.** 0824 I rated #750 low-to-moderate off dom.ts end-to-end while flagging canvas unmeasurable. Said the correction plainly in the #750 comment. Isolated-vs-e2e agree only to ~1.5x BOTH directions (isolated 0.15/1.62/13.30 vs e2e 0.50/2.40/9.40) — same order + same shape, NOT a tight calibration; said so.

**0826-INSTRUMENT CHECKS (both mattered, do this every time):** (a) IDENTICAL-CODE CONTROL (0821 method) — `cp canvas-main.js canvas-mainB.js` + sed the 2nd import, rerun 6x30: max false delta **±0.2ms, ratios 0.994-1.012, direction 2-3/6 = coin flip**. So 2.4/9.4ms clear the floor 12x/47x; **0.3ms @500 rows sits AT the floor and I refused to defend anything under 5k rows.** Control ALSO reproduced the growth curve in BOTH arms (17.2->19.1->26.3) which is exactly right when both arms are unclamped. (b) **IS THE ~17ms FLOOR REAL WORK OR A STALL?** It scales with VISIBLE ROWS 0.2/0.6/6.1/17.8ms @1/5/25/50 => genuine per-glyph drawing. Mattered because the floor is the DENOMINATOR of every "% of frame" claim.
0826-NOISE FLOOR COMPARISON (big, reuse): browser harness ±1.2% worst case vs `benchmarks/` p50 4.7% / p90 23.3% (0821). Browser is a MUCH quieter instrument — but part of that is the browser's coarse clock + a frame being a bigger unit of work than a microbench; said that caveat rather than claiming harness.ts is indicted.
0826-HEADLESS CAVEAT I VOLUNTEERED: headless chromium SOFTWARE-rasterizes, ~17ms to draw 50 rows is far slower than GPU-backed canvas => a faster denominator makes the loop share LARGER, so 35% is CONSERVATIVE. Cuts in my favour, still stated.
**0826-WEBGPU IS GENUINELY UNMEASURABLE HERE: navigator.gpu ABSENT under BOTH chromium-headless-shell AND channel:"chromium", with --enable-unsafe-webgpu --use-angle=swiftshader --enable-features=Vulkan,WebGPU. gpu:false,adapter:false in all 3 configs.** Reported as a limit; did NOT generalise the canvas result to webgpu.ts.

0826-FIXTURE (rebuild each run, /tmp/gh-aw/agent/cbench, NOT committed, PR hold intact): bench.html imports both bundles as ES modules; stub snapshot needs ONLY lineCount / lines(a,b) / excerptAt() / resolveAnchor() / excerptBoundaries(). Measurements {lineHeight:20,gutterWidth:40,charWidth:8}. `new CanvasRenderer(m)` then `.mount(host)` then `.setSnapshot(stub)`; host needs an explicit px width/height + a font. Decorations start row 0, viewport pinned at row 300000 so extra rows are genuinely OFF-SCREEN. Driver = Bun.serve(port:0) + chromium.launch() + page.evaluate(); return raw samples to bun and aggregate there. performance.now() in chromium is coarsened to ~0.1ms — fine at ms magnitudes, USELESS below ~0.5ms.
0826-WORKTREE: `git worktree add -q --detach $WT HEAD` + `ln -s node_modules`, patch canvas.ts INSIDE it, bundle both arms out to /tmp, then `git worktree remove -f` + `git worktree prune` + assert `git status --porcelain` EMPTY. Done, tree clean.

0826-T2 SURVEY (react/ + worker/, 1742 loc, ZERO bench coverage, FIRST look, **FILED NOTHING** because nothing is measured yet — recorded as backlog item 7 in #670):
(a) **mergeDecorations (editor-view.ts:108) re-flattens EVERY decoration into a FRESH array on EVERY frame** (called editor-view.ts:255), and use-diff-view.ts:158 adds ANOTHER full copy `[...ctrl.decorations]` per frame. O(total decorations), viewport-independent — same FAMILY as #750 but a DISTINCT upstream site. If measured, likely belongs as a #750 comment, not a new issue.
(b) both React hooks compare `theme`/`decorations` props by IDENTITY in useEffect deps (use-editor-view.ts:120-131, use-diff-view.ts:358-372) => an inline `theme={{...}}` / `decorations={[...]}` literal re-runs themeToVars + Object.entries + setProperty, or scheduleRender, on EVERY parent render. Classic React footgun, partly a consumer problem — measure before filing.
(c) use-diff-view.ts:318-348 replaces the ENTIRE buffer (`replace(0, len, text)`) whenever the oldText/newText prop changes — inherent to the uncontrolled->controlled API shape, probably NOT a defect. Don't chase.
(d) worker/highlight-client.ts:206 parseBufferAsync sends the WHOLE `text` in the postMessage per parse; playground/demo.ts:151 calls `parseBuffer(id, buf.snapshot().text())` on edit with **no TreeEdit** => full text materialise + structured clone + full reparse per keystroke. BUT that call site is playground DEMO code, not src => docs/API-shape issue at best. Verify who calls it in src before spending a run on it.

0826-T7: #670 body was 18408B INTACT (the 0822 lean rebuild is holding, 4 runs now, no repeat of the 0821 destruction). 18408->~17.5KB: added the 08-26 entry, condensed 08-25 into the condensed block, LINKED **#756** (was a bare search link) and **#750** (already linked), upgraded the #750 SA item, rewrote backlog item 2 (blocker removed), added backlog item 7. **Held the 15-20KB ceiling.** Dropped the stale trailing "Generated by" footer again — safe-output appends its own.
0826-T5 not scheduled; Task2 step6 + Task6 step5 authorise the 2 comments I posted (**#750** and **#739**, BOTH had ZERO comments). Posted nothing on #703/#731/#725 despite the noise-floor comparison being relevant — it lives on #739 where it is actionable and those already carry my comments. Restraint held.
