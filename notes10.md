SIZE-WATCH: **THIS FILE (notes10.md) IS THE APPEND TARGET.** memory.md + notes.md + notes2-9 are all at/near the 10240 PER-FILE cap (notes9=5349 but appending there costs ~5.3KB of patch and the 10KB PATCH cap is the binding limit). New small file = patch ~= file size, which is why I started this one. notes9's header still names itself; ignore it. `wc -c` BEFORE appending; keep appends <1KB or start notes11.md.

0829-RUN2:T3+T4+T5+T7 run:33271585014 (SECOND run same day; run1=T1+T2+T6). main STILL ce545ec. `commenter:iamnbutler updated:>2026-07-01`=0 AND `is:pr is:closed closed:>=2026-08-04`=**0** => nothing actioned, ALL SA still pending. Combined-arm bun test 2265/0 typecheck0, 2.86s. #774 = the 0829-run1 bracket issue, NOW LINKED in #670.

**0829-RUN2-BIGGEST: I CLOSED A GAP I HAD LEFT IN MY OWN TOP RECOMMENDATION 18h EARLIER, and it produced TWO retractions against my own case.**
(1) **#510+#684 vs #766+#684 are INDISTINGUISHABLE**: 0.98/1.00/1.00x @100/500/1000 exc, direction 1/3, 2/3, 1/3 = COIN FLIP. So the #684 pairing is NOT an argument for #766 — the ONLY argument is the 0829-run1 `.bytes`-consumer axis (1.97x vs 70.0x). Rewrote the #670 SA item to say so.
(2) **RETRACTED my own 0814 "super-additive" claim.** Measured in ONE fixture: combined/sum = 0.89/1.05/0.94 = plainly ADDITIVE. The 0814 "188 vs 175us = 7% excess" sits INSIDE this suite's p50 4.7% noise. **Cross-run/cross-fixture arithmetic manufactured a finding that one-fixture measurement dissolved.** Same class as the 0811 fixture trap.

0829-RUN2-NUMBERS (reuse), us/keystroke insertText, shared shape (N excerpts over 1 buf), TOTAL_ROWS pinned 10000: main 1861.6/1863.4/2334.5 @100/500/1000; 766 1107.5/1421.8/1697.8 (1.68/1.31/1.38x); 684 600.5/759.2/962.3 (3.10/2.45/2.43x); both 60.3/236.2/436.2 (**30.9/7.9/5.35x**), 3/3 direction everywhere. NOTE 766@500=1.31x is AT the 1.33x zero-FP threshold — only defensible via the 3/3 count.
**0829-RUN2-THE REAL HEADLINE IS NOT A RATIO.** Objection I raised + tested: 29x is inflated by pinned doc size. Varied it @100exc: main 230/718/1782us vs both **59/61/64us** @1000/4000/10000 rows => **paired arm is FLAT across 10x doc growth** = O(doc) -> O(n_excerpts), a COMPLEXITY-CLASS change. Ratio is big only because the DENOMINATOR shrinks. Also crosses the CLAUDE.md <1ms target: 1.78ms -> 0.064ms @10k rows.
0829-RUN2-INSTRUMENT CHECKS (2, both passed): (a) main@1000rows/100exc = 230us reproduces my **0814 shared@100 = 239.7us within 4%** => this fixture is calibrated against my own historical record (0814 must have used TOTAL_ROWS~1000). (b) residual after both PRs is FLAT in doc but LINEAR in n (~0.41us/exc) = **4th independent confirmation #713 is orthogonal**.

0829-RUN2-METHOD (reuse): 6 arms via file-level checkout, src diffs disjoint — 766=src/buffer/rope.ts, 684=src/multibuffer/{excerpt,multibuffer}.ts, 510=src/buffer/buffer.ts. Checkout only src/ (NOT the PR's tests) so the arm is graded by MAIN's suite. `git checkout -q HEAD -- src/` to reset between arms. Fixture /tmp/gh-aw/agent/pairbench, SRCROOT placeholder+sed, worktree wt684 + `ln -s node_modules`; removed, pruned, porcelain EMPTY.
0829-RUN2-T5: commented ONCE, on **#767** (Repo Assist, 0 PI comments). Content = the 3rd instance of the MODE-GATED axis: wrapWidth(#756)/bracketMatching(#774)/search(#767), **3 for 3**, and re-verified live that all 4 `new Editor(` in benchmarks/ pass NO options + `SearchController` appears in benchmarks/ ZERO times. Argued PRIORITY not approach (its 3 options are a maintainer call): once this cluster lands, soft-wrap + search are the ONLY remaining O(doc) per-keystroke terms => #767 goes from one-of-several to dominant. Did NOT comment #684/#766/#510 (all already carry mine, no human replies) — routed both retractions through #670 SA instead.
0829-RUN2-T4: #310/#339/#565/#575 all open, no conflicts; #565+#575 `unstable` with **0 check runs** = infra, NOT my changes => no pushes (memory rule held). 0829-RUN2-NOT-DONE: backlog item 8 (react/worker mergeDecorations) — surveyed the code but deliberately did NOT measure it this run; the #684/#766 pairing gap was higher value because a live top-of-list recommendation depended on it. Still "measure before filing".

0830:T2+T3+T7 run:33331891833. main STILL ce545ec, 0 human comments EVER, 0 PRs closed since 08-04. tests 2265/0 (2.55s, NO flake, cum 12/71=16.9%) typecheck0 lint1info bench118/118.
**0830-BIGGEST: WASM TREE LEAK. Highlighter.parseBuffer (highlighter.ts:95-97) does `_trees.set(id, tree)` WITHOUT `oldTree.delete()`.** web-tree-sitter has NO FinalizationRegistry (grepped) => every re-parse leaks a whole tree. 1000 lines/1000 parses: rss 71.0->169.9MB, marginal 97.1/99.8/99.9/101.3 KB/parse. 5000 lines/4000 parses: 86.5->**2057MB**, marginal 503.5/504.0/504.2/504.5 = DEAD FLAT, no plateau. Bun.gc(true) every 250, no recovery. Leaks w/ AND w/o edit descriptor (97.3 KB/parse no-desc).
**0830-CONTROL THAT MADE IT: call the EXISTING deleteBuffer() (which DOES tree.delete()) before each reparse -> rss PLATEAUS 78.8/78.1/78.1/78.1 @250/500/750/1000.** Isolates cause to the missing release, no argument needed. Reuse this shape: when a class already has a correct-release method, drive it as the control arm.
0830-FIX VERIFIED (1 line, `if (oldTree && oldTree !== tree) oldTree.delete();`): leak GONE 80.7->69.6MB/1000 parses; 2265/0 typecheck0 lint0; 4 parse benches -4.4%..+3.1% = inside 4.7% p50 noise. Applied to src/, measured, `git checkout -q HEAD -- src/`, porcelain EMPTY. FILED (number UNKNOWN - **LINK IT IN #670 NEXT RUN**, SA has a bare search link).
2nd site: injection-highlighter.ts:182 `_bufferParses.set` drops primary tree + ALL injection subtrees (:177, one per region) and that class has **NO deleteBuffer AT ALL** (grep = 0 hits).
**0830-9th ATTRIBUTION OVERTURN, against my own hypothesis: I predicted no-edit-descriptor => FULL reparse per keystroke (demo.ts:151 passes none). WRONG. noedit 0.366 vs incr 0.375ms @1k, 1.873 vs 1.856 @5k = INDISTINGUISHABLE, both ~12x faster than full.** It is fast BECAUSE it is wrong: tree-sitter reuses an un-edit()ed tree. Correctness check: tokens vs fresh parse DIVERGE at EXACTLY the edited row (500/1000, 2500/5000); incr matches exactly. Flagged as correctness + outside remit, did NOT dress it up as a perf win.
0830-2nd FIXTURE ARTIFACT CAUGHT: InjectionHighlighter first read 731 KB/parse but marginal was FALLING (110->20->6.4->6.0 MB per 50) = warm-up not leak. Re-ran to 600, quoted TAIL ~118 KB/parse (+46.2MB over parses 200-600). **Falling marginal rate = your average is warm-up. Always print per-interval deltas, never just the average.**
0830-REJECTED-do-not-reinvestigate: **mergeDecorations + React per-frame deco copies (editor-view.ts:108/:255, use-diff-view.ts:158) = 0.041ms/frame @20k decorations, ~2ns/elem, 0.25% of a 16.7ms frame.** 0828 predicted "likely small" - now MEASURED, backlog 8 CLOSED. Also NOT filed: worker transport (parse msg carries whole doc; post 0.009/0.029/0.128ms @59KB/293KB/1.17MB vs edit-payload FLAT 0.004-0.007 = 19.6x@20k rows) - real but modest AND no live consumer (createHighlightClient/createDiffClient have ZERO callers outside tests/). Recorded as #670 backlog 9.
0830-TOOLING: **Worker WORKS under `bun run` (not `bun test` - tests/worker/ header says unavailable, that is bun test only).** tree-sitter WASM loads fine under plain Bun from playground/wasm/. **RSS is the ONLY instrument for WASM linear memory - bun:jsc heapStats CANNOT see it** (noted on #716 item). Bash tool caps at 60s: use run_in_background for anything longer.
0830-T5: commented NOWHERE. #739/#716 both already carry my comment w/ no human reply => routed thru #670 SA. Restraint held (4th run in a row I declined a re-comment).
0830-MEMORY-TOOL GOTCHA: push_repo_memory FAILED reporting `.git/objects/pack/*.pack` (48958B) over the 10240 cap. That is the memory checkout OWN git plumbing, NOT a file I wrote - all 11 .md are <=10198B. Not actionable, do NOT delete .git or panic-trim notes over it.
