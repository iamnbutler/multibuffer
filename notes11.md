SIZE-WATCH: **THIS FILE (notes11.md) IS THE APPEND TARGET.** memory.md + notes.md + notes2-10 are all at/near the 10240 PER-FILE cap (notes10=8044; appending there risked it, and the 10KB PATCH cap is the binding limit, so I started a new file per the established pattern). notes10's header still names itself; ignore it. `wc -c` BEFORE appending; keep appends <1KB or start notes12.md.

0831:T1+T4+T5+T6+T7 run:33443568857. main STILL ce545ec. `commenter:iamnbutler updated:>2026-07-01`=0 AND `is:pr is:closed closed:>=2026-08-04`=0 => 0 human comments EVER, nothing actioned, ALL SA pending. bun1.4.0 test 2274/2265pass/0fail 2.87s NO flake (cum 12/72=16.7%) typecheck0 lint1info bench118/118. #781 = the 0830 tree-leak issue, NOW LINKED in #670.

**0831-NEW AXIS (5th coverage blind spot, and the first that is DATA SHAPE not file/flag/usage-pattern): NO benchmark fixture has a line >40 chars.** Verified exhaustively: generateText in buffer/editor/multibuffer.bench = `Line N: Some text content here` (~33); renderer.bench lineLen default 40 and its ONLY call passes 40; wrapmap.bench default 30. Rope changes regime at TARGET_CHUNK_SIZE=1024 => suite runs 25x below the threshold, long-line behaviour NEVER executed.
**0831-MECHANISM (exact, rope.ts):** textToChunks(:35-54) ONLY SPLITS, NEVER MERGES. insert(:479-487) rechunks `combined` = affected chunk + inserted text. If the chunk holds NO newline, `lastIndexOf("\n",end)` returns <=pos so `newlinePos>pos` is FALSE and end stays at pos+1024 => 1025 chars split into **1024 + 1**, and the 1-char remainder is PERMANENT. Next insert hits the full chunk and repeats.
0831-NUMBERS (reuse). Fixed-offset repeated insert, 5 lines x 6000 chars: chunks 30/78/118/178/278/1078/4030 @ k=0/1000/1040/1100/1200/2000/4000 = **EXACTLY +1 chunk per insert after k~1038, no ceiling** (ideal 32, median chunk size 1 char). vs rope rebuilt over BYTE-IDENTICAL text (34 chunks): lines(0,5) 1.05->39.9us=**38.0x**, insert 1.84->29.2us=**15.9x**, 3/3 both. PERMANENT: from 3078 chunks, +4000 appends=3082, +4000 sequential inserts=3137; only deleting the typed text back recovers (->97).
**0831-THE BOUND IS MOST OF THE FINDING (say it first, it cuts against me): FORWARD TYPING DOES NOT REACH IT.** 200 lines x 3000 chars, 4000 keystrokes: plateaus 1080 chunks vs 604 ideal (1.8x); line() read **1.0x (0/3)**, 50-row viewport lines() **1.0x (0/3)**; ONLY insert is worse, 7.52 vs 5.34us = **1.41x, 6/6**. End-to-end Editor.dispatch insertText drift over 3000 keystrokes @ lineLen 40/200/1200/5000 = 0.97/1.02/1.20/1.05x = **UNDETECTABLE** (keystroke is 320us-1.3ms, dominated by the #766/#684 O(doc) cost). => NOT user-visible today; becomes visible only once those land. Found **NO editor command** reaching the unbounded case: repeated `indent` 2000x on a 6000-char line = 1.0x CLEAN, repeated delete() = 1.0x CLEAN; replace() at a fixed anchor = 3.3x but src/ has NO composition path (grep: only 2 comment mentions of IME in input-handler.ts).
0831-"WOULD THE SUITE CATCH IT" (best actionable half): `Insert character - 1K buf (isolated)` ALREADY has the right shape — **benchmarks with setup() ACCUMULATE state across all iterations** (100 warmup + 1000 iters mutate ONE buffer at fixed offset 500). Only the fixture is too short. Same byte size, varying line length: drift(last100/first100) = 0.9x repo-as-shipped / 1.3x@512 / **1.5x@1100** / 1.2x@3000 / 1.4x@8000, 3/3 rounds stable to ~1%. **ALL STILL PASS** (0.06 vs 0.2ms target = 3.3x slack) = #703 in miniature.
**0831-10th ATTRIBUTION OVERTURN, caught before it became the headline:** my FIRST fixture inserted at `rope.length/2` EVERY iteration (midpoint drifts +0.5 char/keystroke) and reported **39.5x fragmentation**. Nobody types that way. Rebuilt with explicit fixed/sequential/append MODES; sequential-on-short-lines is totally clean (1.9x, no drift), and THAT contrast is what isolated the newline-free condition. **When a fixture shows a huge number, name the access pattern you actually implemented and ask if a human does that.**
0831-SELF-CORRECTION I had to make mid-run: I first claimed "no benchmark applies a SEQUENCE of edits to one buffer". WRONG — setup() runs once and fn() mutates the same buffer 1000x, so sequential edits ARE covered. The real gap was the fixture DATA, not the loop shape. Checked before writing it up.
0831-GREP-BEFORE-FILING PAID OFF AGAIN (8th time): searched textToChunks/TARGET_CHUNK_SIZE FIRST and found **#545 (Repo Assist, 05-06) documents the SAME fall-through under the SAME condition** — surrogate pair split at the 1024 cut => byteLength overcount. NOT a dup (different symptom/axis) but same lines. Filed mine separately + cross-referenced, and added a #670 SA "decide how to repair textToChunks": their minimal fix (extend end past a trailing high surrogate) fixes only theirs; a MERGE/COALESCE step fixes BOTH (a boundary that doesn't exist can't split a pair).
0831-T5: commented ONCE, on **#545** (0 prior PI comments). Said plainly that my finding does NOT make theirs more urgent — it changes the FIX SHAPE worth considering. Did NOT re-comment #739/#703/#716 (all carry mine, no human replies) — routed via #670 SA. 5th run in a row declining a re-comment.
0831-T4: #310/#339/#565/#575 ALL open+draft+mergeable:true, no conflicts. #565 `unstable` = the parked-CI gate (#757, action_required, 0 check runs), NOT my changes => NO pushes. Memory rule held.
0831-FIXTURE (rebuild each run, /tmp/gh-aw/agent/drift, NOT committed, PR hold intact): SRCROOT placeholder + sed (static imports); `ln -s <repo>/node_modules`. Read chunk count via private `(r as unknown as {_chunks:readonly{text:string}[]})._chunks`. **THE CONTROL THAT MAKES THE WHOLE THING: compare every fragmented rope against `Rope.from(r.text())` — a rope over BYTE-IDENTICAL text — so the delta isolates LAYOUT from CONTENT.** Assert `dirty.text()===rebuilt.text()` before timing. Editor arm: mb has no public buffers(), reach the rope via `(mb as ...)._buffers` then `._rope`. Bun.nanoseconds, 200 warmup + 400 iters, median, arm order alternated per round. Removed the dir; `git status --porcelain` EMPTY.
